# VerdictUI — Deliberate Non-Decisions (no.md)

Numbered list of things we deliberately did NOT do, so future sessions stop re-litigating them.

1. **No private SwiftUI API in the core path.** `_viewDebugData()` / `SWIFTUI_VIEW_DEBUG=287` / AttributeGraph interception are powerful but break across OS releases. They may ship later as an *optional* adapter target, never as a core dependency. Decided 2026-08-04 (three-option deliberation during product design; public-API instrumentation chosen as the spine).
2. **No external AX scraping as the primary channel.** `AXUIElement` trees depend on developer labeling and permissions; they are the *cross-validation* channel (middle loop), not the source of truth. Decided 2026-08-04.
3. **No web backend in Waves 1–9.** The contract (CLI/MCP verbs, verdict schema) is designed platform-agnostic from day one, but the web adapter is deferred until the SwiftUI backend is proven — web engines already exist free (CDP/Playwright); SwiftUI is the differentiated wedge. Decided 2026-08-04.
4. **No proprietary licensing for the engine.** Open-core: MIT engine for adoption, monetize the hosted workflow layer later. Decided 2026-08-04 after competitive research (all serious competitors are free; closed dev CLIs get zero adoption).
5. **No CoreGraphics types in the kernel.** `Rect` is hand-rolled so `VerdictUIKernel` compiles on Linux CI for pure-logic tests. Decided 2026-08-04.
6. **No path-only `TreeDelta`, and no `relocated` case.** The Wave 1 plan sketched `added: [NodePath]`; the shipped `TreeDelta` uses `added: [NodeAddition]` carrying the added subtree, and concretizes the `moved`/`changed` tuples as `NodeMove`/`NodeChange` structs (tuples are not `Codable`). Reason: `TreeDiff.apply(_:to:)` must reproduce the after-tree exactly — that round-trip is the property-test invariant and the reason Wave 7 can ship a delta instead of a full tree; a bare path tells an agent that *something* appeared but not what. For the same reason a re-parented node is reported as a removal plus an addition rather than a `relocated` entry: structure then always replays exactly. An explicit `relocated` case can be added later as a non-breaking hint. Decided 2026-08-04 (Wave 1 Task 2).
7. **No `warning` finding emitted by the harness when the host size is clamped.** The Wave 2 plan's risk list said "cap by explicit viewport, emit a `warning` finding when clamped"; the shipped `OracleHost` caps and exposes `wasClamped` as a fact instead of synthesizing a finding. Findings are the kernel's output and the kernel is deliberately blind to how a tree was produced — a harness that injects findings into a verdict it does not own would break that boundary. The caller that assembles verdicts (Wave 6's CLI, or any consumer of `DemoReport`'s shape) reads `wasClamped` and attaches the warning there. Clamping is never silent: `OracleHostTests` pins the flag on both the capped and the nonsense-size paths. Decided 2026-08-04 (Wave 2 Task 3).
8. **No `accessibilityReduceMotion` pin in the oracle host.** The plan's Task 3 listed `accessibilityReduceMotion = true` among the environment pins; it cannot be written — the `EnvironmentValues` property is get-only, so there is no `WritableKeyPath` to pin through, which the compiler states plainly. Wave 2 renders no animations, so no tree depends on it; Wave 3, which drives animations deliberately, controls them with `Transaction(animation: nil)` instead of asking the system to. Documented on `View.verdictPinnedEnvironment()` ("the one pin that could not be made"), which is where all seven pins that *could* be made live. Decided 2026-08-04 (Wave 2 Task 3).
9. **No partial JSON from a failed demo run.** When one scenario in `DemoReport.verdicts()` times out, the already-computed verdicts for earlier scenarios are discarded and the run fails with exit code 1, the error (naming the scenario) on stderr, and *nothing* on stdout. The alternative — printing the verdicts that did settle — was rejected because a partial array parses cleanly and reads as a smaller catalog: a consumer diffing against six baselines would see three "missing scenarios" instead of one named failure. Stdout's contract is binary: one complete document, or empty. Decided 2026-08-04 (post-Wave-2 audit, correctness reviewer finding P1).
10. **No test for a probe that wraps zero subviews, and no rewrite to avoid needing one.** `ProbeLayout.measure` ends in `union ?? .zero`. Two attempts to reach that fallback — `EmptyView().probeLayout(id:)` and an empty `ForEach` — both produced *no measurement at all*: SwiftUI elides empty content before a custom `Layout` is instantiated, so `sizeThatFits` is never called and there are no subviews to be zero. The branch is unreachable through the public modifier. The first attempt at resolving that replaced the optional accumulator with `reduce(into: .zero)`, which makes the empty case an identity element instead of a branch — and was reverted, because it puts a `max(0, _)` on the **single-subview** path, where `max(0, -5)` and `max(0, .nan)` are both `0` in Swift (verified, not assumed). A wrapper documented as returning its subview's answer byte-for-byte would then silently rewrite a negative or NaN dimension. Trading an unreachable `??` for a reachable misstatement is a bad trade, so the fallback stays and is documented as unreachable at the call site. The general rule: prefer deleting a branch no test can enter, but not when deleting it changes behaviour on a path tests *do* cover. Decided 2026-08-04 (post-Wave-2 audit, test-quality reviewer finding #4; reverted same session).

11. **No separate `Tests/VerdictUIBenchTests/` target.** Wave 3 Task 6 specified the benchmark harness as its own test target; it shipped as `Tests/VerdictUIProbeTests/HarnessPerformanceTests.swift` instead. Reason: the benchmark measures `Harness.perform()`, which needs `OracleHost`, `VerdictScenario`, `ProbeAction` and the demo scenarios — the whole probe surface the existing target already imports. A separate target would duplicate that dependency set, add a second place for the `@MainActor`/autoreleasepool test-host boilerplate to drift, and buy nothing, since `swift test --filter HarnessPerformanceTests` already selects it precisely and `stage_runtime_bench` filters on exactly that name. The plan's own reason for a separate target was isolation from unit-test runtime, which `--filter` provides without a target. `docs/implementation-plan.md` still names the target that was not built, which is how this went unrecorded until the Wave 1–3 audit found the dead path. Decided 2026-08-06 (Wave 3 Task 6; recorded retroactively during the Wave 1–3 audit).

12. **No test pinning `timing.settleMs` on the settle-TIMEOUT path.** The audit found `perform` assigning the *requested* timeout to `settleMs` on that path — a budget reported as if it were a measurement — and the source fix (measure with `ContinuousClock` across the `settle` call, on both branches) is committed. What is *not* committed is a test that discriminates the two, and three attempts failed for the same reason each time: the assertions that seemed to separate a measurement from a budget (`> 0`, `<= elapsedMs`) are satisfied by both. A fourth attempt compared against the budget directly and still passed under mutation, because `.custom("mover")` does not reach a settle bound by `perform`'s timeout at all — the measured value was 3000 ms against a 120 ms budget, i.e. the test had been exercising a different path than its name claimed since the first version. Rather than keep a green test that proves nothing, it was deleted. The honest state: the fix is right and is covered indirectly by `testSettleMsIsMeasuredNotAssumedOnTheSettlePath` (happy path) and `testSettleMsIsZeroWhenNoSettleRan` (no-settle path), while the timeout path is unpinned. Closing it needs a fixture whose settle genuinely runs to a caller-supplied deadline — tracked in CTS-8DDE6D09. **The rule this cost: an assertion that both the correct and the incorrect implementation satisfy is not a weak test, it is not a test.** Decided 2026-08-06 (post-audit sweep).

13. **No p95 assertion in `stage_runtime_bench`, and no widening of the SLO to restore one.** The stage gated p95 against SLO 1's 100 ms until 2026-08-07, when it blocked the PM at p95 105.51 ms on a p50 of 49.09 ms — while two isolated runs of the same commit gave 58.43 and 77.01 ms. `HarnessPerformanceTests` had **already decided this**: it records p95 and asserts p50, because p95 moves 56.7 → 106.7 ms purely with contention while p50 sits at 49.6–51.2 ms in every context measured. The decision was made in the test and reversed one level up by a consumer that re-derived its own verdict from the same printed line. The tempting fix — raise the budget until it stops failing — was rejected: a threshold moved to silence a failure is a silencer (SE Principle 11), and the number was never wrong; the *statistic* was. The stage now gates p50 at 70 ms and reports the tail as evidence, verified live on a run reading p50 49.93 ms / p95 101.99 ms — one the old gate would have failed while the median sat at half budget. **The general rule: when two layers both judge the same measurement, only one of them owns the claim. A consumer that re-derives its own verdict silently outvotes the decision, and no test sees the disagreement unless something compares the two thresholds** — which is why `test_the_gated_budget_agrees_with_the_swift_test` parses `performP50BudgetMs` out of the Swift source. Decided 2026-08-07 (CTS-9686A8BB).

14. **Never run any command against the working tree while `scripts/mutation-check.py` is sweeping.** The harness edits source files in place for the length of the run and restores each one after. A concurrent write — a `pytest` invocation, a `git checkout`, an editor save, another probe — corrupts both the sweep's verdict and the probe's, and the corruption is **silent in the expensive direction**: the harness prints UNNOTICED, which reads as "this guard is untested" rather than "something moved the file under me". On 2026-08-07 that cost about an hour. Two consecutive sweeps reported the same row UNNOTICED, and every isolated re-check of that row — including driving the harness's own `check()` on it — returned NOTICED. A `__pycache__`-staleness theory was constructed, tested by deleting the cache, and **falsified**; the actual cause was that both sweeps had been run in the background while foreground commands touched the same files. **Amended 2026-08-07 (same day, later session): the acquitted theory was real after all, as a SECOND and independent mechanism** — the pytest witness genuinely does read stale bytecode when two rows touch one file inside the same second (measured: identical 14-byte size, mtimes `…352.32` and `…352.78`, the mutated module read as unmutated), and it is now closed by `-B` on the harness's pytest argv (`no.md` #16). Deleting the cache falsified it *that* time because the collision is a race and had not occurred in the run being examined. The correction matters more than the fact: **a theory tested once and acquitted is acquitted for that run, not for the class** — and the two mechanisms here are indistinguishable from the outside, since both print UNNOTICED for a guard that works. A clean foreground sweep with nothing else running returned 54/54 NOTICED with a byte-identical restore. The harness refuses to *start* on a dirty tree, which shows the authors knew tree ownership matters — but it holds that as a precondition, not an invariant, so a mid-run write is invisible to it (CTS-8795E0FE proposes per-row hashing or a dedicated worktree). Until that lands the rule is operational: start the sweep in the foreground and wait, or give it its own worktree per CLAUDE.md rule 5. **And the wider lesson, which is the one that actually cost the hour: when a verification tool contradicts a direct measurement, suspect the apparatus before the subject — the same failure recorded one session earlier, when a deleted `output-file-map` was read as a test verdict for several turns.** Decided 2026-08-07.

15. **No absolute SLO 1 assertion on a shared CI runner, and no widened budget to make one fit.** `testPerformCycleMeetsTheSLO1Gate` asserted p50 < 70 ms everywhere. On GitHub's macOS runner the p50 for **unchanged** code across five consecutive `main` runs was 119.9 / 86.6 / 96.6 / 86.0 / 74.2 ms — a 62% spread bracketing the gate from both sides — while the same commits measured 48.45 ms locally in isolation *and* passed the full 357-test suite under `swift test --parallel`, i.e. under maximum local contention. The gate returned FAIL for a busy neighbour and PASS for the same code minutes later, so it could not fail for the reason it existed. Its message made this worse by asserting the opposite ("the median is load-stable, so this is a real regression rather than contention") — a sentence five CI runs had already falsified, and a gate that misdescribes its own failure teaches its reader to discount it. This is the **p95 decision applied one metric short**: p95 became record-only after moving 56.7 → 106.7 ms purely with contention; the median is load-stable *on one machine*, which justifies gating it locally, but it is not comparable *across* machines. So p50 is now asserted on developer hardware and recorded on a shared runner, and the CI lane still runs every assertion that proves the benchmark RAN (sample count, finiteness, per-cycle PASS, non-empty delta) — a benchmark that silently stopped is still a hard failure in both. **Enforcement moved, it did not weaken**: `stage_runtime_bench` runs this same test pre-push on developer hardware, gates the median at 70 ms, and fails closed when the `SLO1-PERFORM` line is absent. Verified in all four quadrants by planting a real latency regression (settle floor 0.030 → 0.060): dev lane FAILS at p50 85.46 ms, CI lane PASSES and records; on healthy code both pass and each prints its line. The lane is spelled ONCE as `assertsP50Locally`, because two inline `if Self.isSharedCIRunner` checks would be two places to invert and reverting either alone would leave the other's test green — the two-cooperating-lines shape that made a Wave 4 guard UNNOTICED by construction. **And the witness is `testTheP50GateIsAssertedOnDeveloperHardware`, which reads the CONSEQUENCE rather than the predicate**: asserting `XCTAssertFalse(isSharedCIRunner)` would pass whichever way the `if` branches, since inverting it leaves the property untouched (`no.md` #12 again). Decided 2026-08-07 (CTS-2D400CAA, diagnosed by a prior session that deferred the edit for want of a local red; the red was then produced by planting the slow floor with `CI=true`, p50 82.01 ms).

16. **No mutation row for the `-B` flag on the harness's own pytest argv, and no wrapped-line anchor to force one.** `run_named_test` passes `-B` so a pytest witness compiles the source the harness just wrote instead of reading `__pycache__`. Every guard here gets a mutation row (CLAUDE.md rule 6), and this one cannot have one. A `Mutation`'s `old=` must quote the text it replaces; the only text identifying that flag is the argv itself; the catalog lives *inside* `mutation-check.py`, so the row's own string becomes a second occurrence and `--verify-targets` refuses it — correctly, since the harness could not say which of the two sites it broke. Three progressively narrower one-line anchors all self-matched. A line-spanning anchor (the technique the other three `mutation-check.py` rows use, where indentation and `\n` make the target unquotable) does work — but only against a wrapped call, and `ruff format` collapses that wrap back to one line immediately, so the row would rot on the next format run rather than fail loudly. The guard is covered by `test_a_mutated_module_is_not_served_from_stale_bytecode`, whose control was run **by hand in both directions**: red with `-B` removed, green with it present. That is exactly what a mutation row would have proven, minus the automation. **The defect it closes is worth stating, because it is the expensive direction:** six pytest rows target `scripts/verdictui-pm.py` in sequence, CPython validates its bytecode cache on mtime *plus size* at one-second granularity, and two rows landing in the same second make the witness judge the *previous* row's compile — so a working guard reports UNNOTICED, which reads as "untested" and invites rewriting correct code. Measured 2026-08-07: exactly one of six went UNNOTICED in a full sweep and NOTICED when re-run alone; the mechanism was then reproduced directly (same 14-byte size, mtimes `…352.32` and `…352.78`, mutated module read as unmutated). **The general rule: a catalog that lives inside its own subject cannot name that subject's code without matching itself — and when the only available anchor is one a formatter will rewrite, an honest hand-run control beats a row that will silently go stale.** Decided 2026-08-07.

17. **No timing assertion in a Codex repair sandbox, and no negative-control-free marker guard.** `no.md` #15 split the SLO 1 p50 lane — asserted on developer hardware, recorded on a shared runner — and enumerated that lane as `CI` plus an explicit `VERDICTUI_RECORD_TIMING_ONLY` override. A **Codex repair sandbox** (`CODEX_CI=1`, `CODEX_SANDBOX=seatbelt`) is a fourth member of that same class and was in none of them, so it was treated as developer hardware and asserted a median it cannot hold: measured 2026-08-09 at **p50 167.50 ms and 394.97 ms** against the 70 ms budget, on source that measures ~49 ms locally. That produced three P1 tickets (CTS-19EEF979, CTS-2CD56051, CTS-3FD84961) describing a latency regression that does not exist — the expensive failure mode #15 already names, since a gate that fails for the environment teaches its reader to discount it. The markers now live in ONE tuple, `CONSTRAINED_TIMING_ENV_MARKERS`, read by the PM, and mirrored by `isSharedCIRunner` in the Swift lane. **The finding worth keeping is not the marker list; it is what the marker tests could not see.** Each new test asserted that a marker turns record-only ON, and every one of them is satisfied by `return True` — measured directly: with the guard replaced by `if True`, the **full Python suite passed 199/199**. Record-only everywhere means the p50 budget is enforced *nowhere*, on every host, while the suite reads green — a gate that has stopped being a gate with no test able to notice. This is `no.md` #12 in its purest form: an assertion satisfied identically by the correct and the broken implementation is not a weak test, it is not a test. Closed by `test_unmarked_writable_host_still_asserts_its_timings`, the negative control — an unmarked host with a writable SwiftPM cache must assert its timings — verified in both directions (passes on correct source, exit 1 on the mutation, byte-identical restore) and pinned by a mutation row so the direction that matters cannot silently disappear again. **The general rule: a predicate whose tests only ever exercise the TRUE branch cannot distinguish a working rule from one that is always true, and when that predicate decides whether a budget is enforced, the always-true form disables the budget everywhere and reports success.** Decided 2026-08-09.

18. **`no.md` #12 is CLOSED — the settle-timeout `settleMs` test exists, and what unlocked it was the FIXTURE, not the assertion.** #12 recorded four failed attempts and concluded the timeout path was unpinnable; it was wrong about the reason, which is why the fifth attempt succeeded. Two things were missing at once. **First, the discriminator.** `> 0` and `<= elapsedMs` are satisfied by a budget as readily as by a measurement, so any test built on them passes against both implementations. The separating fact is the **overshoot**: a settle that gives up at its deadline has, by then, spent strictly MORE than the deadline — it must notice the expiry and unwind — while an implementation echoing the budget reports exactly `150.0` for a 150 ms budget and can never exceed it. Measured before it was asserted: 150.65 ms against 150 ms and 600.59 ms against 600 ms; under the budget-echoing mutation, exactly 150.0 and 600.0, and the test fails naming both. **Second, and this is the half #12 never diagnosed: every prior attempt was exercising a DIFFERENT PATH than its name claimed.** They used `PerpetualMotionScenario`, a layout that moves from the start — and `perform` calls `currentTree()` *before* the settle, which carries its own 3 s deadline and **throws** against a layout that never settles. So the run took the host-error branch, and the number under test was 3002 ms of capture cost against a 120 ms budget. #12 recorded that 3000-vs-120 measurement and read it as "`.custom` does not reach the settle"; the real cause was the capture in front of it, which no change of action could fix. The fixture had to be static until acted on: `LateMotionScenario` settles cleanly on the first capture and starts moving only on the tap, so the settle *after* the act is the one bound by the caller's deadline. **The general rule, and it generalises past this test: when an assertion cannot be made falsifiable, suspect that the fixture is not reaching the code under test before concluding the property is unpinnable — a test whose name says one path and whose fixture reaches another is not weak, it is aimed somewhere else, and every assertion you add to it will keep passing for the wrong reason.** Decided 2026-08-09 (CTS-8DDE6D09 item 2; supersedes `no.md` #12's conclusion, not its analysis of why the obvious assertions fail).

19. **No file-scope split of `ProbeLayoutTests.swift` that widens its fixtures, and no rename-to-fit.** The file is 999 lines against an 800 advisory limit (CTS-D408B8FC), and the obvious split — fixtures into `ProbeLayoutFixtures.swift`, tests staying put — was attempted and **reverted on compiler evidence**. Swift has no cross-file-but-not-cross-target access level: `fileprivate` reaches one file and the next level up is `internal`, which reaches the whole target. So the split requires widening **12 top-level `private` declarations** at once, and the compiler then reports `invalid redeclaration of 'HeadlessHost'` — because `VerdictUIProbeTests.swift` declares its **own** `private final class HeadlessHost<Content: View>` (verified: both files, `private`, identical name). The two coexist *only* because `private` confines each to its file. **That is the finding: `private` here is not stylistic, it is what lets sibling test files own same-named fixtures independently, and a line-count-driven split silently converts that into a namespace collision.** The available workarounds are all worse than the 199 extra lines: renaming one `HeadlessHost` makes two things with one job answer to two names, and nesting the fixtures inside an enum adds a qualifier to every call site in 686 lines of tests to satisfy an advisory threshold. The limit exists to keep files readable; this file is already sectioned (`// MARK:` — headless host, frame measurement, bounds observation, tests) and its length is 12 fixtures plus one coherent suite, not sprawl. **The general rule: a size limit is a proxy for readability, and when the mechanical fix to the proxy makes the code worse, fix the readability instead and record why the number stands.** Revisit if `ProbeLayout` grows a genuinely separate subject — then the split follows a real seam and carries its fixtures with it. Decided 2026-08-09 (CTS-D408B8FC).

20. **`no.md` #16 is CLOSED — the `-B` guard has a mutation row now, and getting one was a side effect of splitting the catalog out of the harness.** #16 recorded a guard that could not be rowed: a `Mutation.old` must quote the text it replaces, the only text identifying the `-B` flag is the argv itself, and the catalog lived *inside* `mutation-check.py`, so the row's own string became a second occurrence and `--verify-targets` refused it — correctly, since the harness could not say which of the two sites it had broken. Three progressively narrower one-line anchors all self-matched; a line-spanning anchor worked only against a wrap `ruff format` immediately collapses. The conclusion drawn was "an honest hand-run control beats a row that will silently go stale", and that was right *given the file layout*. **What #16 never questioned was the layout itself.** Splitting the catalog into `mutation_catalog.py` for size (CTS-47AA0547) removed the constraint entirely: two files that do not contain each other's text can quote each other freely, so `old='return run([sys.executable, "-B", "-m", "pytest"'` now matches exactly one site. Row added, hand-verified NOTICED (exit 1 on `test_a_mutated_module_is_not_served_from_stale_bytecode`) with a byte-identical restore, and `--verify-targets` reads 64. The narrower rule that replaces it: **a catalog may not quote text from the file the catalog itself lives in** — rows targeting `mutation_catalog.py` would self-match there for exactly the same reason, which is now a small and clearly-bounded exclusion rather than a hole over the whole harness. **The general rule: when a guard is "impossible to test", check whether the impossibility is a property of the guard or of where the code happens to sit — a structural constraint that looks intrinsic is often just a file boundary nobody had reason to move.** Decided 2026-08-09 (CTS-47AA0547; supersedes `no.md` #16's conclusion, not its analysis of why the self-match occurs).

21. **`no.md` #14's operational rule now has a mechanical half — a row whose file is written mid-run ABORTS rather than scoring.** #14 recorded that a concurrent write during a sweep corrupts the verdict *silently in the expensive direction*: the harness prints UNNOTICED, which reads as "this guard is untested" and invites rewriting correct code, and it cost about an hour on 2026-08-07. The harness already hashed the file before and after the RESTORE, so it could detect a botched restore — but that check runs after the row is scored, and `main`'s clean-tree check is evaluated *between* rows. Neither can see the window that actually matters: the test run itself, which is where all the time goes. `check()` now records the sha256 of the mutated content it wrote and re-reads it after the witness returns; a mismatch exits 3 naming the file, instead of classifying a result produced against a tree nobody intended. **The mutation output is the argument for it**: with the guard removed, the harness prints `NOTICED` for a row whose subject had been rewritten mid-run — a confident verdict about the wrong bytes, indistinguishable at the point of use from a real one. The control (an untouched file still produces its verdict and restores) is what stops "aborts on meddling" being satisfied by a harness that aborts on everything. **This does NOT retire #14's operational rule** — the guard converts a silent wrong answer into a loud refusal, which is strictly better but still a stopped sweep; running in the foreground on an exclusive tree remains how you get a *result*. **The general rule: a precondition checked once is not an invariant, and the gap between the two is exactly the interval where the expensive work happens.** Decided 2026-08-09 (CTS-8795E0FE).

22. **No split of `verdictui-pm.py` that moves `_pm_log` out of it, and no chasing the 800-line advisory across the three files now sitting just over it.** The obvious extraction — the Swift toolchain layer (lock files, zombie processes, the timing lane, the streaming runner; ~206 lines) into `verdictui_swift_runner.py` — was **built and reverted on test evidence**. It compiled and the import shim was clean, but 25 tests went red, and the instructive one is `test_pm_log_routes_through_logging`: `_pm_log` uses `logging.getLogger(__name__)`, so moving the function **changes the logger name every PM record is emitted under**, from `verdictui_pm` to `verdictui_swift_runner`. That is not a test needing an update — it is a real change to what the PM's log output looks like to anything that filters by logger, and the test was correctly reporting it. A file-size refactor that silently re-labels every log record is not a refactor. **The three files this session left just over the line are 923 / 878 / 814 against 800**, and each has the same shape of reason: `verdictui-pm.py` is 20 stages with no seam that does not move a logger or thread a dozen constants; `mutation_catalog.py` is a flat list of 67 rows whose only possible cut is an arbitrary row number (`no.md` #20); `test_mutation_check.py` is one subject — the harness — tested from several angles. **The 800 figure is a fleet CEO-audit heuristic and is not enforced by this project's PM** (verified: nothing in `verdictui-pm.py` or `floor-check.py` reads it), which is the right level for it, because it is a proxy for readability rather than a contract. Where the proxy and the thing it proxies disagree, the thing wins. **The general rule, and it is the same one as #19: when the mechanical fix to a size limit makes the code worse — a re-labelled logger, a renamed fixture, an arbitrary cut — take the readability win you can get (sections, docstrings, honest boundaries) and record why the number stands, rather than shipping a worse file with a better line count.** Decided 2026-08-10 (CTS-7AEBCC56, CTS-46EF20D6, CTS-0D2246C8).

23. **No macro mutation row whose witness is a RENDER test, and no trusting a `--filter` run to have re-expanded the macro.** The Task 2 session found by reading — and did not test — that an explicit `.verdictProbe` on an outer container short-circuited `BodyProbeWalk.rewrite`, returning the whole expression and swallowing everything nested inside it. Measured 2026-08-10: `VStack { Text("inner-alpha"); Text("inner-beta") }.verdictProbe("outer-container", role: .container)` rendered to a tree of **exactly one node, `["outer-container|container|"]`** — no content at all. That is this product's worst defect shape, and worse than the empty-tree case the `vacuous-verdict` guard exists for: that guard fires only when NO probed node exists, and the container's own probe makes the tree look observed, so every rule reports PASS about content nobody instrumented and no layer anywhere reports the gap. The fix is one word — recurse first, then suppress — because suppression must be **positional** ("do not probe THIS position") and never a decision to stop walking. **The finding that outlives the bug is about the WITNESS.** Two tests cover the fixed behaviour: an expansion snapshot and a runtime render test. Under the mutation, only the snapshot fails. SwiftPM rebuilds the plugin but does **not** re-expand macros in a consuming target whose own sources are unchanged, so the render test executes the PREVIOUS expansion and passes — verified in all four quadrants: mutated + untouched test file → **passes** (proves nothing), mutated + `touch` the test file → fails naming both swallowed elements, restored → 23/23 green with a byte-identical restore. So a macro row must name a test in the module that expands the macro at build time, and a render test — the layer that looks most like real-world evidence — is the one that silently cannot see a plugin regression. This is also why an earlier attempt at this fix read as "the fix did not work": `swift test --filter` had rebuilt the plugin and reused the stale expansion, which is `no.md` #14's rule arriving in a new costume — **when a verification tool contradicts a direct measurement, suspect the apparatus before the subject.** Decided 2026-08-10 (Wave 4 Task 3 prerequisite).

24. **No `@ViewBuilder` walk that recognises only expression statements, and no `.trimmed` statement re-insertion that drops the item's leading trivia.** Task 4's job was snapshot coverage for the shapes the plan names — generic view, conditional content, `ForEach`, nested custom views. Writing them found that two of the four were not covered because they did not *work*, and both defects had shipped green through 396 tests. **(a) Conditional content was entirely unprobed.** An `if` or `switch` inside a `@ViewBuilder` is a **statement**, not an expression; `rewriteClosure` mapped `item.item.as(ExprSyntax.self)` and returned anything else untouched, so every element in every branch went uninstrumented. Measured: `VStack { if isOn { Text("on") } else { Text("off") } }` expanded with both `Text`s bare. This is the same false-green shape as `no.md` #23's swallow and is invisible for the same reason — the container's other probed children make the tree look observed, `vacuous-verdict` fires only when NO probed node exists, and every rule then reports PASS about content nobody instrumented. **(b) `ForEach` expanded to source that does not compile.** `rewrite` returns a `.trimmed` expression, and the statement's own leading trivia is the only thing separating a closure's `in` from its first statement — so `{ row in` + `Text(…)` became `{ row inText(…)`. `ForEach` is the commonest list shape in SwiftUI, so `@Verifiable` was unusable on any view containing one, and no test in the suite used a closure *with a signature*. **The finding that outlives both is about how the shapes were found: they came from PRINTING what the macro actually emits for each named shape before asserting anything.** Three of my four first-draft expectations were wrong — two on indentation, and one substantively: I asserted `ForEach`'s probe would carry `text:`, but `Text(item)` takes a variable and `literalTextArgument` correctly declines to forward it, so the test was demanding a fabrication the macro is documented never to make. Had the expectations been written from reasoning and then "fixed" until green, the ForEach test would have been repointed at whatever the broken macro produced. **The general rule: when adding coverage for a shape nobody has exercised, measure the current output FIRST and read it as a finding — a snapshot test written from expectation alone tends to converge on the implementation rather than on the requirement, and the shapes with no coverage are exactly the ones most likely to be broken.** Both fixes carry mutation rows witnessed by expansion snapshots per `no.md` #23, hand-verified NOTICED in both directions with byte-identical restores; the conditional row keeps `case .stmt` as a no-op rather than deleting it, so the switch stays exhaustive and the mutation compiles. Decided 2026-08-10 (Wave 4 Task 4).

25. **No lint diagnostic keyed on the plan's wording without measuring what the macro does, and no mutation whose `new` leaves a `var` unmutated.** Task 5 names two diagnostics: duplicate explicit ids → error, and "an interactive element with no derivable ID (unlabeled closure-only Button)" → warning with a fix-it. The first is exactly right. **The second describes a defect that does not exist.** Measured 2026-08-10 before writing any assertion (`no.md` #24's rule): `Button(action: save) { Image("gear") }` expands to `.verdictProbe("Row.button.0", role: .button)` — the id derives fine. What is absent is `text:`, because the only literal in the expression belongs to the nested `Image` and `literalTextArgument` correctly declines to forward a non-literal. So the real defect is narrower and worth naming precisely: an interactive element the verdict can **locate but not NAME** — `TruncationRule` has nothing to read, no assertion can reference the control by what it says, and a human reading a verdict sees an anonymous button among several. Implemented against the measurement, not the wording, with `Button("Save")` and an explicitly-probed button as controls so "warns about an unlabelled button" cannot be satisfied by an implementation that warns about every button. Severity split is deliberate and is not a preference: a duplicate id has no reading under which the author meant it, while an image-labelled button is ordinary correct SwiftUI, so escalating it would make `@Verifiable` refuse to compile working code — how an adoption-focused tool gets deleted rather than fixed. **The finding that outlives the feature is about the mutation harness.** The scenario-macro row's obvious `new` — delete the `walk.rewriteStatements(...)` call — leaves `var walk` never mutated, which under `-warnings-as-errors` is an **error**, so the build failed and the row reported NOTICED **having executed zero tests**. Exit 1 from a compiler is indistinguishable at the harness's boundary from exit 1 from a failing assertion, so a row that cannot build looks exactly like a guard that works. The row now keeps `_ = walk.rewriteStatements(closure.statements)` and executes 1 test that genuinely fails. This is the third occurrence of that shape here (Wave 4 Tasks 2, 3, and now 5), which is why it is a rule rather than an incident: **a mutation's `new` must still COMPILE, and the check is not "does the row score NOTICED" but "did the witness RUN" — grep the output for the runner's summary line before believing any verdict.** Also: Task 5 made `rewriteStatements` internal and deleted the scenario macro's own local statement map, which was a second implementation of one rule and carried the Task 4 conditional defect independently — fixing the view macro had left the scenario macro broken and green, with no test on either side able to see the other's copy. Decided 2026-08-10 (Wave 4 Task 5).

26. **`no.md` #23 has a THIRD costume, and it is the dangerous one: a stale expansion after a RESTORE reads as a regression in correct code.** #23 recorded that SwiftPM rebuilds a `.macro` plugin but does not re-expand macros in a consuming target whose own sources are unchanged, so a render test executes the PREVIOUS expansion. Two consequences were already known — a mutated plugin passes (proving nothing), and a hand-check of a plugin change measures nothing unless the consuming file is `touch`ed. Measured 2026-08-10, the third: after hand-verifying a mutation and restoring the source **byte-identically** (sha256 confirmed), the full suite went red on the two tests the fix had made green minutes earlier, reporting `vacuous-verdict` and an empty tree — the exact symptom of the bug that had just been fixed. Source was intact (`grep -c verdictProbing` = 4); the test binary was still running the MUTATED expansion. A `touch` of the consuming test file returned 3/3 green and the full suite to 414/0. **This is worse than the other two because the failure is loud, specific, and points at the feature you just shipped.** The mutated-passes case merely fails to inform; this one actively argues that correct code is broken, at the moment you are least able to dismiss it — right after touching that code. The reflex it defeats is the right one: a red test after an edit normally means the edit. **The rule: after any hand-applied mutation of a macro plugin, `touch` every consuming test file before believing ANY subsequent run — red or green.** The restore is not complete when the bytes match; it is complete when the expansion has been regenerated. And the general form, which is `no.md` #14's rule pointed at a new apparatus: **a verification tool that caches a DERIVED artifact keyed on the wrong input will contradict a direct measurement in both directions, and the direction that costs most is the false red on code you just changed.** Decided 2026-08-10 (Wave 4 Task 6).

27. **No `VerdictPreviewAdapter`, and no `#Preview` bridging in Wave 4 — the only mechanism that could implement it is private SwiftUI SPI, which `no.md` #1 forbids.** The plan's Task 3 names it as a sub-deliverable ("`#Preview` bridging: `VerdictPreviewAdapter` re-exposes existing previews as scenarios where possible"), and it was never built. Found during the Wave 4 close by enumerating the plan's named artifacts against the tree rather than reading the task list as prose — the same miss shape as `no.md` #11, where a named test target was silently not built and went unrecorded until an audit. **The reason it cannot be built is a hard one, not effort.** `#Preview` does not expand to anything an author or another macro can name: the content is registered through `PreviewRegistry`, an underscored SwiftUI type (verified 2026-08-10 — it resolves, and it is SPI). Reaching it puts private API on the core adoption path, which `no.md` #1 rules out as the project's founding constraint, and the qualifier the plan itself wrote — "where possible" — is the answer: it is not possible on public API. **The honest alternative already exists and costs one line**: `#VerdictScenario("name") { MyView() }` next to the `#Preview`, which since ADR 2026-009 renders the view's PROBED content through `verdictProbing`. That is a duplicate declaration rather than a bridge, and duplication a reader can see beats a bridge that breaks on the next OS release. Revisit only if SwiftUI makes preview content public. Decided 2026-08-10 (Wave 4 close).

28. **`no.md` #26's touch discipline is now MECHANICAL for the mutation harness — and the row that proved it needed the fix had a note claiming a hand-verification the harness could not reproduce.** #23 established that SwiftPM rebuilds a `.macro` plugin but does not re-expand macros in a consuming target whose own sources are unchanged, so a RUNTIME witness executes the PREVIOUS expansion; #26 added that this bites in both directions and that the expensive one is a false RED on correct code. What neither recorded is that **the mutation harness itself was subject to it**, in the direction that reads as a coverage gap. Measured 2026-08-11: the row `the two macros stop composing over a custom view` reported **UNNOTICED** in a full sweep, and the identical mutation applied by hand — with `touch Tests/VerdictUIMacroTests/*.swift` first — reported **NOTICED at exit 1 with 1 test executed**, failing on `vacuous-verdict`, which is precisely the defect the guard exists to catch. The row's own `runtime_witness_reason` asserted "Hand-verified NOTICED (exit 1, 1 test executed) with a byte-identical restore" **and said the stale-expansion trap does not apply here**, reasoning that `verdictProbing` lives in the support library rather than the plugin so the consuming target recompiles normally. That reasoning is wrong, and the way it was wrong is the point: the *mutated symbol* was in the plugin (`BodyProbeWalk`), so what had to be re-expanded was the consuming target, regardless of where the overloads live. The note was true about the hand check and false about the harness, because the hand check followed a discipline the harness did not — **a verification that is only valid under a precondition the automated path does not satisfy is not a verification of the automated path**, and it read as one for a full wave. Closed by `refresh_macro_expansions()` on the Swift path of `run_named_test`, deliberately unconditional rather than narrowed to macro rows: the baseline and mutated runs must differ ONLY in the source, so a refresh on one and not the other would reintroduce the same asymmetry one layer down. Its own guard is negative-controlled in both directions (`test_the_swift_runner_restamps_macro_consuming_sources`, plus a pytest-path control so "restamps sources" cannot be satisfied by a harness that restamps unconditionally on every path) and carries a mutation row. **The general rule: when a row's note explains why a known trap does not apply to it, that explanation is a claim about the APPARATUS and deserves the same negative control as the guard itself — an UNNOTICED verdict and a working guard are indistinguishable until something re-runs the row under the discipline the note assumed.** Decided 2026-08-11 (Wave 5 Task 1).

29. **No `@ViewBuilder` branch in the host's environment chain, and no reasoning about SwiftUI environment precedence without measuring it.** Wave 5 Task 4 needed the opposite of what `OracleHost` was built for: the host PINS locale, colour scheme, dynamic type and layout direction so a machine's settings cannot change a verdict, and a variant sweep must vary exactly those axes. Three attempts, and the first two were wrong in ways worth keeping. **(a) I asserted in a doc comment that SwiftUI resolves the environment "outermost-last", applied the variant on the far side of the pin, and measured no change at all — five axes including an explicit 150x100 viewport produced byte-identical frames.** A direct probe (two `.environment(\.dynamicTypeSize,)` writes around one reader) settles it: `inner-medium-outer-ax5` reads **medium**. The writer NEAREST THE CONTENT wins, which is the reverse of what I had written down and then built on. **(b) The fix for that — a `@ViewBuilder` branching between "pin everything" and "pin only what a variant cannot express" — compiled, passed the sweep tests, and BROKE A `Button`.** `'Pay' never reached the tree`, while the sibling `Text` was fine. A `@ViewBuilder` `if/else` wraps its subtree in `_ConditionalContent`, which changes view identity, and a Button's label resolution did not survive it. Confirmed against a **pristine worktree at HEAD** — 10/10 there, 9/10 with the branch — because two earlier isolation attempts were void (one stashed a file whose dependent no longer compiled, so exit 1 meant a build failure; the other silently stashed nothing, since untracked files are not stashed by default, and I read the unchanged tree as evidence). The shipped shape writes **every axis unconditionally** and varies only the VALUES, so a pinned host and a swept host differ in no view type anywhere. **The finding that outlives the feature: a modifier chain that changes SHAPE between two modes is not two configurations of one host, it is two hosts — and the difference surfaces somewhere unrelated to the thing you were configuring.** Also recorded: **`dynamicTypeSize` is delivered but INERT on macOS** — the environment reads `accessibility5` while `Text` renders byte-identically at 13x18 pt, because macOS sizes text from `NSFont` rather than SwiftUI Dynamic Type. The axis stays in the vocabulary (it is real on iOS, and dropping it would silently delete the plan's "German string truncates at AX3" case) but a sweep varying only that axis on macOS produces cells that CANNOT disagree — a table of identical answers reading as clean. `SweepTests` is therefore keyed on `layoutDirection`, which moves `x` from 0 to 204, and the inertness is documented rather than asserted away. **A test written on an inert axis can never pass, and its failure accuses the engine.** Decided 2026-08-11 (Wave 5 Task 4).

30. **No expectation-DSL assertion supplied by the module under test, and no DSL conversion of the settle-correctness assertion.** Wave 5's exit gate asks for the DSL to be "dogfooded" over the demo scenarios, and there are two wrong ways to do it that both look like progress. **(a) The expectations may not live in the demo module.** `DemoScenarios.swift` already decided this for rule ids and the reasoning transfers unchanged: "an assertion written against a rule id supplied by the very module under test would pass whatever the module claimed." A `DemoScenarioEntry` that carried its own `ExpectationSet` would be the scenario grading its own homework — the tree would satisfy the expectations by construction, the test would be green forever, and the DSL would never have been exercised against anything. So the dogfood expectations go in the TEST target, which is the independent observer. **(b) Not every hand-written frame assertion is a DSL candidate.** The gate says "replace all hand-written frame assertions in integration tests", and exactly one survives — `DemoIntegrationTests.swift:375`, asserting `bar.frame == settledBarFrame` and `bar.frame.width != placeholderWidth`. That is not an author's claim about what a screen should look like; it is a **settle-correctness** assertion, checking that the harness returned the settled geometry rather than the placeholder it passes through. The DSL expresses intent about a screen and has no vocabulary for "not the intermediate value", so converting it would replace a precise claim with a weaker one and delete the only test that can catch a harness accepting an unsettled tree. **The general rule: a dogfood is a test of the vocabulary, not a quota of call sites — converting an assertion the vocabulary cannot express is a loss disguised as adoption.** Decided 2026-08-11 (Wave 5 exit gate; tracked as CTS-791F6ED3).

31. **`no.md` #25's rule has now cost a fourth row, so the mutation `new` gets an explicit shape rule: prefer DISCARDING a result over DELETING a statement.** #25 recorded that a mutation whose `new` does not compile scores as if the guard were tested, because a compiler's exit 1 is indistinguishable from a failing assertion's at the harness boundary — and named Wave 4 Tasks 2, 3 and 5 as three occurrences. This session added the fourth: deleting the `findings.append(contentsOf: machineState.expectations.evaluate(...))` statement left `tree`, `context` and `machineState` unused, which is an **error** under `-warnings-as-errors`, so the row built nothing and executed zero tests. Two things changed since #25 and both matter. **The harness now reports INCONCLUSIVE rather than NOTICED**, which is the honest answer and is why the defect was visible at all — a compile failure no longer masquerades as a proven guard. But INCONCLUSIVE is a *diagnosis*, not a fix: the guard is still unproven, and a summary line reading "84 NOTICED, 0 UNNOTICED" invites reading the suite as fully verified when 8 rows never ran. **The shape rule that prevents the recurrence: break the BEHAVIOUR while keeping every binding live.** `_ = f(x)` computes the value and throws it away; substituting a wrong-but-typed constant works the same way. Deleting a statement is what removes the last use of some binding, and under `-warnings-as-errors` that is fatal rather than noisy. Verified here in both directions — red at 1 test / 3 failures citing zero findings, green at 21 / 0, byte-identical restore. **And the check remains #25's: do not ask whether the row scored NOTICED, ask whether the WITNESS RAN — grep the runner's own `Executed N tests` line.** Decided 2026-08-11 (Wave 5 Task 5; the 7 other unproven rows tracked as CTS-D0942526).

32. **A library test suite cannot see a binary that refuses to START, and `swift test` will REBUILD the binary you just broke — so the obvious negative control is void.** Wave 6's CLI put every command in `VerdictUICLICore` precisely so tests could reach it (the `swift-testtarget-unreachable-module-gate` shape: nothing inside an executableTarget is reachable from any test in the same package). That worked — `VerdictEngineTests` drives the real command objects and was **8/8 green against a binary that could not execute a single command**. The root command conformed to `AsyncParsableCommand` but was reached through a `main.swift` calling `VerdictUITool.main()`, which silently selects the SYNCHRONOUS overload; it compiles, links, and then fails at RUN time with `Asynchronous root command needs availability annotation`. There is no compile-time signal and no in-process test that can observe it, because the defect is in how the PROCESS STARTS. The fix is `@main` on a type in a file **not** named `main.swift` — Swift gives that filename top-level-code semantics and then forbids `@main` in the same module, so the two mechanisms are mutually exclusive and picking the wrong one is invisible until you run the artifact. **The half worth keeping is the CONTROL, which failed the first time and looked like a weak test.** Replacing the built binary with `#!/bin/sh\nexit 1` and running `swift test --filter CLIBinarySmokeTests` reported **4 tests, 0 failures** — because `swift test` rebuilt the real Mach-O over the stub before running it (verified: the file was 5,010,192 bytes of `cffa edfe` afterwards, not the 20-byte script). The suite looked unfalsifiable and was not; the apparatus had reverted the experiment, `no.md` #14's rule in its newest costume. Building once (`swift build --build-tests`) and then invoking the bundle directly (`xcrun xctest -XCTest <suite> <bundle>.xctest`) keeps the stub in place, and the control then reads **4 tests, 8 failures**, restoring to 4/0 with a byte-identical sha256. **The general rule: when a negative control comes back green, ask what REGENERATED the thing you broke before concluding the test is weak — a build system that repairs your mutation is indistinguishable, at the summary line, from a test that never checked.** Decided 2026-08-11 (Wave 6 Task 1).

33. **An "INCONCLUSIVE" backlog is not one defect class, and the ticket's stated cause was wrong for a third of it.** CTS-D0942526 recorded "7 pre-existing mutation rows score INCONCLUSIVE (their mutation does not compile)". A full sweep on a clean exclusive tree measured **92 rows, 83 NOTICED, 0 UNNOTICED, 9 unverified** — nine, not seven, and split across **two causes the ticket collapsed into one**: 5 genuinely did not compile, and 4 reported `SETUP FAILED: the filter matched no tests before mutating`. The second group is the more dangerous, because a stale test filter means the row was ALREADY proving nothing before any mutation was applied — and two of those four went stale *in this very session*, when splitting `VerifiableMacroTests` into a diagnostics class moved the tests the filters named. **A file split silently invalidates every mutation row whose filter spells the old class**, which nothing but a sweep can see; `--verify-targets` passes, because it checks the SOURCE anchor and never the test filter. Of the 5 non-compiling rows, 3 were fixable by `no.md` #31's rule (keep every binding live: `duplicates.insert("@never-a-real-name")` rather than `_ = name`; mutate the NODE handed to `makeFinding` rather than rewriting the call and orphaning its `defaultSeverity:` argument; consume `settleStarted` before substituting the budget). **The remaining 2 are UNPROVABLE BY CONSTRUCTION, and each for a reason worth stating rather than retrying:** the ForEach-trivia row guards a defect whose whole nature is emitting source that does not compile, so any mutation reinstating it breaks the target build before its snapshot witness runs; and the scenario-suffix row has a pure-function witness that needs no expansion at all, but a SIBLING file hard-codes the generated type name, so the mutation breaks the target build first. Both are kept rather than deleted — `--verify-targets` still pins their anchors, so a refactor that moves the line is caught even though the mutation is not — and both now carry the measurement in a comment at the row. **The general rule: a backlog row's stated cause is a claim by whoever filed it, and "N rows fail for reason X" is two claims — the count and the reason. Measure both, because the rows that fail for the OTHER reason are the ones nobody is looking for.** Decided 2026-08-11 (CTS-D0942526).

34. **The session that documented "a suite cannot see the artifact" then shipped DOCS describing a daemon and an MCP server with no transport.** `no.md` #32 records the CLI half: 8/8 library tests green against a binary that could not start, closed by `stage_cli_smoke` and a suite that runs the artifact. A cold-read audit of the same session's diff found the identical shape one layer further out, in the half nobody had thought to check. `VerdictDaemon.handle` and `MCPServer.tools` are real, tested (10 + 4 tests) and correct — and **nothing in the package binds a socket, reads stdin, or adds a `daemon`/`mcp` subcommand** (verified: zero hits for `sockaddr_un|socket(|listen(|accept(` and for `readLine|standardInput` across `Sources/`; `subcommands:` is `[List, Render, Verify, Baseline, SweepRun]`). The method surface without its transport is a library, not a service. **What made it a defect rather than an unfinished feature was the DOCUMENTATION**: `docs/runbook.md` gave a literal `printf … | nc -U ~/.verdictui/daemon.sock` against a socket nothing creates, `CHANGELOG.md` listed the daemon under **Added** as "warm JSON-RPC over a unix socket", and `contracts/mcp-tools.md` described a request/response wire contract in the present tense. Each was written from the DESIGN rather than from running the thing — the exact failure `no.md` #23/#26/#32 already name, arriving through prose instead of through a test. **The tell was available and unread**: `DaemonTests` opens by explaining that `handle` is "deliberately separable from the socket plumbing", which is a precise statement that the plumbing is untested *and* — unnoticed at the time — that it does not exist. The fix is honesty rather than code: the runbook now states NOT RUNNABLE YET and gives no example (a command that cannot run is worse than a missing one), the changelog says METHOD SURFACE ONLY, and the contract is marked as the contract the transport must satisfy. **The general rule: a green test suite is not the only thing that can describe a feature that does not exist — a runbook, a changelog line and a contract file each make the same claim with no test behind them at all, and they are read by exactly the people who cannot check.** Decided 2026-08-11 (post-session cold-read audit).

35. **A round-trip test cannot see a wire-format defect, because both halves are the same `Codable` — and this one had shipped a wire the contract never described.** Building the transports (`no.md` #34's remaining work) meant finally sending real bytes to a real client, and the first `nc` reply read `{"pong":{"_0":"1.0"}}` while `contracts/mcp-tools.md` documented `{"scenarios": [...]}` and `{"verdict": {...}}`. Swift's SYNTHESIZED encoding for an enum with associated values wraps the payload in a positional key, so every `DaemonResult` had been travelling inside a `_0` the contract never mentioned. **Nothing caught it for a whole wave, and the reason is structural: `testAResponseEncodesAsOneNewlineTerminatedDocument` asserted the frame PARSES, and `testARequestRoundTripsThroughTheWire` encoded and decoded through the same `Codable` — so it agreed with itself no matter what bytes travelled between.** A client is written against the CONTRACT, not against our decoder. `_0` is also a compiler implementation detail rather than a promise: it would have changed shape the moment a case gained a second associated value, breaking every client at once, for a reason no test in this repo could have explained. Closed by hand-written `encode`/`init(from:)` and by two tests that read RAW JSON with `JSONSerialization` and assert the published key shape — one for `list`, one for `verify`, because the two are encoded by the same machinery and fixing one alone is possible. **The general rule: a serialization test that decodes with the same type that encoded it tests the pair, never the format. When a shape is a PUBLISHED CONTRACT, one test must read the bytes as a foreign client would — with a different parser, against the documented keys.** Decided 2026-08-12 (CTS-81D9483D).

36. **A blocking syscall on the main actor deadlocks the thing it is serving, and the test that found it looked exactly like a broken test.** `DaemonTransport.serve` was written `@MainActor`, because `VerdictDaemon.handle` is `@MainActor` (rendering SwiftUI requires it). But `accept` and `read` BLOCK, so the actor was held for the entire wait — and the main actor is precisely where the render each request needs must happen. With a client in the same process it deadlocks outright: **measured at over ten minutes with no output before the run was killed**, on a suite that normally finishes in seconds. Fixed by running the syscalls off-actor through a continuation and hopping onto the main actor only for `handle`. **The half worth keeping is how the diagnosis nearly went wrong — TWICE, in both directions.** First, the hang produced `Executed 12 tests, with 0 failures` in the log, because the suites that HAD finished reported normally; a summary line was present, it was true, and it described a run that never completed. Then, after the source fix, the end-to-end test still returned ZERO replies — and the temptation was to keep editing the transport. Driving the SHIPPED BINARY over `nc` instead answered it in one command: two frames, two correct replies, pipelined on one connection. The transport was already right; the TEST was calling its blocking client read inline on its own `@MainActor` method, starving the very server it was waiting for — the same defect one layer up, wearing the costume of a subject defect. That is `no.md` #14's rule arriving again: **when a verification tool contradicts a direct measurement, suspect the apparatus before the subject — and the cheapest way to break the tie is to run the ARTIFACT by hand, outside the harness entirely.** Decided 2026-08-12 (CTS-81D9483D).

37. **A free-form protocol field typed as ONE method's shape rejects the ENVELOPE, so the message never reaches the handler that would have answered it — and the one payload the suite happened to send was the one spelling that decodes.** Wave 7's dogfood was supposed to be a transcript-writing exercise; the first real bytes sent to the shipped binary came back `{"error":{"code":-32700,"message":"parse error: DecodingError.keyNotFound: Key 'name' not found … Path: params"}}`. `MCPRequest.params` was typed as the `tools/call`-specific `MCPCallParams`, whose `name` is non-optional. But `params` is **free-form per JSON-RPC method**, and `initialize` — the FIRST message of every real session — fills it with `protocolVersion`/`capabilities`/`clientInfo`. The synthesized decoder therefore failed the whole envelope, so `answer` never dispatched and the correct `initialize` handler sat unreachable four lines below. **No MCP client on earth could complete a handshake**, which is the entire point of the wave. **Why nothing saw it, at THREE layers, all for the same reason.** `MCPTransportTests.testInitializeReportsTheProtocolAndServerIdentity` sends `initialize` with **no `params` key at all** — the one spelling that decodes either way. `stage_transport_smoke`, the gate written specifically because "a library test cannot see a process that refuses to start" (`no.md` #32/#34), sent that same paramless payload — and asserted only the reply COUNT, which cannot see this at all, **because a parse error IS a reply**: the count stayed 3 while nothing could connect. And `contracts/mcp-tools.md` documented the tools without documenting the handshake, so there was no third opinion. Each layer was checking something real; none was checking the first message. **The general rule: a field the protocol defines as free-form must decode leniently, because typing it for one method makes every OTHER method undeliverable — and a test that builds its input in the server's own language cannot discover this, since it never has to spell the field the way a stranger does.** The fix is `try?` on that one field (`callTool` already rejects a missing `params` as a tool-level error, so a genuinely malformed `tools/call` still fails as that call rather than as the connection), pinned by `testTheHandshakeARealClientSendsIsAnswered` with a paramless `tools/list` control, by a mutation row, and by the gate now asserting the handshake SUCCEEDED rather than counting lines. **And the method, for the third wave running: the defect was found by sending real bytes to the shipped binary, in the first command of a session whose purpose was writing prose about it.** `no.md` #32 (a suite blind to a binary that cannot start), #34 (docs describing a transport that does not exist), #35 (a round trip blind to the wire format) and now this are one finding at four depths — **the artifact is a different subject from the code, and only running it asks about the artifact.** Decided 2026-08-12 (Wave 7 dogfood).

38. **SLO 3 gates the MEDIAN and records the tail — decided by measuring THIS metric, not by copying SLO 1 — and a shared type had to leave a test target to make it possible without a copy.** Wave 7's gate asks for MCP tools under 100 ms warm "measured through the actual stdio transport", which SLO 1 cannot answer: it times `Harness.perform` IN-PROCESS, and an agent never calls `perform` — it writes a frame to a pipe and waits. Process boundary, framing, JSON coding and pipe scheduling all sit between the two, so a tool can meet SLO 1 and be slow to every caller. **The lane choice was re-derived rather than inherited**, because inheriting a conclusion is how a threshold ends up right for the wrong metric. Measured on the release binary, 60 samples after 5 warm-ups, client-timed, three runs each: idle **p50 8.30/8.29/8.29, p95 8.52/8.67/8.44**; under 8 spinning cores **p50 10.05/10.33/11.30, p95 11.91/12.01/45.82**. The median degrades honestly and stays bounded; the tail swings 5x on unchanged code because one descheduled sample moves it. So p50 is asserted at 40 ms (~4x the idle median, clear of the loaded one) and p95 is recorded — the same shape as `no.md` #13/#15, reached independently. The live PM run then printed `p95 23.57ms` against a 10.25 ms idle tail, which is the decision vindicating itself within the hour. **Two structural notes worth more than the number.** (a) `ConstrainedTimingEnvironment` — the type extracted precisely because three timing suites had drifted (`no.md` #17) — lived in `VerdictUIProbeTests`, unreachable from the CLI test target, so SLO 3 could only have had a COPY. **A type whose whole purpose is having one definition was placed where a second one was inevitable**; it moved into the library both targets already depend on. The same applies to `_parse_slo_line`: both SLO stages now read their figure through one helper carrying both fail-closed conditions (a `--filter` matching nothing exits 0 having run no tests; a missing summary line is a failure, never a skip), because two copies are two places to weaken and weakening either alone leaves the other's tests green. (b) **Every assertion that proves the benchmark RAN is asserted in EVERY lane, including record-only hosts** — sample count, finiteness, and a `result` rather than an `error` on every reply. That last one matters most: an error reply is fast for the wrong reason, so without it a server that had stopped rendering entirely would post the best numbers the suite has ever seen. **The general rule: a latency claim belongs to a LAYER, and the layer a user experiences is rarely the one a unit benchmark measures — so measure the artifact, and re-derive which statistic can carry a gate on that metric instead of importing a sibling's answer.** Decided 2026-08-12 (Wave 7 exit gate).

39. **A backlog of 110 items was 98 tickets about Apple's source code, and the 12 real ones were unreadable underneath — the cause was an exclusion matching a directory NAME that the tool's own invoker chooses.** `audit_engine.audit_swift_source` skipped build directories by EXACT name (`{".build", "build", ".swiftpm", ".git", "DerivedData"}`). SwiftPM's build directory is **not a constant**: `--scratch-path` yields whatever the caller passes, and this project uses `.build-codex-verify/`, which is not equal to `.build`. Reproduced before fixing: `any(part in exclude for part in path.parts)` returns **False** for `.build-codex-verify/checkouts/swift-syntax/Tests/TypeTests.swift`. Those directories hold `checkouts/` — the full source of every dependency — so the miss does not admit a few stray files, it admits **all of swift-syntax**, and the auditor dutifully told this project to split Apple's test files. Every one of those 98 tickets was unactionable BY CONSTRUCTION (the tree is gitignored, vendored, and not ours to edit), which is lesson 209's shape at scale: a finding nobody can act on does not merely add noise, **it destroys the store's signal** — three P1s sat in that backlog and could not be seen. Fixed by prefix-matching the build roots, with a **control test** asserting ordinary project source is still scanned, because "skips build directories" is otherwise satisfied by a scanner that skips everything and reports a permanently clean project. **The second finding is about the three P1s themselves, and it is the more dangerous one.** They read `CEO/stage_test: 5 test failure(s) in 629 tests` and cited exact file:line locations — and all three were a background PM sampling the tree **inside a hand-applied mutation window**. This session's discipline is: mutate source, confirm the witness goes RED, restore byte-identically. A concurrent reader of that tree records the intended red as a regression, with a precise-looking citation and no way to tell it from a real one. That is `no.md` #14/#21's rule — *a mutation sweep needs an exclusive tree* — arriving through a **concurrent PM rather than a concurrent write**, which the per-row hash guard of #21 cannot see because nothing wrote anything. The two lessons compose: **a ticket's stated cause is a claim by whoever filed it (`no.md` #33), and an automated filer's claim is no better — so re-verify on a CLEAN tree at HEAD before acting, and check `git status --porcelain` first, because the tree the ticket describes may never have existed.** Decided 2026-08-12 (shared-libs commit e7003d4, PR medlars/pm-base#30).

41. **A compaction is a bet about a SHAPE, and my first two bets were wrong in opposite directions — the wire budget then turned out to be a guess nobody had ever measured.** Wave 7's `act` tool needed a token-frugal delta; the plan budgeted "typical act delta ≤ 300 bytes". Four measured rounds, and each one only became visible by PRINTING the bytes rather than reasoning about them (`no.md` #24 again). **(a) Reusing `CompactTree` for each added subtree made the delta BIGGER — 735 B against the 702 B raw form it replaced.** `CompactTree` amortizes nine array keys across a whole tree, where they cost nothing per node; an ADDED node is typically a subtree of ONE, so each addition paid all nine keys plus its own `strings` table to describe a single node — ~280 B of envelope around ~80 B of payload, and the per-addition table defeated the shared one. A format that is excellent for its own subject can be *worse than nothing* one level down, and the shape mismatch is invisible until you look at the output. **(b) Flattening added nodes into the delta's own arrays fixed that (598 B), and interning ids + flattening `TextMetrics` took it to 511 B** — ids had been paying twice (stored literally AND interned via the addition's path), and three long metric key names cost more than the three numbers they labelled. **(c) Then the opposite error surfaced: for an act that changes NOTHING the compact form was 170 B against a raw 49 B**, because twelve empty lists were still spelled out. That is the *commonest* act in a real agent loop — a tap that flips internal state, a keystroke that does not resize anything — so the format was winning on the rare case and losing nearly 4x on the frequent one. Omitting empty lists made it **2 bytes**. **The lesson that outlives the numbers: a wire format has a DISTRIBUTION, not a size, and optimizing the example in front of you can make the common case worse while every test still passes.** Measure the vocabulary — expand, collapse, inert, error — not one fixture. **And the budget itself was the last finding.** After all four rounds a structural act is 498/419 B and cannot reach 300 B by any means that keeps the payload honest: ~400 B of it is node ids, roles, text and structural paths, and dropping `structuralPath` makes unprobed added nodes uncitable in findings while dropping `TextMetrics` blinds `TruncationRule` — both are information the verdict layer *reads*. The 300 B figure was written before a single act existed to measure, and it is satisfiable only by acts that change nothing, i.e. it could never fail for the reason it exists. Replaced with two gates set FROM the measurement — 512 B structural, 64 B inert (owner decision 2026-08-12) — which is the SLO 1 / SLO 3 discipline (`no.md` #13/#15) arriving at a byte budget: **a threshold moved to fit today's number is a silencer, but a threshold that was never a measurement is a guess, and replacing a guess with a measurement is not weakening a gate.** Both regimes are pinned separately, because a single budget covering both would be met by the inert case alone. Decided 2026-08-12 (CTS-D47CCD1D).

40. **A stage that shells out to `swift build` WITHOUT the shared SwiftPM lock is a flake generator, and the tell is a Grade B whose named test passes in isolation.** `stage_cli_smoke` built `verdictui` with a bare `subprocess.run(["swift","build",...])` while every other Swift stage went through `swift_runner.swiftpm_command_lock`. That was survivable only by luck of ordering: the stage is reachable from **two places at once** — the pipeline runs it directly, and `stage_pytest` runs `TestStageCLISmoke::test_it_passes_against_the_real_binary`, which calls it again — so a single PM run can invoke `swift build` while its own Swift stage holds the package's one build directory. Measured 2026-08-12: **Grade B (95.6) on a CLEAN tree with 629/629 Swift green**, `stage_pytest` naming `test_it_passes_against_the_real_binary`, and that test passing on its own in 5.31 s. The same suite that takes 11 s standalone took **75.56 s** in the contended run, which is the actual signature — not a failure, a queue. **What makes this worth a rule rather than a fix is how it reads.** A gate that fails for the ENVIRONMENT teaches its reader to discount it (`no.md` #15), and this one goes further: it fails while naming a specific test, so the obvious next move is to investigate that test — which is fine, correct, and a dead end, because the test is not what broke. "It passed on retry" is then the tempting stopping point, and it is not a diagnosis; it is the symptom restated. The mechanism had to be found by asking what ELSE was running, not by reading the named test more carefully. **The general rule: any stage that shells out to a tool holding a global or per-package lock must take that lock, and the requirement is not visible from the stage's own code — it emerges from the set of callers, which is exactly what a stage author cannot see.** Pinned by `test_the_build_takes_the_shared_swiftpm_lock`, which reads the source rather than running it (two concurrent SwiftPM invocations cannot be staged deterministically in a unit test) and asserts the lock is acquired BEFORE the build, negative-controlled by removing it. Decided 2026-08-12.

42. **No AX witness that trusts `AXIsProcessTrusted()`, and no witness launched with fork/exec — and the "blocked on a permission grant" premise was never measured.** Wave 8 opened with the resume file recording a BLOCKER: the AX witness needs Accessibility permission, an owner action, so raise it before writing code. One probe falsified it — an unsigned binary built seconds earlier read **Finder's live window tree cross-process**, so trust is INHERITED from the launching process and was already in force. The wave could have started at any point in the preceding week. **A recorded blocker is a claim with a timestamp, and this one was never a measurement at all**; the cheapest possible probe retired it in under a minute. Four further measurements then changed the design before any of it was written (`docs/wave8-ax-findings.md`): a process **cannot read its own AX tree** (`-25208` while `NSApp` reports one visible window in the same instant), so the witness is out-of-process by necessity; **`NSApp.run()`** — not `RunLoop.run` — is what registers a process with the accessibility server, and a trusted, bundled, visible-windowed process still reads `-25204` without it; and **text lives in `AXValue` for text/fields but `AXDescription` for buttons/toggles/images**, so a reader consulting one loses half the vocabulary while still returning a populated-looking tree. **The one that matters most for Task 3: `AXIsProcessTrusted()` returned `true` in EVERY failing case.** A witness gating on the trust flag proceeds confidently onto an empty tree, and an empty tree is indistinguishable at the point of use from a scenario that genuinely renders nothing — so the permission path must be driven by the READ failing, never by the flag. **The general rule: a permission flag reports what was GRANTED, never what is REACHABLE, and the gap between them is where a check that cannot fail for its own reason lives.** Decided 2026-08-12 (Wave 8 Tasks 1–2).

43. **No `Process`-spawned witness host — the launch PATH decides whether AX can see it, and the error code names the wrong subject.** The integration test failed with `-25204` against a host verified working by hand minutes earlier. That code is `kAXErrorAPIDisabled`, which is also what a permission problem returns, so the failure pointed squarely at a permission grant that was already correct. Measured, one variable at a time: shell launch → **err=0, 1 window**; `Process` from `xctest` → `-25204`; `Process` from a plain Swift CLI parent → `-25204`; `open -a` on a minimal `.app` → **err=0, 1 window**. **Two theories were tested and FALSIFIED before the real one was found, and recording them is worth as much as the answer**: session/graphic access is identical in both parents (`SessionGetInfo` reports id `100022`, `sessionHasGraphicAccess: true` in each), and it is not a race — a shell-launched host is readable within 0.3 s and stays readable, a spawned one is never readable at any delay out to the full timeout. The cause is that a fork/exec child does not join the GUI session as a launched application; `open` routes through **LaunchServices**, which registers it. **Testing a plain CLI parent is what ruled out `xctest`** — the obvious suspect, and wrong; without that control the fix would have been aimed at the test harness, which is `no.md` #14's rule wearing a new costume. Two consequences: **`-n` is load-bearing** (without it LaunchServices reuses a still-terminating host, so the reader verifies the PREVIOUS scenario's window while reporting the current scenario's name — a wrong answer that looks entirely well-formed), and **the readiness handshake had to change**, since stdout is not connected through a detached launch; readiness now polls for the WINDOW rather than the process, which is the stronger signal anyway because the process exists well before the window server publishes it. **The general rule: when a failure code is shared by two causes, it will send you at the one you can already see — and the way out is a control that varies the OTHER suspect, not a closer reading of the one you suspect.** Decided 2026-08-12 (Wave 8 Task 1).

44. **No unbounded walk over a tree the PLATFORM owns — and the crash that proved it was invisible under `--filter`, which is how it nearly shipped green.** `AXReader.normalize` recursed through `kAXChildrenAttribute` with no depth bound. Under `swift test --filter VerdictUIWitnessTests` the suite reported **8/8 passing**, twice, including a hand-run negative control that failed correctly when the fix was reverted — every signal said the code was right. The full suite then **segfaulted at test 15 of 669**: `EXC_BAD_ACCESS`, *"thread stack size exceeded due to excessive recursion"*, with a backtrace of `normalize` → `map` → `normalize` repeating to the stack limit inside `AXUIElementCopyAttributeValue`. **The filtered runs passed because the process died AFTER printing its per-test results**, so `xctest` had already emitted `Executed 8 tests, with 0 failures` before it fell over — a true summary line describing a run that crashed. `stage_test` reported it as a test FAILURE naming an unrelated macro test (the last suite to complete), which sends the reader at the wrong subject entirely; the crash report was the only artifact that named the real one. **The defect itself is a category error worth stating plainly: an `AXUIElement` tree is a GRAPH the window server hands you, not a structure you own.** An element may reference an ancestor, and the children attribute then yields a cycle. My own throwaway spike had `depth < 8` and I dropped that guard when writing the "real" reader, because the real one felt like it should be complete rather than defensive — exactly backwards for data crossing a trust boundary. Two lessons compose here. **(a) A filtered green is evidence about the filter, not about the suite** — `no.md` #33 already records a filter that matched nothing; this is the same blindness one layer along, where the filter matched everything it named and still could not see a process dying. Run the FULL suite before believing a subsystem is done. **(b) A summary line is not proof the run finished.** CLAUDE.md rule 6 says to treat a missing summary as failure; this adds the converse — a PRESENT summary followed by a non-zero exit means the runner died after reporting, and the exit code is the authority. Fixed with `maximumDepth = 64`, which stops descending rather than truncating silently, so a tree that hits the bound is visibly shallow and the reconciler reports the missing children as visibility gaps instead of agreeing about nothing. Decided 2026-08-12 (Wave 8 Task 1, found by the final full-suite verification CLAUDE.md rule 6 demands).

45. **No further size-driven splits of the four files that remain over 800 lines, and `OracleHost.swift` was split because it had a SEAM — not because it was long.** Seven files were over the fleet's 800-line advisory when this was reviewed (CTS-95334966 / E34ADE4D / 890AE0F4 / 12A6A4C0 / 0F47964E / 2F1DB28F / DB1B466D). `no.md` #19 and #22 already ruled on five of them with measured evidence — a split of `verdictui-pm.py` moved `_pm_log` and **re-labelled every log record** from `verdictui_pm` to `verdictui_swift_runner` (25 tests went red and were right to); a split of `ProbeLayoutTests.swift` requires widening 12 `private` declarations to `internal`, at which point its `HeadlessHost` **collides with the identically-named one in `VerdictUIProbeTests.swift`**, which coexists only because `private` confines each to its file. **`OracleHost.swift` was the exception and was split**, 821 → 579 + 259, because `LayoutSettle` is a genuinely separate subject: a reusable settle primitive Wave 3 builds on, already marked with its own `// MARK:` and its own doc comment, extracted along a boundary that was there before the line count was. Full suite 695/695 green after, which is the evidence the split changed nothing (`no.md` #22: a refactor's own claim of equivalence is not evidence). **`VerifiableMacroTests.swift` (920) is the clearest case for the number standing**: 15 tests of ONE subject, each an expansion snapshot averaging ~60 lines because a macro test must quote the source it expands. Any cut is by test index, which is arbitrary by construction, and the fixtures follow whichever half moves. **The rule, stated once for all seven: a size limit is a proxy for readability, and a split is worth making when it follows a SEAM the code already has — a distinct subject, its own vocabulary, its own reason to change. Where the only available cut is arbitrary, or where the mechanical fix re-labels a logger or collides two fixtures, the number stands and the reason is recorded.** Revisit any of the four if a genuine second subject appears; do not revisit on the line count alone. Decided 2026-08-12.

46. **No pixel capture through `bitmapImageRepForCachingDisplay(in:)`, and no assumption that "scale pinned 1.0" is something a backend does on its own.** Wave 9 Task 1's plan line reads "windowless `NSHostingView.cacheDisplay` (matches Wave 2 host, scale pinned 1.0)", which reads as one call with a property. It is two, and the obvious call has the property backwards. Measured 2026-08-12 before any code was written (`no.md` #24's rule): the AppKit convenience returns a rep at the **device backing scale**, giving **240x160 for a 120x80 view** on this Retina machine, while `ImageRenderer` at `scale = 1` gives 120x80. So the default backend does not pin the scale — it follows the display. A baseline written here would then mismatch every capture from a 1x machine (a CI runner, a non-Retina display, a different desk), and the pixel channel would report that hardware difference **as a UI regression** — the most expensive shape a verification tool has, since it accuses correct code. The fix is to construct the `NSBitmapImageRep` explicitly at one pixel per point rather than asking the view for one, verified across three separate process launches producing an identical FNV hash — so determinism is real rather than a warm font-cache artifact one process cannot distinguish from the real thing. **The second finding is that the two backends DO NOT AGREE even at matched dimensions** (2463 B vs 1228 B), which the plan asked to "document honestly". A doc comment is a claim nobody checks, so the divergence is asserted in a test instead: if the backends ever converge, that test fails and the `backend` field and the cross-backend refusal both become wrong together, loudly. **The general rule: a plan line that names a mechanism and a property in one breath is two claims, and the property is the one nobody verifies — measure what the named API actually does before building on the sentence.** Decided 2026-08-12 (Wave 9 Task 1).

47. **No nondeterminism fixture that has not been shown to VARY, and no bare-`NSHostingView` spike trusted as a rendering instrument.** Wave 9 Task 2 refuses a pixel baseline to any scenario that does not render identically twice, so it needs a fixture that genuinely drifts. Three attempts, and the first two could not fail — for two DIFFERENT reasons, which is why this is a rule rather than a slip. **(a) `Text(Date().description)` held in a `View` struct does not drift at all.** A SwiftUI `View` is a value, so `Date()` is evaluated ONCE at construction; rendering that host twice re-encodes one captured string. Measured identical at 1.1 s apart, which reads exactly like a working determinism check on a stable screen. **(b) Constructing the view fresh per render in a bare `NSHostingView` spike ALSO reported identical — and that one was the instrument lying.** The control that exposed it took one line: rendering the knowingly-different strings `"AAA"` and `"BBB"` **also produced identical bytes**. The spike was not drawing text at all, so every `identical=true` above it was reporting *nothing* rather than *stability*. The real `OracleHost` does draw text (724 vs 892 bytes for that same pair), so the shipped capture path was sound the whole time and only the throwaway harness was broken — `no.md` #14's rule arriving in the cheapest possible costume, since a hand-rolled spike feels too small to distrust. The shipped fixture varies on a **counter incremented per `body` evaluation**: it changes by construction, does not depend on wall-clock resolution, and cannot silently agree because two renders landed in the same second. It is pinned by its own assertion (`bodyEvaluationCounter >= 2`) precisely because a fixture that quietly stopped drifting would make the refusal test go green for the opposite reason — the check accepting everything looks identical, at the summary line, to the fixture no longer needing refusal. **The general rule: a fixture built to trigger a check is itself an untested claim, and it must be shown to have the property before it is used as evidence — and when a rendering spike reports "no difference", render two things you KNOW differ before believing it.** Decided 2026-08-13 (Wave 9 Task 2).

48. **No pixel-diff threshold on the differing-pixel COUNT, and no anti-aliasing tolerance justified by anti-aliasing.** Wave 9 Task 3's plan line asks for "per-channel tolerance + anti-aliasing awareness", which reads as one requirement and is two claims, only one of which survives measurement on this capture path. Measured 2026-08-13 before any assertion was written (`no.md` #24's rule): a border going **black → red** touches **196 of 8000 pixels (2.45%) at max channel delta 255**, and a border going **black → near-black** touches **the same 196 pixels at max delta 5**. So the pixel COUNT is identical for a regression anyone would see and one nobody could, and any rule of the form "fail when more than N% of pixels differ" — the shape general-purpose image-diff tools ship, typically defaulted around 5% — fires identically on both **and would swallow the 1-px border regression whole**, which is the exact case this channel exists as the existence proof for. Channel MAGNITUDE is the only discriminator, so `perChannel` carries the gate and `maxDifferingFraction` defaults to **0**; the swallow case is pinned as a negative control, because "a 1-px change fails" is otherwise satisfied by a comparison that fails on everything. **The second half is the one that would have shipped a lie in a doc comment.** Sub-pixel AA jitter does not occur here at all: offsets of **0.37 pt and 0.5 pt both produced byte-identical output**, and only a full 1 pt move changed anything — at which point the change is large and hard (649 px, 497 of them saturated) rather than a faint halo, because this capture path snaps layout to the pixel grid at 1x. The non-zero `perChannel` default therefore stays, but is documented as what it *is* — a **rounding allowance** for 8-bit colour conversion — rather than as anti-aliasing insurance the measurements do not support. **The general rule: a plan line naming a mechanism and a property in one breath is two claims, and the property is the one nobody verifies — measure what the named phenomenon actually does on YOUR path before writing a rationale for it, or the comment becomes a confident explanation of something that never happens.** Decided 2026-08-13 (Wave 9 Task 3).

49. **No 10× warm-cache gate, and no weaker cache key to earn one.** Wave 9's exit gate asks that a "warm pixel verify be ≥ 10× faster than cold". Measured 2026-08-13 over five cold renders and twenty warm fetches: cold p50 **0.96–1.25 ms**, warm p50 **0.17–0.18 ms**, speedup **5.4–7.0×**, stable across runs. The figure was written before anything existed to measure (`no.md` #41's shape) and assumed pixel capture dominates a verify. It does not — `OracleHost` renders windowless at 1x into a small bitmap, so the capture is already cheap. **What is expensive is the settle, and the cache cannot skip it because THE TREE IS THE CACHE KEY.** That is not an implementation detail to route around: the tree is computed from the same render the pixels come from, so it is what makes the cache able to see a state change it could never enumerate directly, and it is therefore the entire reason a stale hit is impossible. "Skip the settle" and "cannot serve a screen that has since broken" are one decision seen from two sides, and a 10× gate is reachable only by keying on something weaker — trading the channel's whole safety guarantee for a benchmark number, in the one cache where a stale hit reports PASS for a regression using evidence from before it existed. Gate set from the measurement at **3×**, asserted on developer hardware and recorded on a constrained one (`no.md` #15/#17/#38). **The benchmark's own first version then demonstrated why the assertions that prove a benchmark RAN are not optional:** its cold loop rendered five DIFFERENT labels, so it populated five entries the warm phase never asked for and measured two MISSES while reporting a hit-vs-miss ratio of 3.4×. Only `XCTAssertTrue(warm.wasHit)` caught it; with the fixture corrected the identical code reads 7.0× (`no.md` #18 — a test whose fixture was not reaching the code under test, and every number it produced was about something else). **The general rule: when a measured result contradicts a budget, ask what the budget ASSUMED before moving either — sometimes the assumption is false and the number should change, and sometimes the only way to hit the number is to destroy the property the thing exists for.** Decided 2026-08-13 (Wave 9 Task 5).

50. **No offscreen origin for the witness window — `NSWindow` clamps it back onto the screen — and no "readable" check that never asked whether the window is VISIBLE.** The owner reported a small titled window flashing at the bottom-left of the screen, 8–10 times per run, each showing a different scenario (CTS-75914181). The previous session diagnosed it to `WitnessHost.swift:37` (`origin: .zero`) and recorded the fix as "an offscreen origin", deliberately uncommitted because it could not be verified — the probe and its control failed identically in a degraded window-server state. **Both halves of that record were wrong, and the recorded fix would not have worked.** Measured 2026-08-14: `NSWindow(contentRect:)` constrains its frame to the visible screen, so `origin: (-20000, -20000)` comes back as **`(160, 800)`** — fully on screen; `setFrameOrigin` *after* `orderFront` does move it, but AppKit clamps to keep a sliver reachable, stopping at `x = -220` of a 260 pt window and `y = -79`, so a 40×41 pt fragment still flashes. A bare `.zero` is itself already constrained, reporting `y = 41` rather than 0. The fix is `alphaValue = 0`: the window server still assigns a windowNumber, still lists it on-screen, and still publishes its AX tree (`no.md` #42/#43 require exactly that), while compositing nothing a person can see — verified externally at `alpha=[0.0]` where the unfixed host read `alpha=[1.0]`, bounds unchanged at `X=0 Y=784 360×292`, with the full witness suite 23/23 green and 0 skips, which is what proves readability survived. **Three findings outlive the one-line fix.** (a) **Readability and visibility are independent properties of one window, and nothing in Wave 8 asserted the second** — 22 witness tests all passed while the window flashed on the owner's screen every run, because every one of them asked "can the AX server read this?" and none asked "can a person see it?". A suite can be complete about the property it models and blind to a defect the user reports daily. (b) **The instrument had to be verified in both directions first** (`no.md` #47), and the obvious placement was void: `CGWindowListCopyWindowInfo` read from INSIDE the host returns nothing whether the window is visible or hidden, because a shell-launched binary is not LaunchServices-registered — the same mechanism #43 records for AX. Only reading from OUTSIDE, about the host's pid, discriminates. (c) **The recorded blocker's own falsify command retired it in one run** (DIR-036): `swift test` returned 0 skips, meaning the instrument this defect was declared unverifiable with had been working all along; the degraded state was plausibly caused by **eight orphaned PanoMac test hosts** still running from the previous day, reparented to pid 1 — an unrelated project's leak, ticketed rather than touched per DIR-035. **The general rule: when a prior session records a diagnosis it could not verify, the diagnosis is a claim and the platform is the authority — measure what the named API actually does before implementing the sentence, because a clamped coordinate looks exactly like a fix that shipped.** Decided 2026-08-14 (CTS-75914181).

51. **No treating DIR-035 as a bar on machine hygiene — it scopes CODE edits, and conflating the two left a live contaminant running.** While closing `no.md` #50 this session found **8 orphaned `swiftpm-testing-helper` processes for PanoMacPackageTests**, alive 1d04h–1d16h, all reparented to pid 1. My first framing was "PanoMac is out of scope, file a ticket, do not touch" — and a gate challenged it, correctly. Re-reading DIR-035: it forbids **drive-by fixes to another project's code**, on the reasoning that the session has not read that project's CLAUDE.md and cannot judge blast radius. **Killing an abandoned process is neither an edit nor a change to PanoMac** — it is hygiene on a shared machine, and the blast radius is fully knowable from `ps`. The distinction matters because the assumption was testable and I had not tested it: `lsof` showed **each orphan holding a windowserver/CoreGraphics connection**, and System Events listed a foreground GUI process among them, while `pgrep` found **no live parent runner**, so they were abandoned *and* actively holding the shared resource the witness depends on. That is the same degraded window-server state whose symptoms got recorded last session as an unverifiable blocker, costing a session (`no.md` #50 (c)). Killed by specific pid (never `killall` — lesson 121), 0 survivors, full suite then 775 tests / 0 failures / 0 skips. **The code defect is still PanoMac's and is still a ticket** (CTS-8A34C940): whatever spawns those hosts has no bounded lifetime or terminating `tearDown`, so they will accumulate again — VerdictUI's own witness solves that class with a self-terminating lifetime timer. **The general rule: a scoping directive names a CLASS OF ACTION, not a project boundary, so before invoking one to decline, check that the action you are declining is actually in the class — and when a scope call rests on an untested assumption about shared state, test it, because "out of scope" is the cheapest possible way to leave a real problem running.** Decided 2026-08-14.

52. **A fixture population can be invisible BY CONSTRUCTION — and the language itself can be what hides it.** `@Verifiable` did not compile on any `public` view for four waves. `VerifiableView` is a PUBLIC protocol requiring `verdictProbedContent`, the macro emitted that member with no access modifier, and Swift refuses to satisfy a public requirement with an internal member — so every view a library module EXPORTS failed to build, which is the macro's entire primary audience. Isolated to one token: the identical view compiles when internal and fails when `public`. **777 tests never saw it because every macro fixture in the test target is internal, and most are nested inside an `XCTestCase`, where `public` is not expressible at all.** That is the sharp version of lesson 342 (a suite built from one input shape is green by construction): here the missing shape was not merely unwritten, it was *unwriteable in the place all the fixtures live*, so no amount of diligence within that file could have produced it. The fix's test is therefore at FILE SCOPE, with the internal spelling beside it as the control, and a comment saying why demoting it to internal makes the test pass against the broken macro. **The general rule: when every fixture for a subject lives in one structural position, ask what that position cannot express — and note that "we have N tests" is evidence about the shapes those tests can take, not about the subject.** The corollary is why this was found at all: it took a consumer package OUTSIDE the engine, whose first build hit it immediately. A dogfood is not a demo; it is the only test that runs in the caller's structural position. **And one mutation-harness consequence:** this row is the second `runtime_witness_reason` opt-out, admitted on a MEASUREMENT — the mutation was applied and its witness run *without* touching any consuming source and still failed, so `no.md` #23's stale-expansion trap cannot apply, because the defect IS a compile failure in the consuming target and a stale expansion is the broken one. An `assertMacroExpansion` snapshot could not witness it at all: the generated text differs by one keyword and is syntactically valid either way. Decided 2026-08-14 (Wave 10 Task 1).

53. **A rule can be arithmetically correct, cite real measurements, and still be wrong — because the THRESHOLD is a claim about the platform that nobody re-measured.** The Wave 10 dogfood produced three `tap-target` findings at ERROR severity against a SagaMail-shaped settings screen. Every number was right: the toggles measure 142×18, 60×18 and 100×18 pt against a 28×28 minimum. But an independent probe outside VerdictUI entirely — `NSHostingView(rootView: Toggle("Sound", isOn: .constant(true))).fittingSize` → **(60.0, 18.0)** — shows that **18 pt is the height of a native SwiftUI macOS `Toggle`**. So the rule fires on idiomatic SwiftUI written exactly as Apple ships it, its suggested fix (`.frame(minHeight: 28)`) would make the adopter's app *non-standard*, and its own doc comment ("a firing rule means the control is genuinely below the platform's documented floor") is FALSE for the commonest interactive control on the platform. This is `no.md` #25's severity principle arriving from the other direction: that entry warned against an error on ordinary correct code because it is how a tool gets deleted rather than fixed — here the tool would greet every new adopter with three unactionable red findings on their first run. **Not fixed in-session, deliberately**: recalibrating needs a survey of macOS control metrics the way `excessive-wrap` and `ContentOverlapRule` set theirs, and picking a number that makes today's screen green is the silencer this project forbids (CTS-DB551166). The dogfood test records the finding as expected rather than asserting a `.pass` it would have to fake. **The general rule: a threshold is a claim about a population, and a rule that has only ever been run against fixtures built to trip it has never tested that claim — so the first honest adoption is also the first real test of every threshold you shipped.** Decided 2026-08-14 (Wave 10 Task 1).

54. **`git add -A` stages whatever a TOOL wrote, and the tool that writes into your repo root is the one you never think about.** Wave 10 flipped this repo public, and within the same session a `git add -A` swept 9 `.playwright-mcp/*.yml` accessibility snapshots into two pushed commits. They were captures of a Cloudflare dashboard taken while registering a domain, and they carried the owner's **home address, phone number, email, and a PayPal account identifier** into a world-readable repository. Nothing in `.gitignore` covered them, because nothing in the repo had ever produced them — the Playwright MCP writes into the working directory by default, and I had been reading its output as tool results rather than as *files landing in a git worktree*. **Three things this cost, each worth stating separately.** (a) **Remediation is not one action.** Setting the repo private stops new readers; the history rewrite (`git-filter-repo --path … --invert-paths`) removes the blobs from reachable history; and NEITHER touches the objects themselves. (b) **A tag is a live ref.** After the force-push, `v1.0.0` still pointed at the old commit, which kept the whole PII tree reachable and served — verified, not assumed: `gh api …/contents/.playwright-mcp?ref=<old-sha>` returned a file listing. Deleting the release and the tag was mandatory, and it broke the Homebrew formula, which had to be withdrawn — the release and the leak could not be separated. (c) **Even then it is not gone.** With live refs down to `main` and forks at 0, the old commit is STILL fetchable by direct SHA, because GitHub retains unreferenced objects until its own GC runs. Only Support can force that (CTS-77D76C19), and the repo stays private until the probe returns 404. **And the quieter half: my own `wave-status.md` named the PayPal account and card digits in prose**, while describing the very purchase that produced the leak — a committed file, written by the same hand that was cleaning up. Sensitive detail belongs in the ticket store, not in a resume note that gets pushed. **The general rule: before making a repository public, enumerate what WRITES into its worktree — not what you authored. And after any history purge, verify by fetching the old SHA, because a force-push moves a ref and proves nothing about the objects.** Decided 2026-08-14 (Wave 10 release).

55. **A threshold is a claim about a POPULATION, and a rule whose fixtures were all built to trip it has never tested that claim.** `tap-target` shipped a 28 × 28 pt macOS floor for nine waves, with seven fixtures and a demo scenario all sized to fail it, and every one of them passed. The first honest adoption — the Wave 10 dogfood, against a screen written by somebody else — produced three ERRORS on toggles that are the size SwiftUI makes them. Measuring the platform settled it in one command: `NSHostingView(rootView:).fittingSize` gives `Toggle` 60 × 18, `Button` 54 × 24, `Stepper` 65 × 26, `Slider` 200 × 16, `TextField`/`Picker` 200 × 24 — **not one native macOS control reaches 28 pt**, so the rule could never have distinguished "too small to hit" from "a macOS control". It was a TOUCH metric wearing a pointer label, and its own doc comment asserted the opposite ("genuinely below the platform's documented floor"). **The floor is now 12 pt, and that number is also a measurement rather than a taste**: `.controlSize(.mini)` yields a 19 × 12 switch, the smallest control the platform will produce on request, so anything below it was shrunk by a layout accident rather than by an API — which is exactly the defect class the rule exists for. **Two things generalise.** (a) **Fixtures built to trip a rule cannot falsify its threshold**, because they are drawn from the rule's own model of the world rather than from the population it will meet; the first real adoption is therefore the first real test of every threshold you shipped, and it will arrive as a bug report about your tool rather than about the code. (b) **Moving a threshold moves every fixture calibrated against it**, and finding them is what the suite is for: 20 failures at first run across SIX sites, and the instructive one was the misreported-role LIE scenario, where an 18 pt control had quietly stopped being below the floor — so the lie was no longer load-bearing and the scenario would have reported PASS whether the role was truthful or not. A test that cannot fail for its own reason is the shape that whole fixture exists to catch, and it had become an instance of it. Decided 2026-08-14 (CTS-DB551166).

56. **A fixture can ALIAS against the sampler that watches it, and every symptom then points at the engine.** `testOscillatingLayoutTimesOutWithDeltaEvidence` intermittently reported that a deliberately-oscillating layout had *settled*. Two earlier sessions tuned the fixture's timer interval (4 ms, then `pumpInterval/4`) and a third — this one — filed it as unfixable after three failed approaches, having concluded from a constant progress token that `VerdictTreeSink.accept()` must stop firing mid-change. **Instrumenting `accept()` directly falsified that in one run**: 67 deliveries, `updateCount` climbing 59 → 66, and the probed width reading `10, 40, 40, 10, 10, 40, 40, 10`. Nothing was broken. The tree repeated every **two** deliveries while `LayoutSettle.requiredAgreeingChecks` is **2**, so two identical samples could land consecutively and — spanning the 30 ms quiet floor — settle *correctly* declared the screen quiet. A wheel filmed at its own rotation rate looks stationary, and neither the wheel nor the camera is defective. **That is why the interval tunings could never work: they change the RATE, and the defect was the PERIOD.** Fixed by making the width non-repeating (`10 + tick % 97`, prime and far above any plausible check count), verified 20/20 consecutive runs, and negative-controlled by freezing the width so the test fails as it must. **Two rules.** (a) **A periodic fixture must not share a period with the sampler that observes it** — pick one that cannot alias (a prime well above the check count, or a monotonic value), not merely one that is *faster*. (b) **An inference drawn from a signal you then FIXED is void, and must be re-measured against the fixed signal** — my "accept() stops firing" came from a token that could not see content, and I repaired that token in the same session without ever re-testing the conclusion built on it. Measuring the subject directly (67 accept calls, printed) cost one run and overturned three sessions of theory. Decided 2026-08-14 (CTS-6C1078DD). **AMENDED the same day: this fixed A mechanism, not THE one.** CI failed again on f0251975 (run 31809760395) at 0.037 s with the identical message, so the aliasing was real and the fix is worth keeping — 20/20 local passes, negative control working — but something else bites on a shared runner. The remaining suspect is the fixture's main-RunLoop `Timer` being starved by the very `settle()` it must outpace, which cannot reproduce on an idle developer machine; tracked as CTS-9A2A7301 with four measured dead ends listed. **And that amendment is the sharper half of rule (b): I closed the ticket on 20 green local runs, which measured the machine I was on rather than the machine that was failing.** A fix verified only where the bug does not reproduce is not verified — reproduce the FAILING condition first, or say plainly that you could not.

57. **`no.md` #56 is CLOSED, and what closed it was reproducing the failure LOCALLY — which happened by accident, while a WRONG fix was being tested.** #56 ended by conceding the aliasing fix addressed a mechanism rather than THE mechanism, and named the remaining suspect (a main-RunLoop `Timer` starved by the `settle()` it must outpace) as unreproducible on developer hardware. Five drives were tried across five sessions; the two new ones are worth recording because each failed in a way that TAUGHT something. **(a) Ticking from a custom `Layout`'s `sizeThatFits`.** It genuinely rides the sampler — `pump` sets `needsLayout` and calls `layoutSubtreeIfNeeded()` every iteration, so it needs no delivery guarantee — but mutating the `@Published` property DURING layout re-invalidates the view, which requests another layout, which mutates again. `swift test` HANGS with no summary line, measured twice at a 200 s timeout, each after a clean `Build complete!`. **(b) A plain non-`@Published` counter incremented in `body`.** This looked strictly better: no reentrancy, and `body` surely runs on every layout pass. It does not. SwiftUI caches the view VALUE and re-runs `body` only when OBSERVABLE state changes — measured `bodyEvaluationCount` advancing **1 → 1 across an entire settle**. **And that failure is the one that cracked the whole thing open: the test then failed at `settled(after: 0.034 s)`, which is the CI signature, on a developer machine, on demand.** Four sessions had been unable to produce that. The fix is a `CFRunLoopObserver`: a `Timer` is a request to be woken at a wall-clock INSTANT, so its delivery competes with whatever the loop is doing, while an observer is a request to be called on every ITERATION the loop performs — and `pump`'s `run(until:)` is what performs them. The drive stops racing the sampler and starts riding it, and no rate remains to starve. Verified at the ticket's own bar: **two consecutive green CI runs on one commit** (run 31871233285 attempts 1 and 2), with the test confirmed RUN at 0.272 s rather than skipped. **Two rules.** (i) **A wrong fix is worth building when it fails DIFFERENTLY** — (b) was not merely a dead end, it was the first instrument ever pointed at the actual failing condition, and it worked because it removed the thing that was accidentally hiding the bug. (ii) **`@Published` here is load-bearing, not incidental**, and the same is true of anything a framework uses to decide whether to re-evaluate: removing it does not merely slow the fixture down, it stops the fixture existing. Also added, and it caught (b) immediately: an assertion that the fixture actually MOVED more than settle's agreement window — without it "the layout timed out" says nothing about quiescence, and a silently-frozen fixture passes for the opposite reason (`no.md` #47). Decided 2026-08-15 (CTS-9A2A7301).

58. **A predicate read by several consumers is several claims, and widening it for one is how a guard elsewhere dies green.** `ConstrainedTimingEnvironment.isActive` means "is my wall clock comparable to a developer's?", and six timing suites read it correctly to choose ASSERT vs RECORD. A seventh consumer read it to gate `no.md` #18's OVERSHOOT discriminator — which is an ORDERING relation between two durations from the SAME clock, so contention inflates both sides and a slow host remains a valid witness, arguably a better one. An unattended repair agent then widened `isActive` to also fire on an unwritable SwiftPM cache. That is **correct for the six** (a sandbox without `CODEX_CI` genuinely cannot hold a wall-clock budget, and a direct `swift test` there has no PM wrapper to inject the override) and would have **silently retired the seventh** on every read-only-cache host. **Nothing in the suite could have noticed, and the measurement is the point: forcing `VERDICTUI_RECORD_TIMING_ONLY=1` runs 784 tests to 0 failures with 3 skipped.** A gate that has stopped gating is indistinguishable, at the summary line, from a gate that passed. Fixed by splitting the question rather than the value: `isActive` keeps the detector and answers clock-comparability; `canEvaluateElapsedInvariants` answers "can I order two durations I measured?" and is suppressed ONLY by the explicit human override, never by an inference about the machine. **The same shape was found one layer out in the same session**: `stage_test`'s summary regex discarded the skip count in a non-capturing group, so five AX witness tests skipping on a degraded window server printed `786 tests PASS` under a Grade A — the cross-validation channel, including every planted-lie test, went dark with no signal anywhere. Reported rather than gated (skipping instead of accusing is CORRECT for an environment the suite cannot see, `no.md` #15), so the fix was to make the silence audible: `786 tests PASS (5 SKIPPED — unverified)`. **The general rule: when one predicate gates several checks, ask whether they are asking the same question — and any predicate that can switch a check OFF needs a test asserting the ON state, because a suite that only ever exercises the permissive branch cannot tell a working rule from one that is always true.** Decided 2026-08-15 (CTS-391BEA74).

59. **An unattended agent that can edit source is a writer nobody reviews, and its damage is indistinguishable from a session's own.** The fleet's `ceo.py --watch` daemon dispatches a Codex repair agent per non-green project — `--sandbox workspace-write -c approval_policy="never"` — and 17 were live at once on 2026-08-15. Its VerdictUI agent produced `no.md` #58's defect. But the wider cost was epistemic, and it consumed most of a session: a background PM sampling the tree mid-build produced **three false P1 tickets** citing exact file:line locations; a `.build/.lock` contention was recorded in `TODO.md` as a blocker claiming *"SwiftPM hangs before compilation"* (the tree builds in 12.1 s, and the sandbox could not run `ps` to see the real lock holder); and the witness hosts flashing on the owner's screen — the symptom that started the investigation — were its test runs, not a regression in the alpha fix. Every one of those reads exactly like a real finding. **Three rules.** (i) **Before believing any ticket a PM filed, check `git status --porcelain` and re-run on a CLEAN tree at HEAD** — the tree the ticket describes may never have existed (`no.md` #39, now with a second population of instances). (ii) **A verification run needs an EXCLUSIVE tree, and an isolated worktree at HEAD is the cheap way to get one** — it is what separated this session's commit (784/0, exit 0) from the other agent's uncommitted edits, turning "my change broke 2 tests" into a measurement instead of an argument. (iii) **The owner's fix is the right shape: a watcher should REPORT, not repair.** The 30-minute sweep and the dashboard are useful; dispatching an unreviewed writer into a repo whose CLAUDE.md it has not read is not. Replacement tracked as CTS-F42D74A7 (file CTS/CIS rows, notify Moonitor and the CEO Dashboard, Claude rather than Codex, kill switch defaulting OFF). Decided 2026-08-15.

60. **A window can be invisible and still flash, because the WINDOW and the APPLICATION are two different things on screen — and a comment asserting otherwise kept it flashing for two sessions after the fix landed.** `no.md` #50 closed the witness-window flash with `alphaValue = 0`, verified externally: the window server reported `alpha=0.0` and the AX tree still published. That fix was real and is still correct. The owner then reported the flashing **twice more**, and both times the window was measurably transparent — `CGWindowList` reading `alpha=0.0`, bounds `X=0 Y=925 260x152`. What he was seeing was the **application arriving**: `setActivationPolicy(.regular)` makes the host a foreground app, so macOS gives it a Dock tile and a launch animation, and `open -n` starts one **per scenario** — about 23 per suite run. **The line was guarded by a comment that made the true cause unreadable**: *".regular rather than .accessory: an accessory app is not a first-class AX citizen, and the witness has to be readable."* Measured with two bundles differing only in that policy, each read from an EXTERNAL process: `.accessory + LSUIElement` → `AXerr=0 windows=1`, foreground procs **0**; `.regular`, no `LSUIElement` → `AXerr=0 windows=1`, foreground procs **1**. Identical AX visibility. The claim was simply false, and because it was phrased as a settled trade-off it was inherited rather than tested — by me, twice. **Both halves of the fix are load-bearing and neither is redundant**: `LSUIElement` is read by LaunchServices BEFORE the process starts, the only point early enough to suppress the launch animation, while the runtime call keeps the policy once `NSApplication` is up and **overrides** the plist — measured, `LSUIElement` with `.regular` still reported one foreground process. **Three rules.** (i) **"Invisible" is not one property.** A subject can be composited-transparent and still announce itself through the Dock, the menu bar, or an activation animation; a test asserting only the first cannot fail for the second, which is why the new guard is PAIRED with the alpha test rather than replacing it. (ii) **A comment stating why an alternative was rejected is a CLAIM with no test behind it**, and it is the most inherited artifact in a codebase — the next reader treats it as measured. When a comment is the only thing standing between you and the obvious fix, test the comment. (iii) **I verified the previous fix by reading the SOURCE and stopped there.** That is the same defect as `no.md` #32/#34/#37 arriving through a symptom report instead of a test: the artifact is a different subject from the code, and only running it — or in this case, measuring what the window server and LaunchServices actually report — asks about the artifact. The owner's second report was the instrument I should have been. Decided 2026-08-15 (CTS-8CE92EAB).

61. **The override that suppresses a correctness invariant was wrapped around the WHOLE suite, so every test ran with that invariant switched off — and the wrapper was put there by the same session that split the predicate to prevent exactly this.** `no.md` #58 split one question into two: `ConstrainedTimingEnvironment.isActive` answers *"is my wall clock comparable to a developer's?"* and gates the six absolute-budget lanes, while `canEvaluateElapsedInvariants` answers *"can I order two durations I measured myself?"* and gates `no.md` #18's overshoot discriminator. The split is right, and its own definition-side mutation row was added. **What nobody checked is the two CONSUMERS.** `stage_test` wrapped the entire Swift run in `_swift_timing_environment()`, which exports `VERDICTUI_RECORD_TIMING_ONLY=1` — and that variable is not a clock marker, it is the **explicit human override**, the one and only input `canEvaluateElapsedInvariants` reads. So the PM's full-suite run had the overshoot guard inert on every host, on every run, while printing 787 tests / 0 failures. #58 measured precisely this and recorded it as a hypothetical: *"forcing `VERDICTUI_RECORD_TIMING_ONLY=1` runs 784 tests to 0 failures with 3 skipped, i.e. the gates stop gating and every signal stays green."* It was not hypothetical; the PM was already doing it. **`stage_runtime_bench` keeps the wrapper and is correct to** — that stage measures absolute figures, which is what the override is for. The difference between the two call sites is the whole point, and one line of `with` hid it. **Three things generalise.** (a) **Splitting a predicate is half a fix.** The defect recurs at every site that READS it, and from outside the two failures are indistinguishable — both leave the gate inert while green. A definition-side mutation row cannot see a consumer naming the wrong lane, so each consumer needs its own row; both were added here (121 targets, up from 119). (b) **An override strictly stronger than the inference it overrides must never be applied broadly.** `isActive` is an inference about the host and is safe to widen; `RECORD_TIMING_ONLY` also suppresses a correctness check, so wrapping a whole suite in it is a different act from setting it for one benchmark, even though the code reads the same. (c) **This arrived as an unattended repair agent's uncommitted diff** — the writer `no.md` #59 says should not exist — and it was correct. The rule that survives is not "trust it" but the one that made judging it cheap: read every line, re-run the suite, and mutate each new guard in both directions before adopting. Verified 2026-08-15: both rows hand-mutated to exit 1 naming their assertion, restored byte-identically (sha256 confirmed), full suite 787/0/0. Decided 2026-08-15 (CTS-91B278AB).

62. **A SKIPPED XCTest is byte-identical to a passing one, so the mutation harness read a working guard as untested — and the accusation is the expensive half.** The full 121-row sweep that closed `no.md` #61 returned exactly one UNNOTICED: `the AX walk keeps its depth bound but loses its node budget`, witnessed by `ThirdPartyAuditTests/testTheReaderIsBoundedAgainstAHostileTree`. The obvious reading is a coverage gap in a real safety bound — the very guard added after an unbounded AX walk segfaulted the runner (`no.md` #44). **It was not a gap. The witness had skipped.** Its first three statements are `XCTSkipIf(isHeadless)`, `XCTSkipUnless(AXReader.isTrusted)` and a skip when no third-party app publishes a readable window; measured 2026-08-15, it completed in **0.062 s** against the ~2 s a real AX read costs. **And XCTest emits NO skip marker**: the output reads `Test Case '-[…]' passed (0.062 seconds)` and `Executed 1 test, with 0 failures`, exit 0 — searched at default and at `-v`, there is nothing to parse. `--xunit-output` does not rescue it either; the file written here covers only the swift-testing lane (0 tests), not the XCTest lane. So `classify` saw `ran == 1` and `returncode == 0`, fell through its four cases (compile failure, trap, zero-tests, nonzero exit), and printed **"UNNOTICED — the test passed with the guard broken."** Every word of that is false: the test did not pass, it did not run, and the guard is fine. **The direction matters more than the defect.** A missing NOTICED reads as *your code is unprotected* and sends the next session to rewrite a correct bound — the same shape as `no.md` #26's false red, and `no.md` #14's rule again (*suspect the apparatus before the subject*), arriving through an environmental precondition rather than a cache. It is also `no.md` #58 one layer out: a check that cannot observe its subject reports identically to one that observed and approved. **The fix could not be a stdout parse, because there is no signal to parse** — so the row declares the condition, `skips_when=`, and `baseline_problem` refuses it *before running anything*, alongside the two unusable-witness cases already there (already-red, matches-nothing). That is the same discipline as `runtime_witness_reason` (`no.md` #23): a claim about the APPARATUS is stated on the row and checked, never argued in a comment nothing reads. The refusal names the condition, so an operator gets "run it on a host where it executes" instead of a false accusation. Negative-controlled in both directions (`if False` on the check → the declaring test fails, restore byte-identical at sha256 `54ce01b4…`), pinned by a mutation row, and by a test asserting **this specific row** still carries its declaration — by name, not by count, so deleting it fails loudly rather than quietly restoring the false UNNOTICED. **The general rule: a test that did not run is not evidence, and no runner is obliged to tell you it did not run — so any harness that scores a test's OUTCOME must first establish that the test was CAPABLE of running, and where the runner cannot say so, the catalog must.** Decided 2026-08-15.

81. **No fleet-wide SessionStart hook edit from a project save — the extension to `inherited-dirty-tree-session-start.sh` is CORRECT and was still deferred.** This session's root cause (lesson 1378 / AP-3359) was an abandoned uncommitted refactor whose dirty files all **parsed and linted clean** while the breakage sat in a SIBLING artifact: `mutation_catalog.py`'s anchor quoted source text the refactor had moved, so `--verify-targets` read 1 of 138 stale. The natural prevention is to teach the existing hook — which already owns the right moment, SessionStart on inherited state — to run the anchor check too. **Measured: the gap is real** (zero hits for `verify-targets|mutation_catalog|anchor` in the hook) **and the hook checks SYNTAX ONLY by design**, its own comment saying "A full lint would be noisy on work-in-progress". So the finding is sound and the fix is obvious. **It was still not made here, for a reason that is about BLAST RADIUS rather than effort:** the hook resolves its subject from `CLAUDE_PROJECT_DIR`, so it runs for all 127 registered projects, and a SessionStart hook **fires before any session can review it** — a bad edit is not caught by the next session, it degrades the next session's startup. That is a different risk class from a VerdictUI edit, and it is the narrow case where DIR-035 tier 3 and DIR-038's "fix, don't file" do not conflict: DIR-038 forbids filing to AVOID work, not filing work that genuinely needs its own verification pass. The hook's own header argues the same way ("This is deliberately NOT a fourth working-tree gate"), which is evidence its authors already weighed casual additions and declined them. **The general rule: a fix's correctness and its safety to make RIGHT NOW are separate questions, and the discriminator is whether a mistake would be caught before it costs anything — for an artifact that runs before review, it would not.** Tracked as CTS-1977E67E with the measurement and a falsifier, not as a vague follow-up. Decided 2026-08-24.


## A ported API with no caller is not an integration

**2026-08-17.** `AXReader.press` was added, tested, mutation-verified and
committed — and reachable by nobody. No CLI verb, no MCP tool, no production
caller: measured by grepping every call site, which returned only its own test.
The capability existed solely for someone already writing Swift against this
package, which is exactly the audience that does not need a tool.

The owner asked "did you integrate your instrument into VerdictUI?" and the
honest answer was **no** — the code was on main and the capability was not.
That is the same defect this project already guards one level down (a rule is
only worth the call site that invokes it), shipped by the person who wrote the
guard.

Closed with `verdictui inspect --pid <n> [--press <name>]`, and the assertion
deliberately lives on the ARTIFACT: an in-process test of `InspectCommand`
passes against a binary that never registered the subcommand. Mutation-verified
— removing `Inspect.self` from the subcommand list turns the smoke test red.

The rule to carry: **"is it committed" and "can anyone reach it" are different
questions, and only the second one is integration.** Ask the second by running
the shipped binary, never by reading the source.

## Read and act must share one identity, or the round trip is a coincidence

**2026-08-17, CIS-3DDA018A.** `readTree` and `press` searched different
vocabularies AND different subtrees, so the natural workflow — read the tree,
act on what you saw — was precisely the one that failed. Worse, the obvious
conclusion from that failure ("the press is broken") was wrong, which is the
expensive part: a caller debugs the wrong half.

Two independent causes, and finding only one would have left it broken:

1. **Attribute order.** `readTree` names an element Value → Description → Title;
   `press(named:)` tried Title → Description → Value.
2. **Anchor.** `readTree` walks from the hosting content group so frames land in
   root coordinates; `press` walked from the window.

Fixed by pressing on **structural path** — the identity `readTree` already
assigns — and by extracting `anchoredWindow()` so the two cannot drift onto
different anchors again. The path walk mirrors
`SemanticNode.withAssignedStructuralPaths` deliberately: **a resolver that
indexes differently from the assigner is a second implementation of one rule,**
and neither copy's tests can see the other's drift.

Two mutations were needed, not one. "Never resolves" and "resolves to the
**wrong** element" are different defects, and the second is worse — it reports
success while pressing something else. A guard that only catches the first is
half a guard.

**The rule: when one component names things and another acts on those names,
the naming must have exactly one implementation.** Two is a coincidence waiting
to expire.

## The witness skips are the HOST refusing to publish new windows, not an anchor defect

Measured 2026-08-17, with **no VerdictUI code in the path** — a 20-line probe
that opens an `NSWindow` and a separate reader process that asks the
accessibility server what it publishes:

| Target | `kAXWindowsAttribute` | Windows | Geometry |
|---|---|---|---|
| Freshly-launched GUI process | **-25204** (`kAXErrorAPIDisabled`) | 0 | — |
| Finder | 0 | 1 | readable |
| Same fresh process, after 6 s | **-25204** | 0 | — |

The fresh process reports `NSApp.windows = 1` in the same instant. So the window
exists, the accessibility API works (Finder proves it), and the grant is intact
— but a **newly-launched** GUI app is not published to the AX server in this
login session. Not a timing race: it is stable at 6 s and across three
consecutive suite runs.

### Why this is not CIS-C5D9A5E8, despite sharing a throw site

Both surface as `Failure.anchorUnreadable`, which invites the conclusion that
one fix closes both. It does not:

- **CIS-C5D9A5E8** is `hostingContent(of:)` finding no `AXHostingView` and no
  `AXGroup` on a pure-AppKit app. The window IS published; its anchor is not
  classifiable.
- **These skips** are one line lower, at `frame(of: content)`. The witness host
  is SwiftUI (`verdictui-witness-host/main.swift` imports SwiftUI;
  `WitnessHost` builds an `NSHostingView`), so it publishes a perfectly good
  `AXHostingView` — there is simply no window to find it in.

The window fallback a reader reaches for (`?? window`) is **already present** at
`AXReader.swift:262` and cannot help: the window publishes no geometry either,
because it is not published at all.

**A shared throw site is not a shared cause.** Two conditions reaching one
`throw` look identical in a test report and need opposite fixes — the general
shape of lesson 231, arriving through an error type rather than a symptom.

### What follows for the verification bar

"`swift test --filter VerdictUIWitnessTests` reports 0 skipped" **cannot be a
release criterion for an anchor fix.** On a healthy login session it is already
0 (measured today at 17:32); on a degraded one no code change can make it 0. A
criterion that no code change can satisfy is not a test of the code.

Scope an anchor fix to what it actually governs — `inspect --pid` exits 0 on a
pure-AppKit app — and treat the witness channel as an environment precondition,
which is what its own skip text has said all along.

### The recurrence, and the one cheap experiment

The same transition happened on 2026-08-12 (`WitnessIntegrationTests.swift:55`):
passing 17:22:55, failing 18:04, source byte-identical. Today: 0 skips at 17:32,
6 at 18:09. Nearly the same clock window, five days apart, both after a session
of launching and killing unsigned GUI apps.

That is a correlation with two observations, not a cause — but it is cheap to
test and nobody has: **reboot, then re-run the probe above.** If a fresh login
session publishes new windows, the contaminant is session-accumulated state and
the suite needs no change. It is the owner's machine, so it is the owner's call.

## The titlebar-corrected anchor fallback cannot be built on this host, and its premise is not universal

Attempted 2026-08-17, stopped deliberately before writing code. Two measurements
say why.

**1. The subject cannot be observed here.** A pure-AppKit window — the exact
shape CIS-C5D9A5E8 is about — was built and launched (`NSWindow` + `NSTextField`
+ `NSButton`, zero SwiftUI). Reading it cross-process returns **no window at
all**, because this login session does not publish newly-launched GUI apps (see
the entry above). Every app that DOES read here — Finder — was launched
2026-08-16, before the degradation.

So on this host the anchor gap is unobservable: the read fails earlier, for an
unrelated reason. Writing the fix now would mean shipping a correction whose
before-state was never seen and whose after-state cannot be verified. That is
the "plausible-looking lie" the existing comments guard against, one level up.

**2. The 32 pt premise does not generalise.** `AXReader.swift:259` records the
window frame as including "a titlebar (measured at 32 pt)", and a correction of
that size is the obvious fix. Measured against Finder:

```
WINDOW    frame=(0, 0, 1728, 1117)
  [0]     role=AXGroup  frame=(0, 0, 1728, 1117)
```

**Identical.** The offset is 0, not 32. So the correction is not a constant to
subtract — it is a per-window quantity that must be DERIVED from the content
element, and on the window whose content element is missing (the whole failing
case) there is nothing to derive it from.

That is the trap: a fix that subtracts a hardcoded 32 would corrupt every frame
by 32 pt on windows like Finder's, in exchange for maybe-correcting windows
nobody could measure. It would convert a loud failure (`anchorUnreadable`,
exit 2, honest) into a silent one (a tree whose every frame is wrong by a
constant) — the precise inversion `frame(of:)`'s own comment warns about when it
refuses to return `.zero`.

**What a correct fix needs, when a host can run it:** a window that publishes no
usable content element, read on a session that publishes new windows, plus a
control window on the same session where the derived offset can be checked
against a known-good `AXGroup` anchor. Until both exist in one session, the
honest state is `anchorUnreadable` — which names the failure accurately and
costs a caller nothing but an exit 2.

63. **A path-based purge cannot see a string, so the PROSE describing a leak outlives the files — and the person most likely to write that prose is the one doing the cleanup.** `no.md` #54 recorded this as a "quieter half" aside: my own `wave-status.md` had named the payment details while describing the purchase that produced the leak. This session shows it is not the quiet half — it is the PRIMARY failure mode, and it recurs on the remediation document itself. **What survived two prior purges.** Both earlier passes ran `git filter-repo --path .playwright-mcp --invert-paths`, which removes blobs by PATH. A payment-card last-four sat in a SENTENCE in `docs/wave-status.md` (commit `89198bc`), reachable from `origin/main` *and* from `feat/appkit-merged`, so it was never a GC candidate at all. It was found only by scanning reachable history for the identifier STRINGS rather than the filenames — the same command the tickets now carry as `falsify:`. **Then I committed the identical defect.** The GitHub Support request I wrote to ask for the fragment's removal QUOTED the literal digits while explaining why they must be removed, reintroducing the string to `main` in `8573d1a`. It was caught only by re-cloning the LIVE remote after pushing and re-scanning — `main` read 0 matches before the push and 1 after — then fixed by amend + force-with-lease to `9fc2cfa`, with a final fresh-clone verification reading 0 on every branch ref. **A remediation doc needs the same string scan as the thing it remediates**, and nothing in the process asked for one, because a document about removing a secret does not look like a place a secret would live. **Three method notes, each of which changed the answer.** (a) **The derived copy lies.** The rewritten MIRROR reported `refs/pull/14/head` clean; the LIVE remote reported 1 match. The mirror's PR ref carried a different sha (`ad14b46` filtered vs `e241793` live), so it was answering about a different object — `no.md` #14's rule arriving through a git ref rather than a cache. Verify a purge from a **fresh clone of the live remote**, never from the mirror you rewrote. (b) **Enumerate live refs first, and do not trust a prior note's enumeration.** Four earlier notes recorded live refs as "main only"; there were TWO heads plus three `refs/pull/*`. GitHub owns `refs/pull/*` and a force-push cannot move them, which is why the remaining fragment is a Support request rather than a command. (c) **Target the PHRASE, not the number.** A bare `9337` replacement would have corrupted `.vexp/index.db`, where all 24 occurrences are incidental digit runs inside float scores (`0.09337261`) — falsified by inspection before the rewrite, not assumed. **The general rule: a purge is only as wide as the question it asks, and "which files contain this?" is a narrower question than "which bytes contain this?" — so scan for the STRING across all reachable history, include your own remediation artifacts in that scan, and verify from a fresh clone of the live remote rather than from any copy you produced.** Decided 2026-08-17 (CTS-77D76C19, CIS-CB73F2EA; the same datum found in 3 committed files of `claude-config` and filed out-of-scope as CIS-803D2927).

64. **Two independent PII sweeps both reported clean and one card number was still live on `main` — because their blind spots did not overlap, and because a sweep reports clean in proportion to the question it asked.** After a Luhn sweep (mine) and a context sweep (a peer's, merged), re-measuring `origin/main` found a 13-digit Scotiabank Visa still served, quoted beside its cardholder. **Neither filter was sufficient, and each missed what the other caught.** Luhn alone misses numbers committed next to a cardholder name that are not checksum-valid, and it fires on the fractional parts of stage durations, which are Luhn-valid often enough to matter. Issuer-context alone missed this one because `scotialine` was not in the hand-written issuer list and the number sat inside a quoted JSON key rather than beside a bare brand word — the lesson-219/240 shape, a rule keyed on a hand-copied subset being blind to everything the list omits. The verification that works is the **UNION** — Luhn OR issuer-context, excluding digit runs abutting `.` — run over every file in the tree, not over the file the first hit happened to be in. A pure *shape* rule (any 13–16 digit run adjacent to a quoted key) was proposed and **rejected on measurement**: on this corpus it fires on stage-duration floats, SHA fragments and GitHub `databaseId`s. Incompleteness of a vocabulary is an argument for ADDING a second arm, not for deleting the first. **Two further traps, both of which cost a wrong reading before being caught.** (a) **A stale branch and an unmerged improvement look identical at `git diff --stat`.** My earlier PR still differed from `main`, which reads as "has work to contribute"; it was actually BEHIND, still carrying five PANs in a sibling file that the peer's PR had already fixed, so merging it would have REINTRODUCED them. Compare CONTENT per ref, never the diff stat. (b) **A large file returns `encoding: "none"` and empty `content` from the GitHub contents API**, so a scan of that response reports zero markers and unparseable JSON — indistinguishable from a clean file. Fetch via the blob API and assert the decoded length. **The general rule: a clean result is a statement about your filter, not about the corpus — so verify with a union of filters whose failure modes you have actually measured, on the artifact as it is served, and re-measure after someone else's fix lands rather than accepting either report, including your own.** Decided 2026-08-17 (projects-shared PR #58 closed as behind, #59 merged, #60 open; CIS-8E1A790C).

65. **"The artifact describing a leak is a leak" is true, and applying it without judgement DELETES THE EVIDENCE OF HOW THE DETECTOR FAILS.** `no.md` #63 established that remediation prose inherits the leak; the closeout sweep then found the same digit strings in three store rows and the reflex was to redact all of them. Two of those rows — `CIS-3FEBD82F` and `CTS-E96D98A3` — quote two Luhn-valid digit runs **while explaining that each is the fractional part of a `"duration"` value** in a PM timing record. They are the only record of why a boundary-less Luhn matcher false-positives. Redacting them would have left the finding unreadable and guaranteed someone rediscovers it from scratch. A third hit was the standard Visa TEST number (the well-known all-1s value), which is nobody's card. **The discriminator is whether the digits are the SUBJECT or the ILLUSTRATION**, and it cannot be automated — a scanner sees the same 16 characters either way. This matters because the automatable half of this class WILL get built (a write-time gate, a store scanner) and the judgement half will get skipped, leaving a tool that faithfully destroys its own diagnostic corpus. **A fourth carrier makes the point that no author can be the control:** a peer's inter-agent message about these PANs was captured into the InstructionStore *by the instruction-capture hook* — created by machinery, not by a person, so nobody wrote a document and the leak propagated anyway. The remediation surface therefore includes conversation-capturing machinery and store rows, not just files anyone thinks of as prose. **The general rule: before redacting a match, read whether the datum is what the text is ABOUT or what the text is EXPLAINING — and when a class of finding gets a detector, state explicitly which half of it the detector cannot decide.** Decided 2026-08-17 (structural half tracked by a peer as CIS-70B3328C; four measured instances).

66. **A predicate replaced to stop trusting a proxy was tested only where the proxy and the truth AGREE, so reverting it was invisible.** `hasUnwritableSwiftPMCache` was changed from `FileManager.isWritableFile` (permission bits) to an actual write probe, because a sandbox can deny a write the mode bits permit — the condition `no.md` #17 records, where a repair sandbox measured p50 167.50 ms against a 70 ms budget and filed three P1s for a regression that did not exist. It shipped with two tests, and neither could see the change being undone. Measured 2026-08-18 by hand-mutating the consumer back to the mode-bit check: **the suite passed 2/2.** The reason is structural rather than an oversight — `testTheConfiguredSwiftPMCachePathsUseTheWriteProbe` reads the REAL cache paths, which on healthy developer hardware are writable by BOTH methods (verified: `drwxr-xr-x` owned by the running user), so both sides of its equality read `false` and it holds under either implementation. It is `no.md` #12 and #17 arriving together: an assertion satisfied identically by the correct and the broken code, on a predicate whose tests only ever exercise the agreeing branch. **What made it fixable was finding a disagreement that needs no sandbox.** Three candidates were measured and two were FALSIFIED before the third worked: `chmod 555` agrees (both say no), and the `uchg` immutable flag also agrees (Swift's `isWritableFile` already accounts for it — measured directly rather than inferred from Python's `os.access`, which is a different implementation and not evidence about Swift's). A writable **regular file** separates them: `isWritableFile` returns **true** while writing a child into it fails, because it is not a directory. That is reproducible on any host, in any sandbox, forever. The consumer gained a `hasUnwritableCache(among:)` seam purely so a test can supply such a path, and the negative control asserts the fixture's mode-bit writability FIRST — otherwise "the check rejects this path" would be satisfiable by a fixture both methods reject, which is the same defect one level down. Verified in both directions (mutation → exit 1 naming the assertion with the witness confirmed RUN at 3 tests executed; restore → 3/3 green, byte-identical) and pinned by a mutation row. **The general rule: when you replace a proxy with a direct measurement, the test must run where the proxy and the measurement DISAGREE — anywhere else, the old implementation passes and the replacement is decoration. And if the disagreement only occurs in an environment you cannot reproduce, that is not a reason to skip the control; it is a reason to go looking for a cheaper instance of the same divergence.** Decided 2026-08-18.

67. **No per-node `actionable` field on the compact wire form, and no mutation row that breaks the WITNESS instead of the subject.** CTS-71083452 asked for actionability to be discoverable and *preferred* a per-node field, noting the byte budgets as the thing to measure. Measuring first (`no.md` #24) moved the answer twice. **(a) The budgets do not govern the surface the ticket named.** CLI `render` emits the nested `SemanticNode` form (524 B for a 3-node tree); the 512 B / 64 B budgets pin the act DELTA. So "keep both regimes green" was satisfiable by not touching the delta at all — the constraint the ticket treated as central was aimed at a different wire. **(b) The compact tree is PARALLEL ARRAYS**, so a per-node boolean is not a per-node cost, it is a full-length array added to every response: one entry for every node to describe a property almost no node has. Measured on the fixture: 3 nodes, 2 probed, 1 actionable — and on a real screen the ratio only worsens, because a screen is mostly containers and text. That is `no.md` #41's distribution error exactly, arriving as a *feature request* rather than as a compaction: the shape that looks free on the example you are holding is the one that loses on the case that actually recurs. Shipped instead as a separate `actions` verb returning `{probe: [verbs]}`, where **absence means not-actionable** — the same omit-the-empty-case reasoning that took an inert act from 170 B to 2 B. The information is derived from the same dictionaries `performTap` and its siblings consult, never a second registry, because a registry listing "what is actionable" is a CLAIM ABOUT the thing that decides it and the two drift the moment either changes. **The verbs reported are the ones that will be ACCEPTED, not the storage's name**: a bool reports `["tap","toggle"]` because `performTap` falls through to a toggle when no separate handler exists — my own first test asserted `["toggle"]` and the run corrected me, which is the fixture-before-expectation rule paying for itself again. **And the finding that generalises furthest is about the MUTATION ROW.** A row aimed at the new contract-vs-catalog guard mutated the TEST — `let served = documented`, making both set comparisons self-referential — and scored **UNNOTICED**, correctly: *no test can witness its own vacuity*, so a row that weakens the witness proves nothing about the guard. Re-aimed at the SUBJECT (rename a served tool) it scored NOTICED at `Executed 1 test, with 2 failures`. **The rule: a mutation must break the thing the guard WATCHES, never the guard itself — and an UNNOTICED verdict is sometimes a defect in the row rather than a gap in the coverage, which is the reading that costs a session if you take it the other way.** Decided 2026-08-18 (CTS-71083452).

68. **The `no.md` #67 defect had a SIBLING one layer up, and the gate is the worse place for it.** #67 records a per-node wire field rejected on measurement and a mutation row that must break the subject rather than the witness. A cold check of every open item the same day found the same shape in `stage_transport_smoke`: it drove the shipped binary over the real MCP wire and asserted a **hand-copied set of six tool names**. Correct when written; then `focus`, `judge_appkit` and `actions` each shipped past it over successive waves, so the gate silently stopped covering them — **and they are the three most recently added, i.e. the least proven**. Measured: the stage required 6 verbs while the product served 9, and its own detail string had been printing the true count all along with nothing comparing the two. **Why a GATE is a worse host for this than a classifier** (AP-797's usual form): a stale classifier MISFILES something, so a wrong answer is at least present and eventually contradicts something; a stale gate simply STOPS ASKING, and the absence of a question is indistinguishable from a question answered correctly. It also degrades monotonically — every new member widens the blind spot, so the check is weakest exactly where the surface is newest. Fixed by deriving the required set from `contracts/mcp-tools.md`, which the converse test (every served tool must BE documented, `no.md` #67) already keeps honest in the other direction — the two together are what make either trustworthy, since deriving from a doc nothing checks would just move the staleness. **Two failure modes the derivation INTRODUCES, both pinned rather than assumed away**: a parse finding nothing must FAIL the stage, never yield an empty requirement (a required-set of nothing is satisfied by any catalog, including an empty one — lesson 202); and the parser must still EXCLUDE the verb documented as deliberately NOT served, or the gate inverts and demands `baseline_accept` answer over the wire, contradicting the SD4 assertion two lines below it. Hand-verified in that direction, with a byte-identical restore. **And the fixture carried the same rotted list, which is worse there than in the stage**: a healthy-catalog fixture missing a real verb makes the CONTROL fail for a reason the test never intended, which reads as the subject being broken rather than the fixture. **The general rule: when a gate asserts membership, ask what keeps its list in step with reality — if the answer is "someone remembers", it is already stale, and unlike most staleness this kind reports success while it grows.** Decided 2026-08-18 (fleet lesson 912; AP-797 extended with the gate variant).

69. **A CONTROL can point at the wrong guard and still pass, so the mutation that proves a test is also what identifies which line the test protects.** Hardening the contract parser (`no.md` #68) added a control asserting that `####` sub-headings are not tools. It passed — and the first mutation written to prove it, loosening `startswith("### \`")` to `startswith("###")`, left it **still passing**. Both implementations agreed, so the assertion proved nothing about either: `no.md` #12's rule arriving inside a control I had just written to satisfy that very rule. **Measuring settled it in one command**, and the answer was not what the docstring claimed: `####` DOES satisfy `startswith("###")`. What rejects a sub-heading is the NEXT line — dropping exactly three characters and requiring a backtick, which leaves `#` for a fourth-level heading. A second mutation, changing that drop to `lstrip("#")`, fails the test naming both `not_a_tool` and `also_not`. So the test was real all along and my **explanation** of it was wrong, which is the more dangerous defect of the two: a green test with a correct name and a false docstring tells the next reader that the heading match is load-bearing, and they will "simplify" the line that is actually doing the work. **Three things generalise.** (a) A control is a claim about WHICH property is enforced, not merely THAT one is — and only a mutation can distinguish those. (b) When a mutation leaves a test green, the first hypothesis should be *I broke the wrong line*, not *the test is weak*; here the test was fine and the aim was off, the mirror image of #67 where the row broke the witness instead of the subject. (c) **The parser was otherwise sound and the check found that too** — five of six malformed inputs (empty file, no headings, nameless heading, binary junk, weird spacing) returned the empty set, which the caller treats as a hard failure rather than a partial guess, so the robustness pass confirmed a design rather than only finding a flaw. Worth doing for that reason alone: a probe that only ever reports defects is one nobody runs twice. Decided 2026-08-19.

70. **A DETECTOR IS SUBJECT TO ITS OWN FINDING, and the only way to learn that is to probe it with inputs you did not write it against.** Pass 1 of this session's cold checks shipped a `GATELIST` detector into `ssot_detect.py` to catch gates asserting hand-copied membership lists (`no.md` #68). Pass 3 probed it with four spellings of the same defect and found it **misses `MUST_HAVE_STAGES = {...}` and `REQUIRED_TOOLS = {...}` while firing correctly on `must_have` and `required_tools`** — the pattern anchored the keyword at the START of the identifier and matched case-SENSITIVELY, so an UPPERCASE module constant, which is the commonest way a gate spells a required set, was invisible. That is precisely the lesson-219/240 shape the detector exists to catch: *a rule keyed on a hand-picked spelling is blind to every spelling it did not list*. **The tell is that I tested it originally against the defect I had just fixed** — `required = {...}`, lowercase, because that was the literal in front of me — so all four of my original direction-tests shared one input shape and could not discover the blind spot (lesson 342: a suite built from one input shape is green by construction). Fixed with `IGNORECASE` plus allowing the keyword anywhere in the identifier, verified with four spellings firing AND six negative controls still silent (derived form, non-gate file, test file, sub-threshold list, unrelated identifier, dict-not-set) — because widening a detector without re-checking its false positives is how a detector loses the credibility that makes anyone act on it. **The second finding is a correction to myself, and it is the more instructive one.** Reading the file afterwards I saw `except OSError, ValueError:` and called it invalid Python 2 syntax written by a concurrent session. `ast.parse` said the file was fine and the module RAN fine, and I initially read that agreement as the apparatus being broken. It was not: Python 3.14 parses that form as a **tuple of exception types** under **PEP 758**, which `ast.dump` confirms directly (`Tuple(elts=[Name(id='OSError'), Name(id='ValueError')])`). The fleet has a hook for exactly this (`python2-except-syntax.sh`) because THREE prior sessions each 'discovered' it as a fleet-wide bug and began mass-fixing correct code; it fired on my search and named the PEP. The code was correct and my pattern-match was wrong. **`no.md` #14 says suspect the apparatus before the subject; this is its limit case — when TWO independent instruments (a parser and a live run) agree against your reading, the reading is the thing to check.** Decided 2026-08-19.

71. **The probe that fixes a detector is the probe worth RE-RUNNING along a different axis — and a probe harness lies as readily as the subject it tests.** `no.md` #70 fixed a case-sensitivity blind spot by feeding the GATELIST detector spellings its author had not tested. Pass 4 ran that same discipline along a **syntactic** axis rather than a naming one, and found three more real misses: `REQUIRED = frozenset({...})` (a constructor call before the brace), `REQUIRED: set[str] = {...}` (a type annotation between the name and `=`), and `if served != {...}:` (a COMPARISON, binding no name at all). **The comparison form is the one that mattered**, because it is how a gate asserts membership inline and there is no identifier for a keyword-anchored pattern to attach to — so the exact shape the detector exists for could sit in a gate and never be seen. Matching it without a keyword requirement is only defensible because the cost was MEASURED: across `hooks/lib` and `VerdictUI/scripts` there is exactly ONE comparison against a quoted-name literal, a two-element assertion in a test, below the four-name floor and therefore unable to fire. **The second half is a harness failure I nearly recorded as a subject failure.** A fourth apparent miss — the multiline set that `ruff format` produces, which is the shape a real 9-name gate actually has — turned out to be my shell heredoc passing literal `\n` characters rather than newlines. The detector had been handling it correctly the whole time. Re-probing with REAL FILES rather than shell-escaped strings separated the three genuine gaps from the one phantom, and it is worth noting the phantom was the *most alarming* of the four: had I "fixed" it, I would have widened a pattern that was already right. **The general rule: when a probe reports a miss, reproduce it with the same artifact a real caller would supply — a file, not a string you escaped by hand — because a probe harness is a program too, and `no.md` #14's rule applies to the thing doing the measuring even when the thing measured is itself a measuring tool.** Also verified that widening did not disturb the two sibling detectors sharing the module (LIST and SCALAR both still fire), which is the check a shared-module change needs and a single-detector test cannot provide. Decided 2026-08-19.

72. **A DETECTOR THAT FIRES ON ITS OWN RECOMMENDED FIX IS WORSE THAN ONE THAT MISSES A CASE — so the last widening pass widened by ONE of four candidates and documented the three refusals.** `no.md` #70 and #71 both ended by widening the GATELIST detector, and the natural momentum of a fifth pass is to widen again. Probing with seven shapes passes 3–4 had not used produced four apparent misses, and only **one** was a real gap: `if served >= {…}`, a subset comparison that is the same gate assertion in different clothes — semantically identical to the `required - served` that `stage_transport_smoke` computes. The other three were **correct behaviour**, and the reasoning matters more than the fix. `if tool in {…}` is ONE item's membership: a lookup, not a claim that the catalog equals a required set, and a gate asking "is this allowed?" is a different question from "does what I serve match what I require?". `assert_set({…})` puts the semantics in the callee, which the literal cannot reveal. And `{f"{p}_a", …}` is a **COMPUTED** set — which is precisely the fix this detector recommends, so firing on it would punish the remedy. **That last case is the rule: a detector that fires on the fix it advises does not merely produce a false positive, it teaches its reader that the finding is noise, and it pays that cost on every future finding rather than only on the wrong one.** A miss costs one catch; a false positive on the remedy costs the reader's attention permanently. So the asymmetry runs the opposite way to the instinct that four misses means four fixes. **The verification shape that encodes this: the negative-control set now includes the two forms representing the recommended fix** (a computed set and a derived call), so "the detector does not punish its own advice" is asserted rather than assumed — 8 shapes firing, 9 controls silent, and the sibling LIST/SCALAR detectors in the shared module still working. **The general rule: before widening a detector, ask of each candidate not "is this the shape I am hunting?" but "would the fix I recommend for this finding also trip the rule?" — if yes, the miss is correct and the widening is the defect.** Decided 2026-08-19.

73. **A GATE THAT RESOLVES ITS SUBJECT WITH `which` CHECKS THE COPY THAT CANNOT BE STALE, AND MISSES THE ONE THAT SHIPS — and its docstring said so all along.** `stage_installed_parity` was written after a 41h-stale install went unnoticed, and its docstring names the subject precisely: *"the artifact every other project and every MCP client actually reaches"*, closing with *"a check blind to the artifact that ships cannot fail for its own reason."* It then resolved that artifact with `shutil.which`, which returns the **first** PATH hit. Measured 2026-08-19: two copies are on PATH, and the first is `~/.local/bin/verdictui` — the developer's own build, **in parity by construction**, because the session that builds also installs it. The copy that SHIPS (the Homebrew tap symlink) sat second and was four subcommands behind (`actions`, `appkit`, `inspect`, `judge`). The gate reported `installed parity ok` for as long as it had existed. **The stage was an instance of the defect class it was written to prevent**, which is `no.md` #70's shape (a detector subject to its own finding) arriving in a PM stage rather than a detector. **Why no test could see it:** all five existing tests patch `shutil.which` to a single path, so the fixture population could not express a second copy — `no.md` #52's rule, where the missing shape is not merely unwritten but *unwriteable in the place the fixtures live*. The new witness had to drive the resolver directly over a real two-entry PATH. **Two process findings outrank the fix.** (a) **My first mutation row scored UNNOTICED and the row was wrong, not the coverage.** It broke the resolver, but the stage-level test *stubs* the resolver, so the witness never executed the mutated code — `no.md` #67 exactly: a mutation must break what the guard WATCHES. Re-aimed at a direct resolver test it scored NOTICED at `assert 1 == 2`. (b) **My second row also scored UNNOTICED, and that exposed a defect in my own fix.** I had written the terminator as two conditions (`if not line.strip(): break` then `if not line.startswith(" "): break`) — but `"".startswith(" ")` is False, so *either* line alone terminates on a blank and breaking one leaves the other doing the job. Two cooperating lines, each masking the other's removal (`no.md` #15). Collapsed to ONE condition whose removal *is* the bug, and it then scored NOTICED. **The parser defect that turned up on the way is worth its own note:** ArgumentParser closes `SUBCOMMANDS` with a blank line then `  See 'verdictui help <subcommand>'…`; a blank line is FALSY, so the original `if line and not line.startswith(" ")` walked past it and collected **`See` as a subcommand**. It cancels today because both binaries emit the footer — which is precisely what makes it dangerous, since a shipped copy lacking that line would be reported as missing a subcommand named `See`, sending the reader after a command that has never existed. **And one correction to myself:** I first "measured" the built binary's subcommands with `grep -oE '^  [a-z]+'` and concluded `adoption` did not exist, i.e. that the ticket's list was wrong. The grep was the unreliable instrument (indentation), `adoption` is real, and the ticket was right — `no.md` #71's rule, that a probe harness lies as readily as its subject, caught in the act of nearly "correcting" a correct list. **The general rule: when a gate names its subject in prose, check that the code RESOLVES that subject — a resolver that returns the first of several candidates silently picks the one least able to fail, and the gate then reports on the artifact nobody ships.** Decided 2026-08-19.

74. **A GATE THAT CANNOT SEE THE MACHINE IT RUNS ON REPORTS A QUEUE AS A DEFECT, AND THE TICKET IT FILES IS INDISTINGUISHABLE FROM A REAL ONE.** In one day this project accumulated **ten P1 tickets**, every one of them a `check.py --all --quick` fleet sweep racing this worktree — 127 projects driven through the same SwiftPM build directory. `stage_runtime_bench` read **102.52 ms** against a 70 ms budget while an exclusive run of the same commit read **49.80 ms**; `stage_mcp_latency` read **43.82 ms** against **8.32 ms**, a 5x swing on unchanged code. Each ticket carried a precise `file:line` citation and read as a code defect; **two carried RESURRECTED markers, re-filing rows already closed for exactly this cause**, and five more were re-filed hours after I closed the first batch. `no.md` #15/#38/#40 had already ruled on this signature three separate times — which is the point: **a lesson recorded in prose does not stop the next automated filing, because the filer cannot read prose.** The gate had no way to distinguish "the code is slow" from "the machine is busy", so `tree_is_contended()` now answers that and `main()` prints the measured numbers plus a worktree recipe. Two decisions are deliberate and each was the harder call: **the exit code is NOT softened** — a red stays red, because suppressing a failure on a contention *guess* is how a real regression gets waved through, so only the REPORT changes; and it **fails toward NOISE** — an unavailable `pgrep` answers "uncontended", because a guard that suppressed reporting whenever it could not measure would be a check that cannot fail for its own reason (lesson 202). **The second half of this entry is the same shape one layer over, and it arrived from a PEER rather than from my own reading.** A session on another project reported 24 of 52 stages passing without observing their subject. Rather than accept or dismiss it, I measured HERE — and this project's own live report carried `[PASS] watch_testwatch  testwatch: not installed — skipped`. The sentence is honest; the `[PASS]` beside it is not, and **the marker is what a reader scans**. "Could not measure" and "measured and clean" are opposite states rendering identically (lessons 202/206). Fixed at the RECORDING layer rather than by changing a contract, because trusting 47 individual call sites to volunteer a skip flag is what let the divergence exist. **The classifier is anchored to whole phrases, never a bare `skip` substring** — `no.md` #58/#62 record 12 suite hits for "skipped" that were test NAMES, every one passing, so a substring rule would call a fully-executed suite a skip: the same false reading in the opposite direction, and it is pinned by its own test. **Three process notes.** (a) **The negative controls did the real work.** "Warns on contention" is satisfied by an implementation that excuses EVERY red, and "reports skips" by one that appends the caveat to every green — both would have shipped green without a control asserting the quiet case. (b) **A near-miss worth recording: my own assertion searched for the substring `contend`, which does not occur in "contention"** (no `d`). The test failed, and the tempting move was to loosen the assertion to fit — which would have weakened a control to accommodate my typo. Reading the actual string settled it in one command. (c) **I deleted a `status is None` guard as dead code and made pyright WORSE** — 1 error became 3, because the two base classes disagree about the return type: Optional at the call, `Never` after the guard. Both halves are load-bearing, and the fix was to NAME the post-guard type rather than delete either. **The general rule: when a lesson has been recorded three times and the same tickets keep arriving, the missing artifact is not another lesson — it is a check that the FILER can execute.** Decided 2026-08-20.

75. **A SCANNER CANNOT TELL A LEAK FROM A LESSON ABOUT A LEAK, AND THE PUBLISH STEP IS WHERE THAT MATTERS MOST.** Making this repo public again — the last step of the `no.md` #54 remediation — ran a Luhn-OR-issuer union scan (`no.md` #64) over every reachable blob rather than only the working tree, because *a purge is only as wide as the question it asks* (#63). The tracked tree was clean at 276 files; **all reachable history returned THREE Luhn-valid card numbers**, and the flip was stopped mid-sequence. All three turned out to live in `no.md` #64/#65 itself, which quotes them **while explaining that they are false positives**: a stage-duration fraction (`1.4944798946380615`), its sibling, and `4111111111111111` — the standard Visa test number, nobody's card. **This is #65's rule arriving as an operational decision rather than a triage one:** the digits are the ILLUSTRATION, not the subject, and redacting them would have deleted the only record of how the detector fails. The scanner was right to flag them and structurally cannot make that call — which is precisely why the automatable half of this class must never be wired to auto-redact. **What made the stop cheap was scanning BLOBS, not files.** A working-tree scan returned 0 and would have published on that evidence; the history scan is what found them, and the difference between those two questions is the entire content of #63. **Two publish-specific findings worth keeping.** (a) **The sha256 for a formula must be measured from the tarball the HOST serves, never computed locally** — verified HTTP 200, 1062760 bytes, and that the bytes are a real gzip archive before trusting them, because a status code alone does not prove content (the `_headers` and DMG lessons, 178/303, arriving at a package manager). (b) **`v1.0.0` is permanently burned and was skipped deliberately.** Its tag pointed at a commit whose tree carried personal data; reusing the version would resurrect a reference to a sha the whole remediation existed to unreach. The tap had therefore been broken for FRESH installs the entire time — its formula fetched a tarball that 404s — which nobody had measured, because the stale-subcommand symptom was louder and got all the attention. **The general rule: before publishing anything, scan the OBJECT GRAPH rather than the checkout, and when a scanner flags a datum, read whether the text is ABOUT that datum before removing it — a corpus that documents its own false positives is the thing that makes the next scan interpretable.** Decided 2026-08-20.

76. **A GUARD BUILT FROM ONE OBSERVED CAUSE IS BLIND TO THE COMMONEST ONE, AND THE FIX FOR THAT BLINDNESS IS ONE TOKEN AWAY FROM MAKING IT FIRE FOREVER.** `tree_is_contended()` shipped hours earlier (`no.md` #74) with a single pattern, `check.py --all`, because a fleet sweep was the contender I had watched produce ten false P1s. Pointing it at the machine it actually runs on found the real gap: **`ceo.py --watch` calls every PM with `--fix` as its documented "Obligate" step**, so the commonest contender is a SECOND `verdictui-pm.py` on the same tree — measured with one live for **15+ minutes** while the guard read `False` throughout. A pattern list is a claim about the world, and every spelling it omits is not merely unmatched but *invisible* (lessons 219/240). **The fix immediately created the opposite defect, and it is the worse one.** `verdictui-pm.py` matches the argv of the very process running the check, so `pgrep` returns our own pid and the guard reads `True` on **every** run, permanently. That is strictly worse than the blind spot it replaced: a guard that always fires teaches its reader to discount it, and it pays that cost on every future finding rather than only the wrong one — `no.md` #72's rule arriving in a live guard rather than in a detector's candidate list. **Both directions are pinned, because either assertion alone is satisfiable by a broken implementation:** "ignores its own pid" is met by ignoring every pid (restoring the original defect silently), and "detects a contender" is met by firing on everything. **The method note is the part worth keeping.** My first self-match test ran the guard from a process *named* `verdictui-pm.py` while a real contender was also live — so `True` could have been either cause, and the test proved nothing while looking decisive. Only excluding the pid in code and asserting against a *synthetic* pid set made the two distinguishable. That is `no.md` #18's shape: a test whose fixture cannot separate the hypotheses is not weak, it is aimed at nothing. **And a smaller one: my own edit invalidated an existing mutation row** — the earlier contention row anchored on `if probe.returncode == 0 and probe.stdout.strip():`, which the self-exclusion rewrote, and `--verify-targets` reported `1 of 133 stale`. The check worked exactly as designed on the person who added it, which is the only real evidence that a guard guards. **The general rule: after widening a detector to catch a case it missed, immediately ask whether the widened pattern now matches the detector's own subject — and when a live probe and a synthetic one disagree about WHY something fired, only the synthetic one is evidence.** Decided 2026-08-20.

77. **A GUARD THAT ENUMERATES ITS SUBJECT'S OWN PROCESSES CANNOT SEE THE SWEEP RUNNING SOMEWHERE ELSE — and the prose above the list already named the contender the code did not match.** `no.md` #76 widened `tree_is_contended()` one day earlier, after a second PM on this tree went undetected for 15+ minutes. The widened list read `check.py --all` and `verdictui-pm.py`, and **both name THIS project's own processes**, so the class it still could not see was a sweep parked inside a *sibling* project. Measured 2026-08-21: `ceo.py --watch 30` (pid 863) was driving a full **SagaMail** `swift test --parallel` suite while this tree's timing stages ran, at **load average 241.37**. On one commit (fda4c1d), contended vs exclusive: `testWarmVerifyRoundTripMeetsTheLatencyBudget` **120.23 → 9.63 ms** (budget 40), `testPerformCycleMeetsTheSLO1Gate` **249.57 → 50.03 ms** (budget 70), `testWarmCurrentTreeMeetsTheSLO1Gate` **106.16 → 6.15 ms**. A 12x swing on unchanged code, which filed **three P1s naming code that was never slow** — two of them already flagged RESURRECTED against rows settled for the same cause. **The tell was sitting in the file the whole time**: the comment directly above the list called `ceo.py --watch` "the commonest contender" while the code matched something else entirely. A claim in prose is not an encoded rule, and the gap between them is invisible precisely because the prose reads as though the rule exists (`no.md` #60 — a comment is the most inherited artifact in a codebase). The fix is one entry, keyed on the WATCHER rather than on its victim, because what saturates this machine is that the watcher is sweeping at all, never which project it happens to be inside. **The mutation row then scored UNNOTICED, and the row was right to.** The test's fake matched `"ceo.py" in argv[-1]` — a substring the mutation `"ceo.py --a-pattern-no-process-can-match"` preserves — so the assertion was satisfied identically by the working and the broken list. Re-keyed on the exact pattern, it scores NOTICED (exit 1 on `assert False is True`) with a byte-identical restore. **The general rule, and it is `no.md` #76's rule aimed one level up: when you widen a guard after it missed something, ask what the widened patterns have IN COMMON — if every one of them names your own subject, the class you still cannot see is everything that isn't yours.** Verified against the live process twice, an hour apart: `pgrep` finds pid 863 and `tree_is_contended()` returns True where it returned False. Decided 2026-08-21 (CTS-42073527 / CTS-950D2C5E / CTS-97624E22 / CTS-DDBD30E3).

78. **A DETECTOR WHOSE PRESCRIBED FIX WOULD BREAK THE TEST IT ACCUSES IS WORSE THAN ONE THAT SAYS NOTHING — and closing its ticket teaches it nothing, so it re-files.** `audit_engine._HARDCODED_PATH_RE` matched the SHAPE `/Users/<anything>/` without asking whether the path names a real home directory. It filed P1 against `_reinstall_hint("/Users/dev/.local/bin/verdictui", self._BUILT)` — a **fictional** path handed to a **pure string function that does no I/O** — was closed with measured evidence, and **re-filed 20 minutes later** as a second ticket the store itself flagged RESURRECTED. **Both halves matter.** The rule was wrong: measured, `/Users/dev/`, `/Users/test/`, `/Users/you/` and `/Users/username/` all fire identically to a genuine baked-in home, so the detector could not distinguish a portability defect from a documented stand-in. And the remedy was worse than the finding: that line is the plain-user **control** for a sibling test asserting the hint refuses `install -m 755` over a read-only Homebrew Cellar path, so substituting `Path.home()` makes the control environment-dependent and "refuses the unsafe path" becomes satisfiable by an implementation that never offers a usable fix at all. **A detector that demands a change which breaks the test proving the behaviour costs its reader's attention on every FUTURE finding, not merely the wrong one** (`no.md` #72, arriving from the remedy side rather than the false-positive side). Fixed at the source — `(?P<user>...)` plus a four-name `_PLACEHOLDER_HOME_USERS` exemption — and **deliberately narrow**: `/Users/someone/` still fires, which the pre-existing positive control depends on, because exempting "any name that is not the current user" would silence every genuine hardcoded path on every machine but the one that wrote it, i.e. the failure the rule exists to catch, inverted. Verified in both directions on the branch tip: 56/56 pass, the real file that filed both tickets now yields **0** findings, and under `if m.group("user") or True` **two** tests fail — the new control and the pre-existing one — with a byte-identical restore. **The second half is the structural one and it was NOT fixed, deliberately:** closing a CIS/CTS row records a judgement in the *store*, while the detector re-reads only *source*, so a disposition cannot reach the scanner (lesson 1312). Fixing the RULE removes this instance entirely; a general suppression channel between every store and every detector is a much larger design that needs its own advisory and, more importantly, a **second** worked example to make it decidable. Filing it as a standing gap now would be backlog noise about a defect that no longer exists. **The general rule: before acting on a finding, ask what its prescribed fix would DO to the code — a remedy that damages a deliberate control is evidence the rule is wrong, not evidence the code is.** Decided 2026-08-21 (CTS-B116EFDE / CTS-29BDB149 / CTS-3BAD138D; pm-base `c5ce73a`). **AMENDED 2026-08-22 — the finding re-filed a FOURTH time, 1h38m AFTER the fix landed, and the stale-artifact explanation could not survive that timeline.** The cause was a SECOND CHECKOUT of the auditor: `~/Projects/.worktrees/pm-base-pr14/audit_engine.py`, on a divergent peer branch, lacks the exemption — and when the CEO auditor runs with cwd inside that worktree, `.` leads `sys.path` and the UNFIXED rule executes. Reproduced: the same file yields **0** findings from the shared-libs copy and exactly **1** from the worktree copy. **Fixing a rule fixes the COPY you edited, and nothing tells you how many copies exist** (`no.md` #1341 — a program reachable from two paths is two programs). **One correction worth keeping, because it nearly caused a wrong edit:** an early probe using `inspect.getsourcefile(pm_base)` reported the WORKTREE path, and I briefly concluded the worktree always wins — I was one step from editing a peer's branch. Re-measuring with THREE instruments in one process from a normal cwd (find_spec origin, `__file__`, getsourcefile) put all three on the fixed copy; the first reading was an artifact of the probe itself running inside the worktree. **A path-resolution question must be asked from the cwd the real caller uses, because the answer is a function of where you stand.** Ticketed rather than fixed (CTS-2C67ED00) — the stale copy is on another session's branch.

79. **A HELPER'S CALLER-SIDE TEST CANNOT SEE THE HELPER'S OWN CONTRACT, and the three properties it left unpinned all fail SILENTLY.** `refresh_macro_expansions` had exactly one test, and that test was aimed elsewhere **on purpose**: `test_the_swift_runner_restamps_macro_consuming_sources` asserts the RUNNER calls the helper, because the defect it guards was the runner not calling it at all — and its comment says so, correctly citing `no.md` #12 (a test that invoked the helper directly would pass against the unfixed harness and prove nothing). But that test creates **one** directory, so it holds identically whether the loop covers every entry in `MACRO_CONSUMING_TEST_DIRS` or stops after the first. Three properties were therefore unpinned, and each is invisible when broken: **(a) covers every declared dir** — a stale expansion in ANY consuming target makes a runtime witness execute the previous plugin build, so the harness prints UNNOTICED for a guard that works, the expensive direction that reads as a coverage gap and invites rewriting correct code; **(b) skips a missing dir without raising** — the harness runs in detached worktrees, and this needs a *paired positive control*, since "does not raise" is otherwise satisfied by a helper that returns immediately and stamps nothing; **(c) resolves against `REPO`, not the process cwd** — the sharpest one, because a cwd-resolving helper finds no directories in a worktree, skips its loop, and **returns normally, reporting success while doing nothing**, which IS the stale-expansion condition it exists to prevent. Test (c) points the cwd at a decoy tree holding an identically-named source and asserts BOTH halves — the REPO-rooted file stamped, the decoy untouched — because asserting only the first is satisfied by a helper that stamps everything everywhere. **The general rule: a test written to pin a CALL is not a test of the thing called, and when a helper's failure mode is "silently does nothing", every property it has needs a control that would notice the nothing.** Coverage asserted over the DECLARED tuple rather than a hard-coded pair, so a third directory extends the test instead of leaving it quietly behind (lesson 219/240). Decided 2026-08-21 (TODO.md P1; `c79836a`).

80. **A DEFECT CLASS THAT RECURS THREE TIMES DESERVES A DETECTOR, NOT A FOURTH LESSON — and building one caught two errors in my own statement of the rule that no amount of re-reading would have.** `no.md` #76 and #77 both recorded the same shape (a guard whose pattern list is blind to everything outside itself), lesson 1360 and AP-3293 recorded it again, and none of that could stop the next occurrence, because **prose cannot execute**. The missing artifact was a check the fleet runs at write time, now shipped as `_detect_self_naming_guard_list` in `~/.claude/hooks/lib/ssot_detect.py` (claude-config `743062d`). **What makes this entry worth writing is that BOTH of my formulations were wrong, and only a measurement said so.** (a) I wrote the rule as *"every entry names this PROJECT"*, which reads exactly right and reports **QUIET** on the real pre-fix list — because `check.py --all` is the FLEET runner and contains no project name. The property that actually mattered was never "names this project" but **"names something WE run"**, leaving anything a third party runs invisible. (b) Widening to *"any bare `.py`"* then made the detector **fire on `ceo.py --watch`, which is the prescribed fix** — the precise failure `no.md` #78 / lesson 1361 forbids, caught here by the control asserting the remedy must silence the rule. The final discriminator is narrow and stated: a process that RUNS THE PROJECT (`ceo`, `launchd`, `cron`) silences it; a sweep the project runs over the fleet (`check.py --all`) does not, and putting that token in the supervisor list silenced the very defect — measured, then reverted. **Three method notes.** The detector is driven **end-to-end through `main()`**, not only through the private function, because an unwired detector is a library (`no.md` #34). It is wired **last**, since it fires at two entries where its sibling needs four, and running it first would pre-empt the broader finding. And it was proven falsifiable — `or True` on the predicate turns the suite red, restored byte-identical — with the three sibling detectors re-checked, which a shared-module change requires and a single-detector test cannot provide. **One incident worth not repeating: I broke the live module.** Writing the function through a shell heredoc expanded `\t` and `\n` inside the regex, producing a `SyntaxError` in a file every PostToolUse edit imports — and a file that does not parse suppresses every detector in it (lesson 1307), so the blast radius was fleet-wide and silent. Restored from git in one command, verified by import AND by re-firing a sibling. **The rule: author code containing regex escapes with a file-writing tool, never a shell heredoc — and after touching any shared detector module, prove it still IMPORTS before proving anything else.** **A COROLLARY the closeout found, which is about the SCRATCHPAD rather than the subject:** the two fragments written to compose that function were, by construction, INCOMPLETE code — they referenced `re`, `pathlib` and three helpers that live in the target module, not in the fragment. Once spliced they were spent, but they sat in the scratchpad, and a static checker grading a FRAGMENT AS A MODULE reported 22 F821/reportUndefinedVariable errors against code that had already shipped clean (the real module: ruff clean, pyright 0). **A staging fragment and a broken file are indistinguishable to any checker that reads files rather than intentions** — so the fragment must be DELETED at the moment its content lands, not left for a closeout gate to find. The gate was right to fire; nothing was wrong with the code, and something was wrong with the process. Verified before deleting (ADV-242): untracked by every repo, named by no LaunchAgent/cron/doc, not discovery-named, and bare code with no shebang — spent, not reusable. Decided 2026-08-21.

82. **A WRONG INSTRUMENT THAT AGREES WITH YOUR HYPOTHESIS IS THE EXPENSIVE KIND, BECAUSE AGREEMENT ENDS THE INVESTIGATION — and the defect actually worth fixing was sitting beside the one being chased.** A PM run reported one failure in 871: the `LieCatchTests` honesty gate, `lie-invisible-control` caught but citing `[]` rather than `hidden-submit`. Reading the source produced a clean hypothesis — `LieCatchTests` builds `WitnessHostProcess(lifetime: 20)` while `readTree` defaults to `readyTimeout: 20`, two deadlines of the same length started at the same moment, so under load the reader could be handed a host about to self-terminate and a truncated read would produce fewer visibility-gap findings. The shipping CLI path uses the default `lifetime: 30` against the same 20, a 10-second margin; only the tests collapse it to zero. **A probe then measured hosts dying at 3.51 s, 4.12 s and 9.64 s against a requested 20 — apparent confirmation.** It was the instrument: liveness was tested with `NSRunningApplication(processIdentifier:) == nil`, which stops resolving when an app **deregisters from LaunchServices**, which is not process death. A shell control tracking the real pid with `kill -0` showed the same host alive at **21.70 s**, and a direct (non-bundle) launch showed a clean 20.41 s. Switching to `kill(p, 0)` alone gave the true table: **window readable at ~1.1 s, read budget ~19.8 s**, at load 66-83 on 16 cores. The hypothesis was dead. **Two more followed it**: AX never publishes the `.accessibilityHidden(true)` button (4/4 byte-identical dumps, so the fixture still states its lie), and the hosting anchor never falls back to the window (12/12 resolved via `AXHostingView` at load 89, so positional paths never shift). The assertion itself did not reproduce — **15/15 isolated runs at 4.4-5.2 s against the failing run's 25.8 s**, then a full **872/0 exit 0** suite with zero skips. **What the investigation DID find is a real defect nobody was looking for**: `makeBundle` creates a UUID-named ROOT directory and nests the `.app` inside it, while the cleanup removed only the BUNDLE — so the parent survived every launch, ~23 per suite run. **88 had accumulated, all empty, while `launchservicesd` burned 208-278 % CPU** tracking app-bundle paths that no longer existed. Twenty-two witness tests covered readability, visibility, foreground policy and error mapping, and **not one observed what SURVIVED a launch**, so a resource leak was invisible while every signal stayed green. **Three rules.** (a) **When a probe confirms your theory, verify the probe before celebrating** — a disconfirming instrument gets scrutinised by reflex and a confirming one does not, which is precisely backwards (`no.md` #14/#71). (b) **A liveness check must ask the kernel, not a registry**: `kill -0` answers whether a process exists, while any framework lookup answers whether it is still *registered*, and the two diverge exactly when a process is shutting down. (c) **A suite can be complete about every property it models and blind to what its own execution leaves behind** — the leak was not an untested branch, it was an unasked question, and the cheapest way to find that class is to look at the machine while the suite runs rather than at the suite. Decided 2026-08-25 (CIS-2C757660, CTS-53F13D1A; fix in `9332ef7`).

83. **A TICKET THAT PRESCRIBES ITS OWN FIX IS TWO CLAIMS — the DEFECT and the REMEDY — and here the defect was right while both halves of the remedy were wrong, each in the expensive direction.** CTS-72228C9F diagnosed `ThirdPartyAuditTests` precisely: a 30 s wall-clock budget asserted unconditionally on a CROSS-PROCESS read of a third-party app, which failed at 47.66 s under load 97+ and "passed" in 0.078 s when it had actually SKIPPED. That diagnosis is correct and the fix keeps its first instruction (do NOT raise 30 s — the number was measured, the LANE was missing). Its other two instructions were followed literally and both had to be discarded on measurement. **(a) THE PRESCRIBED LANE COULD NOT HAVE CAUGHT THE FAILURE IT WAS PRESCRIBED FOR.** The ticket said to give the test "the lane the sibling gates already have" — `ConstrainedTimingEnvironment`, which six budget lanes read. Measured before building on it: the failing host had **ZERO markers set** (`CI`/`CODEX_CI`/`CODEX_SANDBOX`/`VERDICTUI_RECORD_TIMING_ONLY` all absent), so `isActive` was **false** and the marker lane would have sailed straight past a 47.66 s read. The failing host was a developer machine under FLEET CONTENTION, which is a different class from a shared CI runner — one the PM already modelled in Python (`SEVERE_OVERSUBSCRIPTION = 2.0` over `load1/cpus`) while Swift had no load reader at all. So the lane became a THIRD predicate, `cannotHoldAbsoluteWallClockBudget`, deliberately not a wider `isActive`: that one answers a durable property of the HOST, this one a property of the MOMENT, and folding them would make six unrelated gates flip between runs — the same argument the type already carries for `canEvaluateElapsedInvariants`, arriving a second time. **(b) THE PRESCRIBED FLOOR SHAPE FAILS A WORKING READER, and it took the mutation to see it.** The ticket proposed asserting "a plausible minimum" RUNTIME so a skip could not render as a pass (`no.md` #62), and the file itself documents a "~2 s healthy read" from 2026-08-12, so `elapsed > 0.1 s` looked conservative. Measured: **five consecutive genuine Finder reads at 0.043 / 0.052 / 0.059 / 0.079 / 0.090 s**, every one returning a real tree that satisfied every bound — and one run of the SAME code at **3.376 s** under load, a **~78x spread**. The read's cost is set by the target app's CURRENT WINDOW STATE (an idle Finder publishes a small tree) and by the machine, so a duration floor blames the code for the app being idle — the exact environment-blaming shape the ticket existed to remove, inverted. Replaced with a NODE-COUNT floor, which the mutation then made vivid by failing on a real **58-node tree read in 0.0999 s**: a genuine read the duration floor would have rejected as "the tree was not actually read". Node count is load-independent, matches the sibling test's own bar, and makes the two `<=` bound assertions non-vacuous (both hold for a one-node tree). The stale "~2 s" claim is corrected in place rather than left to be inherited (`no.md` #60). **The general rule: a ticket's REMEDY is a claim by whoever filed it, written at diagnosis time from the evidence then available, and it ages faster than the diagnosis does — so re-measure the remedy's premises before implementing them, especially when the ticket is detailed enough to feel like a spec.** Lesson 334 says a ticket's stated CAUSE is the most-inherited artifact in a backlog; this is its sibling, and it is more seductive, because a fix that a careful author already worked out invites implementation rather than verification. Both errors here were caught by exactly one habit — measuring the thing the sentence asserts before writing code on top of it (`no.md` #24). Decided 2026-08-25 (CTS-72228C9F, commit `a18d281`).

83. **THE SKIP MESSAGE NAMED THE HOSTING GROUP AND THE FACT WAS THAT THERE IS NO WINDOW — five sessions acted on the wrong noun, and the guard that produced it was one unchecked `role` away.** `CIS-2C757660`'s classification half stalled across sessions on 2026-08-28, 08-29 and twice on 08-30, each recording a different blocker — first "needs an exclusive tree at load < 4 on 16 cores", then, correctly, "not load: it SKIPS, and the precondition is WINDOW-SERVER SESSION STATE". Both were reading the same string: *"the host window published no geometry for its hosting group, so node coordinates cannot be converted to root space."* **That sentence is true about the wrong subject.** `AXReader.anchoredWindow` took `windows.first` on the strength of the AXError alone; measured 2026-08-31 on a live host launched exactly as the suite launches it (LSUIElement `.app` via `/usr/bin/open`), `kAXWindowsAttribute` answers **`.success` with a one-element list whose member is the APPLICATION element** — `role=AXApplication`, and `AXPosition`, `AXSize` and `AXFrame` each `kAXErrorAttributeUnsupported (-25205)`. So the app element flowed on as a window, its geometry read failed, and the caller was handed a sentence about hosting groups. **POSITIVE CONTROL, same probe, same minute:** Finder's app element answers all three with err=0 and its first window is an `AXScrollArea` at `(0,0,1728,1117)` — so the reader reads geometry where geometry exists, and the zero is absence rather than a broken instrument (lesson 242). Without that control "no geometry" and "no reader" are the same reading. **THE CLASSIFICATION ALSO DECIDED RETRY, which is why this cost more than a confusing message.** `WitnessHostProcess.waitForReady` treats `.noWindow` as *still registering* and keeps waiting, and treats anything else as terminal — so a host mid-AX-registration was abandoned on its first read, and the suite's own environment-skip `catch` was keyed on `.anchorUnreadable`, the one spelling the state was NOT arriving in. **FIXED at the property, not the symptom:** one `firstWindow(of:)` now owns the only `kAXWindowsAttribute` read — the guard was duplicated verbatim at THREE sites and a defect with N call sites cannot be closed at one of them (lesson 400) — and it rejects the application element. **The predicate is deliberately not `role == kAXWindowRole`**: Finder's first window is an `AXScrollArea`, so strict equality would make real windows unreadable, which is the failure the negative control exists to catch. Both directions are mutation-verified with byte-identical restore: `return true` reddens the app-element test, `return false` reddens the ordinary-roles test. **The suite's skip classifier now keys on the STATE across both spellings**, in ONE shared helper rather than the three copies that let it drift. **Immediate effect, and it is the point:** before, 6 tests skipped citing hosting-group geometry; after, the same 6 skip citing *"the host publishes no accessibility-visible window — the windows attribute succeeded but held the application element rather than a window"*, and the intermediate state where three `WitnessIntegrationTests` FAILED is what proved the change reached them. **A last honesty fix on the way:** `.noWindow(axError: 0)` rendered as *"AXError 0"* — code 0 is `.success`, so it sent readers to look up an error that does not exist. It now says the call succeeded and held no window, with a negative control asserting a real AXError still carries its code. **The rule: a diagnostic is acted on as a cause, so it must name the SUBJECT that failed, not the step that noticed.** The underlying condition — this login session's window server not publishing windows for newly-launched GUI apps — is still environment and still needs a fresh login; what changed is that the next reader is told the truth about it. Decided 2026-08-31 (CTS-9E32C9AB / CTS-42A1E721 / CTS-BE91C1C4).

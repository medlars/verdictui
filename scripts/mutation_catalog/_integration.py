"""Real-product transport and project coverage guard witnesses."""

from mutation_catalog_types import Mutation, Runner

MUTATIONS: list[Mutation] = [
    Mutation(
        name="reload identity admits unsupported platform",
        path="examples/ConsumerApp/verify-integration.py",
        old='check(sys.platform == "darwin",',
        new="check(True,",
        test="Tests/test_consumer_reload_identity.py::test_native_api_rejects_other_platforms",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity treats denied metadata as absent",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(ctypes.get_errno() == errno.ESRCH,",
        new="check(True,",
        test="Tests/test_consumer_reload_identity.py::test_native_identity_fails_closed_on_unavailable_metadata[denied]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity accepts short metadata",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(measured == size,",
        new="check(True,",
        test="Tests/test_consumer_reload_identity.py::test_native_identity_fails_closed_on_unavailable_metadata[short]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity accepts another pid metadata",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(info.pid == pid,",
        new="check(True,",
        test="Tests/test_consumer_reload_identity.py::test_native_identity_fails_closed_on_unavailable_metadata[wrong-pid]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity accepts missing executable",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(library.proc_pidpath(pid, path, len(path)) > 0,",
        new="check(library.proc_pidpath(pid, path, len(path)) >= 0,",
        test="Tests/test_consumer_reload_identity.py::test_native_identity_fails_closed_on_unavailable_metadata[no-path]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity rejects an already absent process",
        path="examples/ConsumerApp/verify-integration.py",
        old="if measured == 0:",
        new="if measured == -1:",
        test="Tests/test_consumer_reload_identity.py::test_native_identity_fails_closed_on_unavailable_metadata[absent]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity treats zombie as live",
        path="examples/ConsumerApp/verify-integration.py",
        old="measured == size and info.status == 5",
        new="measured == size and info.status == 9",
        test="Tests/test_consumer_reload_identity.py::test_native_identity_fails_closed_on_unavailable_metadata[zombie]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity admits failed inventory size",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(0 <= size <= 2 * 1024 * 1024,",
        new="check(size <= 2 * 1024 * 1024,",
        test="Tests/test_consumer_reload_identity.py::test_native_inventory_fails_closed[denied]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity admits unbounded inventory size",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(0 <= size <= 2 * 1024 * 1024,",
        new="check(0 <= size,",
        test="Tests/test_consumer_reload_identity.py::test_native_inventory_fails_closed[oversized]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity admits failed inventory read",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(0 <= measured < capacity,",
        new="check(measured < capacity,",
        test="Tests/test_consumer_reload_identity.py::test_native_inventory_fails_closed[read-error]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity admits overflowing inventory",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(0 <= measured < capacity,",
        new="check(0 <= measured,",
        test="Tests/test_consumer_reload_identity.py::test_native_inventory_fails_closed[overflow]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity admits disappearing child",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(identity is not None,",
        new="check(True,",
        test="Tests/test_consumer_reload_identity.py::test_native_inventory_fails_closed[disappeared]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores fixture entry point",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(source.count(entry) == 1,",
        new="check(True,",
        test="Tests/test_consumer_reload_identity.py::test_crash_capability_is_private_and_one_use",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores crash exit deadline",
        path="examples/ConsumerApp/verify-integration.py",
        old='check(time.monotonic() < deadline, "fixture consumer did not exit")',
        new="return",
        test="Tests/test_consumer_reload_identity.py::test_exit_wait_does_not_wait_for_recycled_process",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity admits extra broker children",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(len(children) == 2,",
        new="check(len(children) >= 2,",
        test="Tests/test_consumer_reload_identity.py::test_unexpected_children_fail_closed[extra]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores direct parent",
        path="examples/ConsumerApp/verify-integration.py",
        old="all(child.parent == owner.pid for child in children)",
        new="True",
        test="Tests/test_consumer_reload_identity.py::test_unexpected_children_fail_closed[wrong-parent]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores guardian executable",
        path="examples/ConsumerApp/verify-integration.py",
        old="child.executable == owner.executable and child.group == child.pid",
        new="child.group == child.pid",
        test="Tests/test_consumer_reload_identity.py::test_unexpected_children_fail_closed[guardian-executable]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores guardian group",
        path="examples/ConsumerApp/verify-integration.py",
        old="child.executable == owner.executable and child.group == child.pid",
        new="child.executable == owner.executable",
        test="Tests/test_consumer_reload_identity.py::test_unexpected_children_fail_closed[guardian-group]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores consumer executable",
        path="examples/ConsumerApp/verify-integration.py",
        old="and child.executable == str(expected.resolve())",
        new="and True",
        test="Tests/test_consumer_reload_identity.py::test_unexpected_children_fail_closed[wrong-executable]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores consumer group",
        path="examples/ConsumerApp/verify-integration.py",
        old="and child.group == guardian.pid",
        new="and True",
        test="Tests/test_consumer_reload_identity.py::test_unexpected_children_fail_closed[wrong-group]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores broker birth",
        path="examples/ConsumerApp/verify-integration.py",
        old="process_identity(broker.pid) == owner",
        new="True",
        test="Tests/test_consumer_reload_identity.py::test_recycled_or_reparented_process_is_not_selected[owner-birth]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores consumer birth",
        path="examples/ConsumerApp/verify-integration.py",
        old="process_identity(consumer.pid) == consumer,",
        new="True,",
        test="Tests/test_consumer_reload_identity.py::test_recycled_or_reparented_process_is_not_selected[consumer-birth]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity ignores exited broker",
        path="examples/ConsumerApp/verify-integration.py",
        old="check(broker.poll() is None,",
        new="check(True,",
        test="Tests/test_consumer_reload_identity.py::test_recycled_or_reparented_process_is_not_selected[exit]",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="reload identity waits for recycled consumer pid",
        path="examples/ConsumerApp/verify-integration.py",
        old="while process_identity(consumer.pid) == consumer:",
        new="while process_identity(consumer.pid) is not None:",
        test="Tests/test_consumer_reload_identity.py::test_exit_wait_does_not_wait_for_recycled_process",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="CI web fixture loses its configured Python interpreter",
        path=".github/workflows/ci.yml",
        old="VERDICTUI_TEST_PYTHON: ${{ steps.fixture_python.outputs.python-path }}",
        new="VERDICTUI_TEST_PYTHON: /usr/bin/python3",
        test="scripts/test_ci_evidence_identity.py::TestWiring::test_fixture_python_is_configured_before_swift_tests",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="live requests allow non-finite or unbounded observation deadlines",
        path="Sources/VerdictUICLICore/LiveRuntime.swift",
        old="guard timeout.isFinite, timeout > 0, timeout <= 60 else {",
        new="guard true else {",
        test="LiveRuntimeTests/testInvalidTargetsAndEmptyExpectationsAreRejected",
    ),
    Mutation(
        name="live requests accept empty outcome assertions",
        path="Sources/VerdictUICLICore/LiveRuntime.swift",
        old="if let expectText, expectText.isEmpty {",
        new="if let expectText, expectText.isEmpty && false {",
        test="LiveRuntimeTests/testInvalidTargetsAndEmptyExpectationsAreRejected",
    ),
    Mutation(
        name="live MCP truncates fractional process identifiers",
        path="Sources/VerdictUICLICore/ExtendedMCP.swift",
        old="let exact = Int32(exactly: number)",
        new="let exact = Int32(exactly: number.rounded(.towardZero))",
        test="LiveRuntimeTests/testMCPRejectsLossyAndWrongTypeArguments",
    ),
    Mutation(
        name="live outcomes pass without observing expected state",
        path="Sources/VerdictUICLICore/LiveRuntime.swift",
        old="if let expected, !containsText(tree, expected) {",
        new="if let expected, !containsText(tree, expected) && false {",
        test="LiveRuntimeTests/testPostedInputDoesNotSatisfyUnobservedOutcome",
    ),
    Mutation(
        name="live denied input is treated as successfully posted",
        path="Sources/VerdictUICLICore/LiveRuntime.swift",
        old="do { try perform() } catch { throw Failure.unavailable }",
        new="do { try perform() } catch { /* deliberately swallowed */ }",
        test="LiveRuntimeTests/testDeniedInputProducesNoVerdictAndDoesNotLeakValue",
    ),
    Mutation(
        name="web actions accept missing node targets",
        path="Sources/VerdictUICLICore/WebRuntime.swift",
        old="guard let node = request.node, !node.isEmpty else {",
        new='guard let node = Optional(request.node ?? "") else {',
        test="WebRuntimeTests/testCredentialAndActionBoundaries",
    ),
    Mutation(
        name="web credential actions accept literal secret text",
        path="Sources/VerdictUICLICore/WebRuntime.swift",
        old="!reference.isEmpty, request.text == nil else {",
        new="!reference.isEmpty else {",
        test="WebRuntimeTests/testCredentialAndActionBoundaries",
    ),
    Mutation(
        name="web type actions silently ignore credential references",
        path="Sources/VerdictUICLICore/WebRuntime.swift",
        old="guard let text = request.text, request.credential == nil else {",
        new="guard let text = request.text else {",
        test="WebRuntimeTests/testCredentialAndActionBoundaries",
    ),
    Mutation(
        name="project checks accept empty and duplicated declarations",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="guard !manifest.checks.isEmpty, manifest.checks.count <= 100,\n              Set(manifest.checks.map(\\.name)).count == manifest.checks.count else {",
        new="guard true else {",
        test="ProjectChecksTests/testEmptyDuplicateAndUnknownDeclarationsAreRejected",
    ),
    Mutation(
        name="project checks certify a different consumer scenario",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="guard verdict.scenario == scenario, (verdict.status == .pass) == (result.code == 0) else {",
        new="guard (verdict.status == .pass) == (result.code == 0) else {",
        test="ProjectChecksTests/testConsumerVerdictPreservesFailureAndISODate",
    ),
    Mutation(
        name="bounded check subprocess accepts oversized completed output",
        path="Sources/VerdictUICLICore/BoundedCommand.swift",
        old="guard data.count <= limit else { throw Failure.excessiveOutput }",
        new="_ = data.count <= limit",
        test="ProjectChecksTests/testSubprocessBoundsOutputAndTime",
    ),
    Mutation(
        name="owned command cleanup signals only leader leaving descendants",
        path="Sources/VerdictUIWeb/OwnedCommandProcess.swift",
        old="guard kill(-processIdentifier, number) != 0 else { return }",
        new="guard kill(processIdentifier, number) != 0 else { return }",
        test="ProjectChecksTests/testCancellationStopsOwnedRunnerAndDescendant",
    ),
    Mutation(
        name="owned command observation reaps its identity anchor",
        path="Sources/VerdictUIWeb/OwnedCommandProcess.swift",
        old="WEXITED | WNOHANG | WNOWAIT",
        new="WEXITED | WNOHANG",
        test="ProjectChecksTests/testExitedLeaderRemainsPinnedUntilDescendantCleanup",
    ),
    Mutation(
        name="owned command mistakes exited Darwin group for live permission denial",
        path="Sources/VerdictUIWeb/OwnedCommandProcess.swift",
        old="if failure == EPERM && groupHasOnlyExitedMembers() { return }",
        new="if failure == ESRCH && groupHasOnlyExitedMembers() { return }",
        test="ProjectChecksTests/testSubprocessBoundsOutputAndTime",
    ),
    Mutation(
        name="signal shutdown exits before cancelling owned check workers",
        path="Sources/VerdictUICLICore/RuntimeShutdown.swift",
        old="await beforeExit()",
        new="_ = beforeExit",
        test="ProjectChecksTests/testCheckSIGTERMCleansChildAndGrandchildBeforeExit",
    ),
    Mutation(
        name="project check cancellation reports configuration failure",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="error: Task.isCancelled || error is CancellationError",
        new="error: !(Task.isCancelled || error is CancellationError)",
        test="ProjectChecksTests/testCancelledProjectCheckReportsCancellationAndCleansRunner",
    ),
    Mutation(
        name="project checks silently ignore incompatible fields",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="Set(declaration.keys).isSubset(of: fields)",
        new="!fields.isEmpty || declaration.isEmpty",
        test="ProjectChecksTests/testCheckKindsRejectIgnoredFieldsAndMissingTargets",
    ),
    Mutation(
        name="project checks allow absent scenario target",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="guard validText(check.scenario) else",
        new="guard validText(check.name) else",
        test="ProjectChecksTests/testCheckKindsRejectIgnoredFieldsAndMissingTargets",
    ),
    Mutation(
        name="project checks allow absent appkit subject",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="guard validText(check.runner), validText(check.subject) else",
        new="guard validText(check.runner) else",
        test="ProjectChecksTests/testCheckKindsRejectIgnoredFieldsAndMissingTargets",
    ),
    Mutation(
        name="project checks accept browser script URLs",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old='["http", "https", "file"].contains(url.scheme?.lowercased() ?? "")',
        new="url.scheme != nil",
        test="ProjectChecksTests/testCheckKindsRejectIgnoredFieldsAndMissingTargets",
    ),
    Mutation(
        name="project checks accept invalid native process identifiers",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="guard let pid = check.pid, pid > 1 else",
        new="guard let pid = check.pid, pid >= 0 else",
        test="ProjectChecksTests/testCheckKindsRejectIgnoredFieldsAndMissingTargets",
    ),
    Mutation(
        name="project checks permit oversized expected text",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="value.utf8.count <= maximum",
        new="value.utf8.count <= maximum * 2",
        test="ProjectChecksTests/testCheckKindsRejectIgnoredFieldsAndMissingTargets",
    ),
    Mutation(
        name="project checks accept whitespace outcome assertions",
        path="Sources/VerdictUICLICore/ProjectChecks.swift",
        old="!value.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty",
        new="!value.isEmpty",
        test="ProjectChecksTests/testCheckKindsRejectIgnoredFieldsAndMissingTargets",
    ),
    Mutation(
        name="bounded commands accept a zero deadline",
        path="Sources/VerdictUICLICore/BoundedCommand.swift",
        old="timeout.isFinite, timeout > 0, limit > 0",
        new="timeout.isFinite, timeout >= 0, limit > 0",
        test="ProjectChecksTests/testSubprocessRejectsInvalidLimitsAndNulArguments",
    ),
    Mutation(
        name="bounded commands accept a zero output limit",
        path="Sources/VerdictUICLICore/BoundedCommand.swift",
        old="timeout.isFinite, timeout > 0, limit > 0",
        new="timeout.isFinite, timeout > 0, limit >= 0",
        test="ProjectChecksTests/testSubprocessRejectsInvalidLimitsAndNulArguments",
    ),
    Mutation(
        name="owned command arguments silently truncate at NUL",
        path="Sources/VerdictUIWeb/OwnedCommandProcess.swift",
        old='!(argv + env + [directory.path]).contains(where: { $0.contains("\\0") })',
        new='!directory.path.contains("\\0")',
        test="ProjectChecksTests/testSubprocessRejectsInvalidLimitsAndNulArguments",
    ),
    Mutation(
        name="artifact secret audit overlooks leaked credential values",
        path="scripts/product-smoke.py",
        old="if any(secret in candidate for secret in self.values):",
        new="if False and any(secret in candidate for secret in self.values):",
        test="Tests/test_product_smoke.py::ProductSmokeAssertions::test_credential_leaks_in_wire_nested_json_and_argv_are_rejected_without_echo",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="artifact secret audit passes without measured output and arguments",
        path="scripts/product-smoke.py",
        old="required <= self.channels and all(self.argv_counts.values()),",
        new="True,",
        test="Tests/test_product_smoke.py::ProductSmokeAssertions::test_missing_secret_audit_channels_or_process_samples_cannot_pass",
        runner=Runner.PYTEST,
    ),
    Mutation(
        name="artifact secret audit ignores MCP shutdown stderr",
        path="scripts/product-smoke.py",
        old='self.audit.scan("MCP stderr", self.stderr.read())',
        new='self.audit.scan("MCP stderr", "")',
        test="Tests/test_product_smoke.py::ProductSmokeAssertions::test_mcp_shutdown_output_cannot_escape_scan",
        runner=Runner.PYTEST,
    ),
]

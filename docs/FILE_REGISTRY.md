# File Registry — VerdictUI

> Single source of truth for all source files. Update when adding, moving, or archiving files.
> Last updated: 2026-08-04

## Source Files

| File                                                                 | Purpose                                                                                                                                                | Status | Added      |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ---------- |
| `Package.swift`                                                      | SPM manifest — kernel, probe, demo-scenarios, demo executable targets, strict concurrency                                                              | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/SemanticNode.swift`                         | Semantic tree: `Role`, `AttributeValue`, `TextMetrics`, `SemanticNode`, `Rect`/`Size` geometry                                                         | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/TreeDiff.swift`                             | `NodePath`, `TreeDelta` and its four categories, `TreeDiff.compute`/`apply`                                                                            | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/RuleEngine.swift`                           | `LintRule` protocol, `LintContext` (viewport, minimums, suppression), `RuleEngine.run`                                                                 | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/Rules/DuplicateProbeIDRule.swift`           | Rule `duplicate-probe-id` — a probe id used twice breaks diffing and act-targeting                                                                     | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/Rules/ZeroSizeRule.swift`                   | Rule `zero-size` — visible node with an empty frame                                                                                                    | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/Rules/SiblingOverlapRule.swift`             | Rule `sibling-overlap` — intersecting siblings without declared layering                                                                               | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/Rules/ContentOverlapRule.swift`             | Rule `content-overlap` — leaf content colliding across different parents                                                                                | Active | 2026-08-06 |
| `Sources/VerdictUIKernel/Rules/OffscreenRule.swift`                  | Rule `offscreen` — visible node entirely outside the viewport                                                                                          | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/Rules/TruncationRule.swift`                 | Rule `truncation` — clipped lines or a one-line text denied its intrinsic width                                                                        | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/Rules/ExcessiveWrapRule.swift`              | Rule `excessive-wrap` — text wrapping far past intent, which clips nothing so `truncation` cannot see it                                                | Active | 2026-08-09 |
| `Sources/VerdictUIKernel/Rules/TapTargetRule.swift`                  | Rule `tap-target` — interactive node below the platform hit minimum                                                                                    | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/Rules/EmptyContainerRule.swift`             | Rule `empty-container` — a container reserving visible area whose children all fail to paint                                                            | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/Rules/MisalignmentRule.swift`               | Rule `misalignment` — the "2 px off" class: sibling edges that near-miss inside a 0.5–4 pt window                                                       | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/Rules/InconsistentSpacingRule.swift`        | Rule `inconsistent-spacing` — one gap in a single-axis stack that breaks the modal rhythm                                                               | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/Rules/ClippedContentRule.swift`             | Rule `clipped-content` — content extending past an ancestor's frame, clipped or painting over the layout                                                | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/Expectations.swift`                         | `Expectation`/`ExpectationSet` — declarative per-element assertions compiled to findings; a missing subject is itself an error                          | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/Baselines.swift`                            | `Baseline`/`BaselineCheck` — canonicalized recorded trees and the drift comparison, the regression channel                                              | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/Verdict.swift`                              | `Verdict` envelope (status/findings/tree/delta/timing) + `Finding`, custom `Codable`                                                                   | Active | 2026-08-04 |
| `Sources/VerdictUIKernel/SchemaVersion.swift`                        | Wire-format version and the major/minor compatibility contract                                                                                         | Active | 2026-08-04 |
| `Sources/VerdictUIProbe/VerdictProbe.swift`                          | `.verdictProbe(id:role:text:attributes:)` emitting `ProbeRecord`s in the `verdict-root` space, `.verdictRoot(into:)` + `VerdictTreeSink` tree delivery | Active | 2026-08-04 |
| `Sources/VerdictUIProbe/ProbeLayout.swift`                           | Byte-transparent `Layout` wrapper recording size negotiation + intrinsic/ideal measurements into `ProbeRecorder`; forwards explicit alignment guides   | Active | 2026-08-04 |
| `Sources/VerdictUIProbe/PixelCapture.swift`                           | Wave 9 pixel channel: `PixelCapture` + two documented-divergent backends (`cacheDisplay` at an explicit 1x rep, `imageRenderer`), FNV content hash, zero-area refusal                | Active | 2026-08-12 |
| `Sources/VerdictUIProbe/PixelDeterminism.swift`                      | Wave 9 double-render check: refuses a pixel baseline to any scenario that does not render identically twice, with actionable evidence naming the usual unpinned inputs                | Active | 2026-08-13 |
| `Sources/VerdictUIProbe/PixelCompare.swift`                          | Wave 9 platform half of the pixel compare: PNG->RGBA decode through a fixed sRGB context, heat-map encode, before/after/heat artifacts written only on failure and cited by path, cross-backend refusal, path-traversal-safe slugs                | Active | 2026-08-13 |
| `Sources/VerdictUIProbe/PixelCache.swift`                            | Wave 9 render cache: `PixelCacheKey` (scenario, tree hash, viewport, variant, backend, build id) content-addressed under ~/.verdictui/cache/pixels — invalidation by construction, every failure a MISS, PNG written before its sidecar so a crash leaves an orphan that misses                | Active | 2026-08-13 |
| `Sources/VerdictUIKernel/PixelDiff.swift`                             | Wave 9 structural pixel compare: `PixelRaster`/`PixelTolerance`/`PixelDiffResult`, per-channel tolerance, changed-region bounding box, severity-scaled heat map, and the dimension/backend refusals — platform-pure, no image format                | Active | 2026-08-13 |
| `Sources/VerdictUIProbe/TreeAssembly.swift`                          | Pure tree building: frame-containment nesting, layout-order siblings, honest `TextMetrics` attachment                                                  | Active | 2026-08-04 |
| `Sources/VerdictUIProbe/Scenario.swift`                              | `VerdictScenario` + `ScenarioState` (`ObservableObject` bindings, probe action registration, env key)                                                  | Active | 2026-08-05 |
| `Sources/VerdictUIProbe/StateMachine.swift`                          | `ScenarioStateMachine` (validated named states + transitions), `Harness.walk` driving paths with an arrival check per step, `WalkResult` evidence      | Active | 2026-08-11 |
| `Sources/VerdictUIProbe/LayoutSettle.swift`                          | The reusable settle primitive Wave 3 builds on — extracted from `OracleHost.swift` along a real seam, not a line count (`no.md` #45) | Active | 2026-08-12 |
| `Sources/VerdictUIProbe/OracleHost.swift`                            | Windowless `NSHostingView` harness: pinned deterministic environment, `LayoutSettle` pump primitive, size clamp with `wasClamped`, `currentTree()`     | Active | 2026-08-04 |
| `Sources/VerdictUIProbe/VerdictClock.swift`                          | Controllable `Clock`, `SettlePolicy`, `AnimationControl` (Transaction / CATransaction flush), `\.verdictClock` environment key                           | Active | 2026-08-05 |
| `Sources/VerdictUIProbe/Settle.swift`                                | Quiescence detector: composed quiet signals on `LayoutSettle`, `SettleResult`, timeout FAIL verdict helper                                               | Active | 2026-08-05 |
| `Sources/VerdictUIProbe/Harness.swift`                               | Atomic act-and-observe: `perform` (tree→act→settle→tree→diff→lint), `run` flow batching, `StepResult`/`FlowResult`                                       | Active | 2026-08-06 |
| `Sources/VerdictUIProbe/Actions.swift`                               | `ProbeAction` / `ProbeSiteAction` / `ProbeActionError` — in-process action injection                                                                    | Active | 2026-08-05 |
| `pm-baselines.json`                                                  | Tracked PM metric floor (ADR 2026-002) — seed + refreshed counts/health score                                                                          | Active | 2026-08-05 |
| `Sources/VerdictUIDemoScenarios/LieScenarios.swift`                 | Wave 8 SD1: three scenarios whose PROBES misreport (text / role / AX-visibility) plus the honest control — the reconciler's honesty proof, deliberately NOT in `DemoScenarios.all` | Active | 2026-08-12 |
| `Sources/VerdictUIDemoScenarios/DemoScenarios.swift`                 | `DemoScenarioEntry` catalog — type-erased host construction, stable names, recommended viewports                                                       | Active | 2026-08-04 |
| `Sources/VerdictUIDemoScenarios/DemoReport.swift`                    | Testable core of the demo executable — verdicts + JSON rendering for the whole catalog                                                                 | Active | 2026-08-04 |
| `Sources/VerdictUIDemoScenarios/TruncatingLabelScenario.swift`       | Planted defect: one-line label denied its width → `truncation`                                                                                         | Active | 2026-08-04 |
| `Sources/VerdictUIDemoScenarios/OverlappingBadgesScenario.swift`     | Planted defect: intersecting sibling badges, no layering → `sibling-overlap`                                                                           | Active | 2026-08-04 |
| `Sources/VerdictUIDemoScenarios/OffscreenButtonScenario.swift`       | Planted defect: button offset past the viewport → `offscreen`                                                                                          | Active | 2026-08-04 |
| `Sources/VerdictUIDemoScenarios/UndersizedTapTargetScenario.swift`   | Planted defect: 18×18 button below the 28×28 macOS minimum → `tap-target`                                                                              | Active | 2026-08-04 |
| `Sources/VerdictUIDemoScenarios/ToggleLayoutScenario.swift`          | Toggle-driven layout change, clean in both states — Wave 3 action-injection fixture                                                                    | Active | 2026-08-04 |
| `Sources/VerdictUIDemoScenarios/CleanSettingsScenario.swift`         | False-positive guard: non-trivial clean layout incl. a declared-layering ZStack overlap                                                                | Active | 2026-08-04 |
| `Sources/VerdictUIDemo/main.swift`                                   | Thin executable: prints one verdict JSON per demo scenario                                                                                             | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/ReconcileTests.swift`                    | Cross-validation comparator: each disagreement paired with an agreement control so no assertion is satisfied by a reconciler that always fires  | Active | 2026-08-12 |
| `Tests/VerdictUIWitnessTests/AXPressTests.swift`                     | AXPress against third-party apps by name: unreachable-pid and absent-name refusals, plus a control asserting the search discriminates rather than clicking real controls                                      | Active | 2026-08-17 |
| `Tests/VerdictUIWitnessTests/AXReaderTests.swift`                    | Role mapping and coordinate conversion, asserted against the roles a live window actually published                                                | Active | 2026-08-12 |
| `Tests/VerdictUIWitnessTests/WitnessIntegrationTests.swift`          | Drives the REAL host binary end-to-end; skips loudly where no window server exists rather than failing for the environment                          | Active | 2026-08-12 |
| `Tests/VerdictUIWitnessTests/ThirdPartyAuditTests.swift`            | Drives AXReader at a LIVE third-party app — the Wave 8 gate's usefulness question; found the missing breadth bound (CIS-1E89B85D) | Active | 2026-08-12 |
| `Tests/VerdictUIWitnessTests/LieCatchTests.swift`                   | Wave 8 SD1 honesty gate: every planted lie caught, each citing the probe that lied, with a positive control that SKIPS when the window server cannot publish | Active | 2026-08-12 |
| `Tests/VerdictUIWitnessTests/HostLifetimeMarginTests.swift` | The host must outlive the reader's wait for it: host lifetime and `readyTimeout` are two deadlines from different instants, so equal values leave zero margin by construction (CIS-2C757660) | Active | 2026-08-30 |
| `Tests/VerdictUIProbeTests/LieScenarioMeasurement.swift`            | Proves each planted lie reaches the probe tree AND is invisible to the inner loop — the precondition that makes a witness catch meaningful | Active | 2026-08-12 |
| `Tests/VerdictUIWitnessTests/CrossValidationTests.swift`             | The SD6 permission path: every reader failure becomes a warning finding, and a WORKING reader must not emit one (the always-true control)           | Active | 2026-08-12 |
| `Tests/VerdictUIKernelTests/SemanticNodeTests.swift`                 | Roles, attributes, structural paths, node Codable                                                                                                      | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/TreeDiffTests.swift`                     | Diff categories, path encoding, apply/replay                                                                                                           | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/TreeDiffPropertyTests.swift`             | Property test: random mutations must replay exactly                                                                                                    | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/RuleEngineTests.swift`                   | Engine determinism, suppression, severity overrides, timing                                                                                            | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/RulesTests.swift`                        | Per-rule edge cases for the Wave 1 rules                                                                                                                | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/ContentOverlapRuleTests.swift`           | `content-overlap` — the cross-parent defect, plus every relationship that must stay silent                                                              | Active | 2026-08-06 |
| `Tests/VerdictUIKernelTests/EmptyContainerRuleTests.swift`           | `empty-container` — the nested blank region, and the childless container it deliberately declines to judge                                              | Active | 2026-08-11 |
| `Tests/VerdictUIKernelTests/MisalignmentRuleTests.swift`             | `misalignment` — both ends of the near-miss window, pinned so the rule cannot widen into reporting every layout                                         | Active | 2026-08-11 |
| `Tests/VerdictUIKernelTests/InconsistentSpacingRuleTests.swift`      | `inconsistent-spacing` — mode-not-mean rhythm detection, and silence when no rhythm exists                                                              | Active | 2026-08-11 |
| `Tests/VerdictUIKernelTests/ClippedContentRuleTests.swift`           | `clipped-content` — escapes measured against every ancestor, not just the immediate parent                                                              | Active | 2026-08-11 |
| `Tests/VerdictUIKernelTests/ExpectationsTests.swift`                 | `Expectation` — every predicate discriminated in both directions; a missing subject and a missing counterpart both fail rather than passing vacuously   | Active | 2026-08-11 |
| `Tests/VerdictUIKernelTests/BaselinesTests.swift`                    | `Baseline` — canonical form is jitter-proof and idempotent; incompatible schemas are named, not diffed; a removal cannot suppress itself                | Active | 2026-08-11 |
| `Sources/VerdictUIProbe/Sweep.swift`                                 | `Variant`/`Sweep`/`SweepReport` — cartesian variant matrix, one verdict per cell, unmeasured cells blocking a clean result                              | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/BaselineStore.swift`                        | Baseline storage + the SD4 destructive guard — `--accept` required to replace, superseded hash appended to the audit log, platform-pure SHA-256         | Active | 2026-08-11 |
| `Sources/VerdictUIKernel/Reconcile.swift`                            | Wave 8 cross-validation: compares the probe tree against the AX witness tree; role/frame/text disagreements and `ax-visibility-gap` findings                        | Active | 2026-08-12 |
| `Sources/VerdictUIWitness/AXReader.swift`                            | Reads an `AXUIElement` tree out-of-process and normalizes it into `SemanticNode` (role map, AXValue+AXDescription text, hosting-group-relative frames)             | Active | 2026-08-12 |
| `Sources/VerdictUIWitness/WitnessHost.swift`                         | The one component that runs WINDOWED — `NSApp.run()` is what registers it with the accessibility server                                                   | Active | 2026-08-12 |
| `Sources/VerdictUIWitness/WitnessHostProcess.swift`                  | Spawns the witness host through LaunchServices (a fork/exec child is invisible to AX) and reads its tree, plus `compositedAlphas` — what the WINDOW SERVER draws, read from outside about the host pid                                                         | Active | 2026-08-12 |
| `Sources/VerdictUIWitness/CrossValidation.swift`                     | SD6: joins the two channels and decides what a FAILED witness means — a warning finding, never a thrown error and never a silently weaker PASS                    | Active | 2026-08-12 |
| `Sources/VerdictUIAppKit/AppKitRenderer.swift`                       | Walks an `NSView`/`NSViewController` into a `SemanticNode` tree with an off-screen layout pass — no window, no screenshot, no running app                            | Active | 2026-08-17 |
| `Sources/VerdictUIAppKit/AppKitTreeRunner.swift`                     | The consumer-built runner's entry point (`list` / `render <subject>`); an `NSView` subclass exists only in the binary that compiled it, so the CLI drives this        | Active | 2026-08-17 |
| `Sources/AppKitRunnerExample/main.swift`                             | The runner `docs/appkit.md` documents, shipped so that snippet is compile-checked; its defect/clean pair is the falsifiability control `stage_appkit_example` asserts in both directions | Active | 2026-08-18 |
| `Sources/VerdictUICLICore/AppKitCommand.swift`                       | `verdictui appkit` — runs a consumer's runner executable and judges the tree it prints, keeping the three-valued exit contract intact                                 | Active | 2026-08-17 |
| `Tests/VerdictUIAppKitTests/AppKitRendererTests.swift`               | Renderer contract: role mapping, root-space frames, the y-axis flip, visibility, text metrics, and positive controls that FAIL on real defects                        | Active | 2026-08-17 |
| `Tests/VerdictUIAppKitTests/AppKitTreeRunnerTests.swift`             | The runner's argv contract and its 0/2 exit codes — a runner produces trees and does not judge, so there is deliberately no 1                                         | Active | 2026-08-17 |
| `Tests/VerdictUICLICoreTests/AppKitCommandTests.swift`               | CLI-layer contract for `appkit`, including the vacuity guard and the three outcomes asserted distinct in one run                                                      | Active | 2026-08-17 |
| `Tests/VerdictUICLICoreTests/AppKitCommandRunnerTests.swift`         | An independently-derived second suite for the same command; kept because two derivations agreeing is evidence and one is an assertion                                 | Active | 2026-08-17 |
| `Tests/VerdictUICLICoreTests/AppKitMCPToolTests.swift`               | `judge_appkit` over MCP: a failing verdict is `isError:false` (a verdict was produced), an unreachable runner is `isError:true`                                       | Active | 2026-08-17 |
| `Tests/test_diagnostic_probes.py`                                    | Holds the self-exec probes to their contract: refuse interpretation cleanly with exit 2 and name the working invocation, rather than crashing                         | Active | 2026-08-17 |
| `scripts/ax-window-publication-probe.swift`                          | Does this login session publish NEW GUI windows to the AX server? Answers it with no VerdictUI code in the path, so it cannot be confounded by what it diagnoses      | Active | 2026-08-17 |
| `scripts/ax-appkit-anchor-probe.swift`                               | Measures the window-vs-content-group offset before anyone "corrects" for a titlebar; on Finder that offset is 0, not the 32 pt the code comment assumes               | Active | 2026-08-17 |
| `Sources/verdictui-witness-host/main.swift`                          | The windowed host executable; renders a demo scenario's probed body for the external channel                                                                         | Active | 2026-08-12 |
| `Sources/VerdictUICLICore/VerdictEngine.swift`                       | What every CLI/daemon/MCP command actually does — render, verify, baseline, sweep; returns values, never prints or exits                                | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/Commands.swift`                            | Command objects + the exit-code mapping (engine failure is never exit 1) and the sweep variant vocabulary                                               | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/ReleaseVersion.swift`                      | The version of THIS BUILD, deliberately distinct from `SchemaVersion.current` — the wire schema is a stable 1.0 promise (ADR 2026-022) and cannot double as a release identifier | Active | 2026-08-28 |
| `Sources/VerdictUICLICore/CommandLineInterface.swift`                | argument-parser declarations, in the LIBRARY so tests can construct them; `@available` is required for async root dispatch                              | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/ProjectScenarios.swift`                    | Consumer-project scenario discovery: finds `.verdictui/config.json`, resolves declared runners from the project root, and treats malformed manifests as broken adoption rather than absent adoption | Active | 2026-08-16 |
| `Sources/VerdictUICLICore/VerdictOutput.swift`                       | The stdout/stderr contract, injectable `OutputSink`, and the three-valued `ExitCode`                                                                    | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/SweepWire.swift`                           | Serializable sweep report — declared here so SwiftUI framework enums never gain a Codable conformance this package would own                            | Active | 2026-08-11 |
| `Sources/verdictui/VerdictUIMain.swift`                              | The binary's `@main`. NOT named main.swift — that filename forbids `@main` and silently selects the sync overload (no.md #32)                           | Active | 2026-08-11 |
| `Tests/VerdictUIKernelTests/BaselineStoreTests.swift`                | The destructive guard's refusals, the append-only audit log, name escaping, and SHA-256 against the published FIPS vectors                              | Active | 2026-08-11 |
| `Tests/VerdictUICLICoreTests/VerdictEngineTests.swift`               | The command surface in-process — three-valued exit codes, and the Wave 5 baseline round trip (create → FAIL → accept → PASS → audit)                    | Active | 2026-08-11 |
| `Tests/VerdictUICLICoreTests/CLIBinarySmokeTests.swift`              | Runs the BUILT binary — the only suite that can see a tool which refuses to start; negative-controlled via `xcrun xctest` (no.md #32)                   | Active | 2026-08-11 |
| `Tests/VerdictUIMacroTests/VerifiableMacroDiagnosticsTests.swift`    | What the macros REFUSE — duplicate ids, unlabelled controls, misuse at the attachment site; split from the expansion snapshots at their own seam        | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/Daemon.swift`                              | Warm JSON-RPC daemon over a unix socket; `ok` reports whether it could LOOK, never what it saw; deliberately serves no baseline update                  | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/DaemonTransport.swift`                     | Unix-socket transport for the daemon — newline-delimited frames, stale-socket recovery, and malformed-request replies                                  | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/MCPServer.swift`                           | MCP tool catalog + dispatch into the SAME daemon method surface the CLI uses; `baseline_accept` deliberately absent                                     | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/MCPTransport.swift`                        | Stdio MCP transport — JSON-RPC framing, initialize/tools/call handling, notification silence, and compact tree rendering                               | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/TransportCommands.swift`                   | CLI command adapters for daemon and MCP transports — parse intent, start services, and report daemon status                                            | Active | 2026-08-11 |
| `Tests/VerdictUICLICoreTests/DaemonTransportTests.swift`             | Unix-socket daemon transport tests — bind conflicts, stale socket cleanup, framing, and real client/server exchange                                    | Active | 2026-08-11 |
| `Tests/VerdictUICLICoreTests/MCPTransportTests.swift`                | Stdio MCP transport tests — handshake, notifications, tool calls, JSON-RPC id echo, framing, and compact tree output                                  | Active | 2026-08-11 |
| `Tests/VerdictUICLICoreTests/MCPServerTests.swift`                   | Every advertised tool resolves to a method that answers; the destructive verb is asserted absent with a read-only control                               | Active | 2026-08-11 |
| `Tests/VerdictUICLICoreTests/JudgeCommandTests.swift`               | The kernel judges a tree no Swift renderer produced -- hand-written JSON, a clean control, the vacuity refusal, and malformed input as a tool error rather than a verdict                          | Active | 2026-08-16 |
| `Tests/VerdictUICLICoreTests/ProjectScenariosTests.swift`           | A consumer's scenarios must reach the CLI — manifest discovery, the walk that stops rather than borrowing an ancestor's, and a malformed manifest erroring instead of reading as absent   | Active | 2026-08-16 |
| `Sources/VerdictUICLICore/ProjectScenarios.swift`                    | Finds the invoking project's VerdictUI manifest by walking up; a malformed manifest errors rather than reading as absent, because the fallback for absent is the demo catalog        | Active | 2026-08-16 |
| `.verdictui/config.json`                                             | VerdictUI's own scenario manifest — the first consumer of the mechanism, which is what stops the tool telling itself its catalog is borrowed                       | Active | 2026-08-16 |
| `docs/tree-contract.md`                                             | The wire shape a non-Swift producer must emit for `verdictui judge`, with the exit-code contract and the textMetrics keys that are required rather than defaulted        | Active | 2026-08-16 |
| `.decisions/2026-025-judge-takes-a-tree-rather-than-rendering-foreign-ui.md` | Why `judge` accepts a caller-supplied tree instead of rendering foreign UI, and the four alternatives weighed | Active | 2026-08-16 |
| `contracts/mcp-tools.md`                                             | The MCP wire contract — the three-valued `ok` rule, wire-size budgets, and why the destructive verb is not offered                                      | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/CompactTree.swift`                         | Token-frugal wire format — parallel arrays + parent index + interned strings, carrying `textMetrics` so truncation stays judgeable                      | Active | 2026-08-11 |
| `Sources/VerdictUICLICore/CompactDelta.swift`                        | Token-frugal act delta — added subtrees share one flat node table (per-addition tables made it BIGGER than raw), empty lists omitted so an inert act costs 2 B | Active | 2026-08-12 |
| `Sources/VerdictUICLICore/StepWire.swift`                            | Serializable act result — delta plus verdict, after-tree only on request; `settled` kept separate from `status` because incomplete ≠ wrong              | Active | 2026-08-12 |
| `Tests/VerdictUICLICoreTests/ActToolTests.swift`                     | `act` — replay-to-after-tree losslessness, both wire budgets (512 B structural / 64 B inert), published key shape read as raw JSON, refusal vs FAIL     | Active | 2026-08-12 |
| `Tests/VerdictUICLICoreTests/DaemonTests.swift`                      | Daemon protocol — a FAILING verdict is an ANSWERED request, an unknown scenario is not; the destructive method is asserted absent                       | Active | 2026-08-11 |
| `Tests/VerdictUICLICoreTests/CompactTreeTests.swift`                 | Lossless round trip over every real demo render, the ≤2 KB budget, interning proof, and malformed-array refusal with a control                          | Active | 2026-08-11 |
| `Tests/VerdictUIProbeTests/SweepTests.swift`                         | `Sweep` — variants provably reach the render (keyed on layoutDirection, since dynamicTypeSize is inert on macOS), with a determinism control            | Active | 2026-08-11 |
| `Tests/VerdictUIProbeTests/StateMachineTests.swift`                  | Walks — construction guards (a state with no expectations is rejected), arrival falsified by a lying transition, fresh render per path                  | Active | 2026-08-11 |
| `Tests/VerdictUIProbeTests/DemoExpectationsTests.swift`              | Expectation DSL dogfooded on the demo catalog — test-target-owned sets, each falsified against its own render, both-direction coverage                  | Active | 2026-08-11 |
| `Tests/VerdictUIKernelTests/VacuousVerdictTests.swift`               | `vacuous-verdict` — a probeless tree may not report PASS, and the guard may not be disabled                                                             | Active | 2026-08-08 |
| `Tests/VerdictUIKernelTests/VerdictTests.swift`                      | Verdict status derivation and JSON round-trip                                                                                                          | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/VerdictEnvelopeTests.swift`              | Envelope encoding: timestamp form, `tree`/`delta`/`timing` presence and omission                                                                       | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/SchemaVersionTests.swift`                | `SchemaVersion` parsing and compatibility rules                                                                                                        | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/SchemaCompatibilityTests.swift`          | Decoder refuses a foreign schema major, accepts a newer minor                                                                                          | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/ContractFixtureTests.swift`              | Generates and pins `contracts/fixtures/` to the real encoder output                                                                                    | Active | 2026-08-04 |
| `Tests/VerdictUIKernelTests/KernelDocumentationTests.swift`          | Pins `docs/kernel.md` to the code: messages, severities, thresholds, role table                                                                        | Active | 2026-08-04 |
| `Tests/VerdictUIProbeTests/VerdictUIProbeTests.swift`                | Rendered probe tests: root-space frames, tree delivery, TextMetrics, first probe→kernel handshake                                                      | Active | 2026-08-04 |
| `Tests/VerdictUIProbeTests/ProbeLayoutTests.swift`                   | Transparency (size, placement, alignment guides incl. the zero-origin canary), recording, intrinsic/ideal measurements                                 | Active | 2026-08-04 |
| `Tests/VerdictUIProbeTests/PixelCaptureTests.swift`                  | Pinned 1x scale (negative-controlled against the device-scale rep), cross-process determinism, backend divergence asserted as a fact, zero-area refusal                | Active | 2026-08-12 |
| `Tests/VerdictUIProbeTests/PixelDeterminismTests.swift`             | Refusal + positive control + a fixture-is-really-drifting assertion (two earlier fixtures could not fail — see the fixture doc comment)                | Active | 2026-08-13 |
| `Tests/VerdictUIKernelTests/PixelDiffTests.swift`                    | Comparison rules on hand-built rasters: the delta separates what the pixel count cannot, a 1-px regression fails (with the 5%-allowance swallow as negative control), region bounds, refusals, heat-map intensity                | Active | 2026-08-13 |
| `Tests/VerdictUIProbeTests/PixelCompareTests.swift`                  | Real captures: the existence proof (identical trees + identical rule findings, yet the pixel channel sees the border colour), cross-backend and dimension refusals, artifacts written only on failure and cited by path, path-traversal guard                | Active | 2026-08-13 |
| `Tests/VerdictUIProbeTests/PixelCacheTests.swift`                    | SD4 invalidation table (every key component misses alone, with an unchanged-key control), per-axis variant coverage, corrupt/orphan entries miss, unwritable cache never fails a capture, and the warm-vs-cold speedup measured rather than assumed                | Active | 2026-08-13 |
| `Tests/VerdictUICLICoreTests/PixelSurfaceTests.swift`                | Wave 9 Task 6 surfaces: `render --pixels` reports a path to a real PNG, the payload is smaller than the image it replaced (so it cannot be carrying the bytes), cache hit reported end to end, and the daemon/MCP wire shapes read with a foreign parser                | Active | 2026-08-13 |
| `Tests/VerdictUIProbeTests/TreeAssemblyTests.swift`                  | Pure assembly: containment nesting, sibling order, root synthesis, honest metrics                                                                      | Active | 2026-08-04 |
| `Tests/VerdictUIProbeTests/ScenarioTests.swift`                      | `VerdictScenario`/`ScenarioState` contract                                                                                                             | Active | 2026-08-04 |
| `Tests/VerdictUIProbeTests/OracleHostTests.swift`                    | Harness: settle primitive, deterministic env pins, clamp, deadline failure surface                                                                     | Active | 2026-08-04 |
| `Tests/VerdictUIProbeTests/VerdictClockTests.swift`                  | Virtual clock advance/sleep, env injection, settlePolicy skip vs runAnimations flush                                                                   | Active | 2026-08-05 |
| `Tests/VerdictUIProbeTests/HarnessTests.swift`                       | Task 4 act-and-observe: every perform() return path, flow batching, stoppedEarly vs failure, includeTree seam                                          | Active | 2026-08-06 |
| `Tests/VerdictUIProbeTests/HostileSettleTests.swift`                 | Task 5 hostile suite: perpetual motion, deferred mutation, debounced input, rapid actions — each with a control                                        | Active | 2026-08-06 |
| `Tests/VerdictUIProbeTests/HarnessPerformanceTests.swift`            | Task 6 SLO 1: act→settle→verdict p50/p95 (`SLO1-PERFORM`), fast-fail on a rejected act                                                                 | Active | 2026-08-06 |
| `Sources/VerdictUIProbe/ConstrainedTimingEnvironment.swift`          | The timing lane's membership test, spelled once (`no.md` #17) — mirrors `CONSTRAINED_TIMING_ENV_MARKERS` in the PM; moved out of the probe test target so SLO 3 in `VerdictUICLICoreTests` shares it rather than copying it | Active | 2026-08-12 |
| `Tests/VerdictUIProbeTests/ConstrainedTimingEnvironmentTests.swift`   | Swift regression coverage for the constrained-timing lane: write-probed SwiftPM cache detection, cleanup of probe files, and `isActive` parity          | Active | 2026-08-17 |
| `Tests/VerdictUIProbeTests/LateMotionScenario.swift`                 | Static until tapped, then never settles — the only fixture that reaches `perform`'s settle-timeout branch (CTS-8DDE6D09 item 2)                        | Active | 2026-08-09 |
| `Tests/VerdictUIProbeTests/SettleTests.swift`                        | Quiescence: quiet settle, virtual-clock waiter block, oscillating timeout + FAIL verdict                                                               | Active | 2026-08-05 |
| `Tests/VerdictUIProbeTests/ActionDiscoveryTests.swift`               | actionableProbes on state/host/catalog; negative control that an unbound probe is absent; discovery agrees with act's refusal                          | Active | 2026-08-18 |
| `Tests/VerdictUIProbeTests/ActionInjectionTests.swift`               | ProbeAction toggle/tap/custom/setText/setSlider; unknown probe error; ToggleLayoutScenario                                                             | Active | 2026-08-05 |
| `Tests/VerdictUIProbeTests/DemoIntegrationTests.swift`               | End-to-end proof: every planted defect caught by exactly the right rule; 10× determinism; staged-layout settle arm                                     | Active | 2026-08-04 |
| `Tests/VerdictUIProbeTests/OraclePerformanceTests.swift`             | SLO 1 baseline: warm `currentTree()` p95 < 100 ms gate (`docs/slo.md`) + cold-construction guard                                                       | Active | 2026-08-05 |
| `Tests/VerdictUIDemoScenariosTests/DemoScenarioCatalogTests.swift`   | Catalog: pinned count, unique stable names, lookup, viewports                                                                                          | Active | 2026-08-04 |
| `Tests/VerdictUIDemoScenariosTests/DemoScenarioDeterminismTests.swift` | Wave 9 exit gate: every demo scenario renders identically twice, with a vacuity guard so an empty catalog fails rather than passes                  | Active | 2026-08-13 |
| `Tests/VerdictUIDemoScenariosTests/DemoScenarioRenderingTests.swift` | Every scenario settles unclamped; declared probe ids ≡ rendered probe ids                                                                              | Active | 2026-08-04 |
| `Tests/VerdictUIDemoScenariosTests/CleanSettingsScenarioTests.swift` | Zero findings + the exonerated overlap is real (and fires when undeclared)                                                                             | Active | 2026-08-04 |
| `Tests/VerdictUIDemoScenariosTests/ToggleLayoutScenarioTests.swift`  | Both toggle states render clean under the same name                                                                                                    | Active | 2026-08-04 |
| `Tests/VerdictUIDemoScenariosTests/DemoReportTests.swift`            | Executable core: one verdict per scenario in catalog order, valid JSON                                                                                 | Active | 2026-08-04 |
| `Tests/test_verdictui_pm.py`                                         | Python tests for PM stages, floor-check, and the contract validator                                                                                    | Active | 2026-08-04 |
| `Tests/test_kernel_symbol_audit.py`                                  | Python tests for the kernel public-surface audit                                                                                                       | Active | 2026-08-04 |
| `Tests/test_mutation_check.py`                                       | Python tests for the mutation harness: catalog integrity, baseline precondition, both runners                                                          | Active | 2026-08-04 |
| `Tests/test_claude_md_ssot.py`                                       | Pins `CLAUDE.md`: SSoT rows resolve to real files and symbols, every referenced path exists                                                            | Active | 2026-08-04 |
| `Tests/test_cli_version_identifies_the_release.py`                   | Holds `verdictui --version` to the BUILD, not the wire schema — the v1.0.1 tarball printed `1.1`, matching no release (CTS-959AB320)                   | Active | 2026-08-28 |
| `Sources/VerdictUIMacros/VerdictUIMacroPlugin.swift`                 | Compiler-plugin entry point — the macro registry the toolchain resolves against                                                                        | Active | 2026-08-06 |
| `Sources/VerdictUIMacros/VerifiableMacro.swift`                      | `@Verifiable` member macro: generates `verdictProbedBody(into:)`, diagnoses misuse at the attachment site                                              | Active | 2026-08-06 |
| `Sources/VerdictUIMacros/BodyProbeWalk.swift`                        | The body walk: attaches `.verdictProbe(id:role:text:)` to recognised elements, skipping any that already carry one                                     | Active | 2026-08-06 |
| `Sources/VerdictUIMacros/ProbeLintFinding.swift`                     | Compile-time lint findings the walk collects — duplicate explicit id (error), interactive element with no label (warning + fix-it)                     | Active | 2026-08-10 |
| `docs/adoption.md`                                                   | Wave 4 Task 6 — three adoption tiers, probe placement, composition, diagnostics, limitations table, migration steps, measured build cost              | Active | 2026-08-10 |
| `docs/dogfood-session.md`                                            | Wave 7 exit gate — the verbatim edit → FAIL (cited) → fix → PASS transcript, warm MCP latency, and the handshake defect the dogfood itself found       | Active | 2026-08-12 |
| `.mcp.json`                                                          | Project-scoped MCP registration — makes `verdictui mcp` reachable from an editor in THIS repo only; gated by `test_verdictui_gates.py` for verb + path | Active | 2026-08-12 |
| `Tests/VerdictUICLICoreTests/MCPLatencyTests.swift`                  | SLO 3 — warm MCP round-trip latency through the real stdio transport of the BUILT binary; gates p50, records p95                                       | Active | 2026-08-12 |
| `Tests/VerdictUICLICoreTests/FocusToolTests.swift`                  | `focus(nodePath)` as a ROUND TRIP — every path a truncated tree publishes must resolve — plus the wire shape a foreign client actually reads | Active | 2026-08-12 |
| `Tests/VerdictUICLICoreTests/CrossValidateFlagTests.swift`           | Wave 8 Task 5 — proves `--cross-validate` is opt-in across CLI-core, daemon, and MCP wiring, and that `crossValidateMs` survives the wire                | Active | 2026-08-12 |
| `Tests/VerdictUIMacroTests/TwoTokenAdoptionTests.swift`              | The Wave 4 exit-gate claim asserted: an unadorned view + two tokens yields a non-vacuous verdict whose tree carries the content                        | Active | 2026-08-10 |
| `Sources/VerdictUIMacros/VerdictScenarioMacro.swift`                 | `#VerdictScenario` freestanding macro: expands to a `VerdictScenario` conformance plus its `ScenarioEntry`                                             | Active | 2026-08-10 |
| `Sources/VerdictUIMacroSupport/Verifiable.swift`                     | The `@Verifiable` declaration a consumer imports — the opt-in surface that costs SwiftSyntax                                                           | Active | 2026-08-06 |
| `Sources/VerdictUIProbe/ScenarioRegistry.swift`                      | `ScenarioEntry` / `ScenarioRegistry` — static registration; erases construction, not the scenario                                                      | Active | 2026-08-10 |
| `Tests/VerdictUIMacroTests/VerifiableMacroTests.swift`               | Expansion snapshots: happy path, generics, getter body, and every misuse diagnostic                                                                    | Active | 2026-08-06 |
| `Tests/VerdictUIMacroTests/VerifiableCompilationTests.swift`         | The compiling half — macro tree vs hand-probed twin; a snapshot cannot see code that names nothing                                                     | Active | 2026-08-06 |
| `Tests/VerdictUIMacroTests/RoleVocabularyTests.swift`                | Pins the walk's role strings against the kernel's `Role` — the plugin cannot import the kernel, so the two spellings only meet here                    | Active | 2026-08-06 |
| `Tests/VerdictUIMacroTests/VerdictScenarioMacroTests.swift`          | Expansion snapshots for `#VerdictScenario`: conformance, entry, name→type transform, diagnostics                                                       | Active | 2026-08-10 |
| `Tests/VerdictUIMacroTests/VerdictScenarioCompilationTests.swift`    | The compiling half — macro-declared scenarios hosted for real, plus registry lookup and duplicate reporting                                            | Active | 2026-08-10 |
| `Tests/test_macro_isolation.py`                                      | Pins macro optionality: SwiftSyntax never reaches a shipping target, plugin stays `.macro`, version pinned `exact`                                     | Active | 2026-08-06 |
| `Tests/test_file_registry.py`                                        | Pins this registry: every source file has a row, every row names something real                                                                        | Active | 2026-08-04 |
| `Tests/__init__.py`                                                  | Python package marker for pytest collection                                                                                                            | Active | 2026-08-04 |
| `pyproject.toml`                                                     | Fleet tooling config (ruff/mypy/pyright/pytest) — no [project] section                                                                                 | Active | 2026-08-04 |
| `docs/kernel.md`                                                     | Kernel reference: role vocabulary, rule catalog, diff and schema reference                                                                             | Active | 2026-08-04 |
| `docs/wave-status.md`                                                | Session-continuity SSoT — resume point, wave checklist, session log                                                                                    | Active | 2026-08-04 |
| `docs/business-decisions.md`                                         | Business/marketing decision history — founding Q&A, open-core model, naming                                                                            | Active | 2026-08-04 |
| `scripts/verdictui-pm.py` | PM entrypoint — CLI, `VerdictUIPM` composition, stage order, query mode | Active | 2026-08-30 |
| `scripts/verdictui_pm_support.py` | PM shared state — project paths, constants, logging, contention probe, timing lane. SINGLE OWNER of every name the suite monkeypatches, reached as `S.<name>` so a patch binds at call time | Active | 2026-08-30 |
| `scripts/verdictui_pm_swift.py` | PM SwiftPM layer — lock files, runner acquisition, streamed test, locked product build, process-group termination | Active | 2026-08-30 |
| `scripts/verdictui_pm_stages.py` | PM stage mixin — build, test, floor, contracts, architecture, lint, demo, governance wrappers | Active | 2026-08-30 |
| `scripts/verdictui_pm_smoke.py` | PM stage mixin — CLI/transport smoke, mutation catalog, installed parity, stale buffer, SLO benches, pytest | Active | 2026-08-30 |
| `scripts/floor-check.py`                                             | Floor compliance audit                                                                                                                                 | Active | 2026-08-04 |
| `scripts/kernel-symbol-audit.py`                                     | Exit-gate checker: every public kernel symbol documented and mentioned by a test                                                                       | Active | 2026-08-04 |
| `scripts/mutation-check.py`                                          | Mutation harness — breaks a guard on purpose and fails if no test notices                                                                              | Active | 2026-08-04 |
| `scripts/mutation_catalog/__init__.py` | Reassembles `MUTATIONS` from the parts below in a fixed order; separate from the harness so a row may quote harness code without self-matching (`no.md` #16) | Active | 2026-08-28 |
| `scripts/mutation_catalog/_probe.py` | Mutation rows targeting `Sources/VerdictUIProbe` | Active | 2026-08-28 |
| `scripts/mutation_catalog/_kernel.py` | Mutation rows targeting `Sources/VerdictUIKernel` | Active | 2026-08-28 |
| `scripts/mutation_catalog/_macros.py` | Mutation rows targeting `Sources/VerdictUIMacros` | Active | 2026-08-28 |
| `scripts/mutation_catalog/_cli.py` | Mutation rows targeting the CLI, witness and demo scenarios | Active | 2026-08-28 |
| `scripts/mutation_catalog/_scripts.py` | Mutation rows targeting the Python harness under `scripts/` | Active | 2026-08-28 |
| `scripts/mutation_catalog/_misc.py` | Mutation rows targeting `Tests/`, `Package.swift`, docs and CLAUDE.md | Active | 2026-08-28 |
| `scripts/stale-buffer-check.py`                                      | Detects a tracked file overwritten by a stale editor buffer — mtime older than the commit that touched it (CIS-638133AE)                                | Active | 2026-08-09 |
| `Tests/test_stale_buffer_check.py`                                   | Guards the stale-buffer detector, mostly controls: an ordinary fresh edit must NOT fire                                                                 | Active | 2026-08-09 |
| `scripts/mutation_catalog_types.py`                                  | `Mutation` + `Runner`, shared by harness and catalog — a third module so the import is not a cycle                                                      | Active | 2026-08-09 |
| `scripts/dev.sh`                                                     | One-command setup + build + test                                                                                                                       | Active | 2026-08-04 |
| `.github/scripts/create-actions-update-issue.cjs`                    | Opens/refreshes ONE issue when a pinned action goes stale — idempotent, so a weekly cron does not pile up duplicates                                    | Active | 2026-08-09 |
| `Tests/pm_test_support.py`                                           | Loader preamble shared by the PM-stage and gate-script suites, so two copies cannot drift (CTS-E51CBEEB)                                                | Active | 2026-08-09 |
| `Tests/test_verdictui_gates.py`                                      | `floor-check.py` + `validate-contracts.py` — the gate SCRIPTS the PM shells out to, split from the stage tests                                          | Active | 2026-08-09 |
| `Tests/test_verdictui_bench.py`                                      | `stage_runtime_bench` + the no-sleeps guard — every way the timing gate could report health it did not measure                                          | Active | 2026-08-09 |
| `Tests/test_verdictui_failclosed.py`                                 | `stage_pytest` + every stage's behaviour when its tool is absent — a check that cannot observe its subject must never PASS                              | Active | 2026-08-09 |
| `Tests/conftest.py`                                                  | Puts `Tests/` on `sys.path` for the shared helper; pairs with `extraPaths` in `pyproject.toml`                                                          | Active | 2026-08-09 |
| `pm-baselines.json`                                                  | PM performance baselines — tracked, not gitignored (ADR 2026-002)                                                                                     | Active | 2026-08-06 |
| `contracts/verdict-schema.json`                                      | Pinned verdict wire format (v1.0)                                                                                                                      | Active | 2026-08-04 |
| `contracts/validate-contracts.py`                                    | Contract gate: schema integrity, version agreement, fixture round-trip                                                                                 | Active | 2026-08-04 |
| `contracts/fixtures/verdict-pass.json`                               | Generated fixture — clean verdict, pins the field-omission contract                                                                                    | Active | 2026-08-04 |
| `contracts/fixtures/verdict-fail.json`                               | Generated fixture — every optional field populated at once                                                                                             | Active | 2026-08-04 |
| `Tests/VerdictUIMacroTests/PublicVerifiableViewTests.swift`          | `@Verifiable` on a `public` view — the population every other macro fixture omits, since a type nested in an XCTestCase cannot be public                | Active | 2026-08-14 |
| `Tests/VerdictUIKernelTests/TapTargetPlatformMetricsTests.swift`     | The `tap-target` floor against MEASURED native macOS control sizes, both directions — every standard control accepted, a 6×6 pt control still reported | Active | 2026-08-14 |
| `Tests/test_verdictui_pm_artifact_stages.py`                         | PM stages whose subject is a PROCESS (CLI smoke, transport smoke, sweep-in-progress) — split from `test_verdictui_pm.py` along that seam                | Active | 2026-08-14 |
| `Tests/test_pm_process_group.py`                                     | `_terminate_process_group` against REAL process groups — spawns actual children with `start_new_session=True` and asserts they are gone; split from `test_verdictui_pm.py` (CTS-C37A9949) because a process-lifecycle subject is distinct from stage wiring                     | Active | 2026-08-18 |
| `docs/bench/mcp-batch.sh`                                            | Warm MCP round-trip benchmark through the shipped binary; self-asserts reply count and refuses error replies                                            | Active | 2026-08-14 |
| `docs/bench/flake-100.sh`                                            | Flake measurement in BOTH directions plus evidence stability on the failing path                                                                       | Active | 2026-08-14 |
| `docs/dogfood/sagamail/Package.swift`                                | Wave 10 fleet dogfood — a consumer package depending on VerdictUI through its published products                                                       | Active | 2026-08-14 |
| `docs/dogfood/sagamail/Sources/SagaMailDogfood/NotificationsSettingsScreen.swift` | A SagaMail settings screen adopted via `@Verifiable`; reproduces the real view's Form/conditional/ForEach/Toggle shapes                    | Active | 2026-08-14 |
| `docs/dogfood/sagamail/Tests/SagaMailDogfoodTests/NotificationsSettingsVerdictTests.swift` | The dogfood verdicts, expectations owned by the CONSUMER's test target, with a cramped-viewport control                       | Active | 2026-08-14 |
| `docs/audit-2026-08-26.md`                                           | Wave 10 Task 3 hardening audit — 10 phases + SD1–SD6, the fuzz negative control, and the fresh-clone PII scan that cleared the public repo               | Active | 2026-08-26 |
| `evidence/run-v2.json`                                               | Governance-V2 S20 typed run-v2 record, committed by CI so the evidence outlives the run that produced it (an uploaded artifact expires; the committed record does not) | Active | 2026-08-29 |

## Archived / Removed

| File                                                    | Reason                                                                                    | Removed    |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- |
| `Tests/VerdictUIKernelTests/VerdictUIKernelTests.swift` | Split into per-area suites (SemanticNode/TreeDiff/RuleEngine/Rules/Verdict) during Wave 1 | 2026-08-04 |

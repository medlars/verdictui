{
  "runner": ".build/debug/VerdictUIProjectRunner",
  "buildProduct": "VerdictUIProjectRunner",
  "note": "This runner explicitly registers VerdictUI's own test scenarios. It is self-test coverage, never evidence about another consumer. The installed launcher rebuilds this disposable host when project sources change."
}

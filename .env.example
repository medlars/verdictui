# VerdictUI — environment variables
# None required at scaffold: the tool is local-first by design.
# Placeholder for future daemon/MCP configuration (Wave 6+):
# VERDICTUI_DAEMON_PORT=          # override default daemon port
# VERDICTUI_BASELINE_DIR=         # override baseline storage location
